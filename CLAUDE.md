# Claude Code entry point

@AGENTS.md

Это дом продакта. Правила исполнения задач, профили и гейты сюда не
импортируются: они живут в репозитории системы задач — его путь эта установка
называет в `threads.json` — и применяются там, куда работа реально уходит.
