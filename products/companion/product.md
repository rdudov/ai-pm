# Companion (Calypso и Max)

## Концепция
Личный AI-собеседник в Telegram с долговременной памятью: помнит историю, ведёт
планы и напоминания, говорит голосом, умеет отдать тяжёлую работу агенту и
вернуть файл. Ядро одно — пакет `companion` (память, роутинг LLM, файловый
контракт, статистика). Поверхности разные: Calypso — личный контур с голосом и
планами, Max — многопользовательский контур с изолированными файловыми задачами.

Разделять на два продукта не стоит: по истории задач подавляющее большинство
работ общие (модели, память, файловые ответы, стоимость, статистика), и
раздельные записи дублировали бы одно и то же. Непересекающиеся функции живут в
таблицах поверхностей ниже.

## Аудитория (по базам разговоров на 2026-08-04)
- **Calypso — один пользователь**, ты. Активность падает: 512 сообщений в марте,
  390 в апреле, 343 в мае, 232 в июне, 173 в июле. Тренд вниз втрое за полгода.
- **Max — 5–8 активных пользователей в месяц**, стабильно 650–1170 сообщений.
  Кроме тебя это как минимум четыре человека с живой активностью за 90 дней.
  Это единственный продукт с настоящими внешними пользователями.

## Общие пользовательские пути (обе поверхности)
| путь | кто | чем запускает | что получает | состояние |
| --- | --- | --- | --- | --- |
| разговор с памятью | все пользователи | обычное сообщение | ответ с учётом истории и фактов | сжатая история работает; **блок фактов измеренно врёт** (796, живая проба той же службой поиска и той же коллекцией): на вопрос о возрасте верная запись третья, на имя вымышленное выше настоящего, на город верного ответа нет ни одного из 15. Кросс-ревью 805 уточнило границу: проба спрашивала хранилище напрямую, а не через сборку запроса продукта, поэтому измерено поведение хранилища, а не итогового ответа бота; перезамер боевым путём — в 808 |
| ответ файлом | все пользователи | просьба «пришли файлом» | документ нужного формата | работает |
| навыки и инструменты | все пользователи | просьба в свободной форме | вызов навыка/инструмента | работает |

Это доминирующий путь: команд в базе почти нет, люди просто разговаривают.

## Calypso: свои пути
| путь | чем запускает | что получает | состояние |
| --- | --- | --- | --- |
| голосовой ответ и распознавание | голосовое сообщение | синтез Silero, ASR | работает |
| планы квартал/месяц/неделя | запрос плана | список и добавление пунктов | работает |
| напоминания | запрос напоминания | запись в истории разговора | работает |
| `/codex` задача | `/codex <запрос>` | прогон с полными правами и доставкой | работает; с 2026-08-05 подтверждено живой командой (задача 738): приходит ответ по-русски и отдельное заключение файлом |
| личности бота | выбор персонажа | смена стиля общения | **наследие, решено 2026-08-06 письмом пользователя**: пользователь общается только с Эльфионой, роли Калипсо (`tender`, `spicy`, `balanced`) убираются задачей 812; варианты `elfiona_*` остаются как выбор модели, а не роли. С 2026-08-06 их стало на один больше: коммит calypso `9c4862a` добавил `elfiona_kimi` — модель Kimi K3 через OpenRouter, серверная половина которой лежала в companion `3c18328` с 4 августа. Это выбор модели, и 812 его не трогает |
| доступ к боту | любое сообщение | ответ | **закрыт: отвечает только владельцу** (811, выкачено 2026-08-06 17:34, calypso `ba38945` + companion `3124d73`). Чужому — строка «Извини, это личный бот», без вызова модели и без записи в историю и память; правило стоит внешней middleware до всех обработчиков. Живая проба на боевой границе: 9 из 9 обращений постороннего (текст, `/codex`, `/start`, голос, фото, документ, медиагруппа) получили только отказ, счётчики базы до и после совпали. Настоящее сообщение владельца из Telegram получило ответ. Кросс-ревью Codex (819) закрытие подтвердило по коду, но вернуло работу на доработку: отредактированное сообщение чужого отказа не получает — бот такой тип у Telegram не забирает вовсе, поэтому до модели, истории и памяти он тоже не доходит; неверно обещание, а не защита (823) |
| своя жизнь Эльфионы | разговор о ней самой | рассказ о её делах | **решено 2026-08-06**: жизнь есть, база пишется человеком в канон роли, дальше сценарий ведёт модель ситуативно; отдельного журнала её событий не заводим — пользователь просил не тратить на это контекст. Работа — 812 |

## Max: свои пути
| путь | чем запускает | что получает | состояние |
| --- | --- | --- | --- |
| файловая задача в изоляции | `/task <запрос>` | готовый файл в чате | работает |
| продолжить задачу | `/task_resume [NNN]` | та же задача с новым входом | работает |
| индекс задач | `/task_index` | `task-index.xlsx` | работает после выкатки 2026-08-05 11:27:47 (2a8e061/f30af04): живой вызов в 12:06:14 отдал принципалу 340236295 его 35 задач вместо каталога на 630; вызов от 503043504 (8 задач) ещё не повторён |
| браузерная проверка HTML агентом | внутри задачи | визуально проверенный HTML | работает |
| `/codex` из Max | `/codex <запрос>` (два администратора) | прогон агента **прямо на хосте, с полными правами**, не в контейнере | работает; записан 2026-08-06 по ревью 797 (`codex_agent/runner.py:33`, `:802-828`) — путь существовал, но в продуктовой записи его не было. **Путь убран целиком 2026-08-06** (811, max `862c9d4` + companion `3124d73`): команд `/codex`, `/codex_resume` и `/cursor` в Максе больше не существует ни у одного из двух администраторов — это не проверка внутри команды, а отсутствие маршрута. Живая проба: ни одна из форм (команда, подпись к вложению) не совпала ни с одним обработчиком агента, запускатель не вызван; в `/start` этих строк нет. Осталось 24 маршрута, ушли ровно пять хостовых. Кросс-ревью Codex (819) подтвердило закрытие независимо: обхода через подписи, альбомы, пересылки, алиасы с именем бота и регистр нет, потому что нет самих обработчиков; остальные запуски процессов из Макса не дают выполнить произвольную команду. Замечания ревью касаются приватности артефактов и типов сообщений, а не этого пути (823) |

## Текущая ставка
**Переделать память: от извлечённых «фактов» к сырым данным с ручками поиска.**
Выбрана 2026-08-04 по прямому указанию пользователя и подтверждена свидетельством
изнутри продукта.

Свидетельство — ответ Calypso на прямой вопрос «как тебе организация контекста»
(разговор 2026-07-27, `bot_data/conversation_history.db`). Её собственный вердикт:
сжатая история работает, **извлечённые факты наполовину мусор**. Четыре названные
болезни:

1. нет базовых констант (возраст, имена, профессии) — при этом уверенные выводы
   поверх пустоты, вплоть до неверного возраста пользователя;
2. перекос темы: 10 из 14 фактов про кризис в отношениях, ноль про яхту, работу,
   инвестиции — в памяти живёт версия пользователя, состоящая из проблем, и это
   загоняет ассистента в роль кризисного консультанта;
3. оценочные формулировки от третьего лица («пользователь испытывает сложности
   с…») тянут в клинический тон;
4. датировка без статуса: непонятно, что ещё актуально, а что прожито и закрыто.

Отдельно — резюме описывает **роль ассистента** оценочно («ассистент пресёк»), и
модель начинает играть свой конспект.

Гипотеза решения: подход памяти companion-agent — сырые данные плюс инструменты
поиска по ним, вместо предварительной экстракции фактов. Это снимает разом
пункты 2–4: не извлекаем — значит не перекашиваем, не оцениваем и не протухаем.
Это продуктовое решение, а не техническое: исследуется отдельно, см. «В работе».

Экономический довод в ту же сторону: за 90 дней у Max 3656 из ~8000 вызовов LLM —
`memory_deduplicate`, у Calypso 754 против 202 `main_answer`. Почти половина
расхода уходит на обслуживание той самой экстракции, качество которой продукт сам
называет наполовину мусором.

Отложено (не снято): **затухание Calypso** — личный контур используется втрое
реже, чем полгода назад. Ставка на память отвечает и на это: если причина в
качестве памяти, метрика оживёт без сужения продукта.

## Не делаем
- Не дублируем lifecycle-логику в ботах: транспорт остаётся транспортом.
- Не ставим Firefox в обход отказа Chromium в Max (контракт v4 запрещает).
- Не ослабляем изоляцию контейнера Max ради удобства агента.

## В работе
- 2026-08-06 23:2x — **Разбор памяти Calypso и Max доставлен вместе с замечаниями встречной проверки.** Документ 796 лежал готовым и человеку не уходил. Отправлено письмом `19fd8d2ef212b626` (sha256 `3c31449d…`), свидетельство записано в задачу. В письме передано главное содержательное: все четыре названные болезни подтверждаются, механическая причина у них одна, но экономический довод верен лишь наполовину — на обслуживание извлечённых фактов уходит почти половина вызовов модели и только 2,6–2,9% денег, поэтому переход на сырые данные защищается качеством, а не экономией. Замечания 805 переданы целиком, включая три, меняющие доверие к числам (знаменатель без 73 неудачных ответов — 53,3/51,0 вместо 55,5/57,1; несохранённая ручная разметка; замер поиска мимо боевого пути), и сказано прямо, что проценты качества принимать пока не стоит. Перед отправкой проверено, что в самом документе нет ни телеграмных идентификаторов, ни фрагментов чужой переписки.
- 2026-08-06 19:4x — **Разбор пользователей Max доставлен пользователю (822, письмо `19fd82e74b60aa68`, вложение `max-users-report.md`, sha256 записан в `delivery.md` задачи).** Отчёт меняет очередь работ по Максу, и это первое, что в нём важно: внешних людей десять, а не четыре, живых шесть, и двое дают 81% переписки; продуктом пользуются как рабочим инструментом (две трети сессий — тексты и рабочая экспертиза), а он отвечает ролеплеем — 34 прямые претензии к стилю. **Изоляция пользователей друг от друга перестаёт быть срочной по наблюдению, а не по мнению:** память в хранилище разделена по людям, лексический скрин 1922 редких личных слов дал 115 подозрений, все разобраны руками, переноса содержания одного человека в ответ другому нет. Взамен наверх выходит потеря нити внутри разговора одного человека — 39 сессий, где бот путает его же темы, имя, пол и семейное положение, и это главная жалоба самого крупного пользователя. Напоминания и планы предлагается снять совсем: за 13 месяцев напоминание фигурировало в одной сессии из 760. Приватность соблюдена как требование: метки U1…U10, ни имён, ни контактов, скрин артефактов перед отправкой. Смысловую разметку 760 сессий сделала дешёвая модель за $0,50 с нулём сбоев, механический счёт воспроизводится побайтово.
- 2026-08-06 18:0x — **Кросс-ревью 819 (Codex) вернуло 811 на доработку: CHANGES REQUESTED, два пункта, оба приняты продактом.** Само закрытие доступа ревью подтвердило по коду и признало верным: пяти маршрутов хостового агента в Максе действительно нет и обхода через подписи, альбомы, пересылки, алиасы и регистр не нашлось; остальные запуски процессов не дают передать строку пользователя в shell; у Калипсо посторонний отсекается внешней прослойкой до фильтров, так что модель, история и память не вызываются; пустой или битый список допуска валит запуск до поллинга, а не открывает бота молча; 24 маршрута Макса и допуск обоих администраторов к контейнерным задачам на месте. Не принято другое. P1 — приватность наших же записей: в доказательствах 811, в скриптах проб и в пользовательском заключении лежат числовые идентификаторы владельца и второго администратора, имена ботов и дословные реплики настоящего разговора; это повтор дефекта, который кросс-ревью 805 уже отмечало строкой FAIL, и пункт 6 контракта 819 требовал именно этого — правило не придумано под вердикт. P2 — обещание «любое сообщение чужого получает отказ» неверно для отредактированного сообщения: прослойка стоит на `message` и `callback_query`, а `edited_message` бот у Telegram вообще не забирает, поэтому отказа нет; опасности нет (пути к модели, истории и памяти у этого типа не существует), неверно только обещание. Ограничение самого ревью названо честно: pytest 811 переповторить не удалось (общая fixture пишет лог вне разрешённого каталога), службы не трогались по условию read-only. Правки поставлены задачей 823 обычным порядком «анализ с явным согласен/не согласен → план → правки → повторное ревью»; делает Claude как автор 811, повторно проверяет Codex. Заключение 811 пользователю **не отправлено и отправлено не будет в нынешнем виде** — оно и есть один из носителей дефекта P1; письмо уйдёт после обезличивания. Задачи 812 (роли и канон Эльфионы) и 813 (разделение людей в Максе) по-прежнему ждут чистых деревьев companions, 808 (правки памяти) готова стартовать без условий. Write-прогонов на этом пробуждении не запускалось.
- 2026-08-06 17:5x — **Открытый вопрос «кто четыре внешних пользователя Max»
  закрыт пользователем как способ, а не как ответ.** Дословно: «вся информация
  есть в переписке пользователей, можешь взять последние сообщения и посмотреть,
  что они делали», с разрешением подключить дешёвую модель уровня
  `gemini-flash-lite` для обработки объёма. Поставлено задачей 822: механический
  счёт кодом и воспроизводимой командой, смысловая часть — дешёвой моделью
  партиями с записью стоимости. Приватность записана как требование, а не как
  оговорка: люди не наши сотрудники, поэтому в артефакты не идут имена,
  контакты и лишние цитаты, пользователи обозначаются устойчивыми метками.
  Продуктовый смысл — не любопытство: Max единственный продукт с настоящими
  внешними пользователями, а в очереди уже стоит изоляция пользователей друг от
  друга, и её срочность без понимания сценариев определяется гаданием.
- 2026-08-06 17:47 — **811 сделана и выкачена, отправлена на кросс-ревью Codex (819, идёт).** Три коммита в деревьях companions (`3124d73`, `ba38945`, `862c9d4`), деревья чистые, обе службы перезапущены в 17:34:57 и подняли поллинг без трассировок. Verification — семь строк, все OK, ни одного GAP; гейт держат живые пробы на боевой границе, а не pytest (полный набор companion на этом стенде не собирается и до изменения — это записано честно). Сверх заказа исполнитель закрыл третью дверь того же класса: `/cursor` в Максе запускала агента на хосте с доступом к почте и телеграму, и закрыта тем же решением «опасная возможность ровно в одном месте»; задача такой выбор разрешала. Продакт результат **не принимает и не возвращает до вердикта 819** — работу делал Claude, проверяет Codex, бюджет Codex 21% недельного окна, прогон только на чтение. Одно наблюдение исполнителя не относится к 811 и заведено выше как открытый вопрос: `/research` и `/gpt_research` в Максе не проверяют вообще никого. Задачи 812 (роли и канон Эльфионы) и 813 (разделение людей в Максе) ждут вердикта 819, чтобы не писать в те же деревья; у правок памяти 808 условие «дождаться 806 и 811» снято полностью, и они готовы стартовать первыми — на этом пробуждении write-прогоны не запускались.
- 2026-08-06 16:45 — **806 принята продактом: прогон завершился, verification без единого GAP (шесть строк, все OK), доработки не требуется.** Проверено по артефактам: каждая ссылка R1–R9 и D1–D7 открыта в исходниках, агрегаты баз воспроизведены без чтения переписки, минимальный набор `test_message_batching.py` — 21 из 21 за 30 с на копии неизменённого дерева, продуктовые репозитории не правились. Выводы 806 уже разложены в работы строкой ниже; нового решения задача не требует. Побочное наблюдение в том же треде: незакоммиченный эксперимент пользователя с личностью Kimi лежал в дереве calypso с 4 августа, и через три минуты после старта 811 он стал коммитом `9c4862a` — содержимое цело, других работавших в дереве не было, так что подпись под ним наша. Правки по кросс-ревью памяти (808) стартовать готовы — их единственное условие «дождаться 806» снято, — но запуск держится до конца 811: 811 идёт в тех же трёх деревьях companions, а два ребёнка в одном рабочем дереве уже один раз заблокировали приёмку. Живых прогонов по треду один — 811, pid 2270195 жив, прогресс свежий.
- 2026-08-06 — **кросс-ревью ревью кода закончено (806): `CHANGES REQUESTED`, и
  оно меняет, что делать первым.** Codex открыл каждую ссылку R1–R9 и D1–D7.
  Подтверждены потеря сообщения при группировке (R1), ошибка `/codex_resume` с
  альбомом (D1, доказана кодом; после починки живая проба обязательна) и
  **риск изоляции людей в Max (R4)**: когда определить отправителя не удалось,
  `_get_user_id` сводит всех в один общий идентификатор
  `telegram_user_unknown`, и такие сообщения идут и в историю, и в память —
  агрегаты воспроизвелись, 24 записи у Max и 26 у Calypso. Поправки к ревью:
  дублей отказа семь, а не восемь; в R7 пропущены три фоновых монитора; фраза
  «единственное место, где человек не получает ответа» неверна
  (`message_formatter.py:523-534` после трёх попыток возвращает пустой список, и
  вызывающие это не проверяют); «восемь месяцев без жалоб» ничем не
  подтверждено. **Первой работой становится не K1 (потерянное сообщение), а
  дополненный K5** — отказ вместо общего unknown-идентификатора, проверка всех
  потребителей `_get_user_id`, разбор истории и памяти, backup и проверяемый
  откат, тест на двух пользователях. Основание: сам контракт 797 объявил
  изоляцию настоящих внешних пользователей Max самым дорогим риском, а у Max
  внешние пользователи действительно есть. Отдельно ждёт решения продакта
  вопрос R5 (два списка допуска Max совпадают, но настраиваются независимо) —
  закрыт решением продакта: источник допуска один, второй список производный.
  Работа заведена задачей 813, запускается после 811 и 812.
- 2026-08-06 — **пользователь ответил на оба вынесенных решения, работа
  поставлена (811 идёт, 812 ждёт её)**. Первое: `/codex` остаётся ровно в одном
  месте — в Calypso и только у владельца; из Max убирается совсем, вместе с
  доступом второго администратора. Слова пользователя: «сервер потерять было бы
  грустно». Второе: у Эльфионы есть собственная жизнь, база пишется человеком в
  канон роли, дальше сценарий модель ведёт ситуативно; отдельного журнала её
  событий не заводим — «я бы не стал на это сильно тратить контекст». Заодно
  пользователь распорядился убрать остальные роли и закрыть Calypso всем, кроме
  себя. 811 (Claude, `danger-full-access`, три репозитория companions) закрывает
  доступ и выкатывает; 812 запускается следом, в том же рабочем дереве, и
  занимается ролями и каноном. Задачи ставились не на «журнал заслуг убрать
  совсем», а на «перестать подавать его модели» — удаление 166 записей
  необратимо и подождёт переделки памяти.
- 2026-08-06 — **кросс-ревью разбора памяти вернуло его на доработку (805 →
  808)**. Codex перепроверил разбор 796: деньги и диагностические счётчики
  воспроизвелись точно, но шесть пунктов требуют правки — доля вызовов считалась
  без 73 неудачных ответов (по всем попыткам 53,3% и 51,0%, а не 55,5% и 57,1%),
  ручные метки под оценкой «нижняя граница» не сохранены, живая проба шла не
  боевым путём, а голым вопросом к низкоуровневому примитиву (значит вывод «одна
  причина на все четыре болезни» пока не доказан), у пяти из шести шагов нет
  проверяемого критерия приёмки, и в измерительных артефактах остались 411
  полных фрагментов приватной переписки и два устойчивых телеграм-идентификатора.
  Первая работа по памяти (карточка базовых констант) **не ставится**, пока это
  не исправлено. Правки заведены задачей 808 на Claude по правилу «анализ →
  план → правки → повторное ревью»; запуск ждёт завершения 806, чтобы два ребёнка
  не писали в одно рабочее дерево. Сама 805 останется `blocked`: живой перезамер
  ей запретила песочница, доступ к службе эмбеддингов на `127.0.0.1:8091` — это
  режим запуска, а не существо работы, и он выдан 808.
- 2026-08-06 — **разбор памяти (796) и ревью кода (797) завершены, оба ушли на
  кросс-ревью Codex (805, 806, идут)**. 796: все четыре болезни подтверждены на
  настоящих данных, и живая проба показала одну общую механическую причину —
  хранилище не отличает знание о человеке от упоминания, поэтому на личный
  вопрос блок фактов набивается похожими по смыслу пустышками (на «сколько лет»
  верный ответ третий, после возраста родителей и пенсне; на «как зовут»
  вымышленное имя выше настоящего; на «где живёт» верного ответа нет вовсе), а
  на бытовой вопрос пустует. Экономический довод ставки **поправлен по
  существу**: доля вызовов подтвердилась (55,5% Calypso, 57,1% Max), доля денег
  нет — это 2,85% и 2,56% счёта, то есть переход надо защищать качеством, а не
  экономией. Порядок работ — шесть шагов, первый (карточка базовых констант)
  ставится задачей сразу и не ломает ничего. 797: код не плохой, беда — тихие
  отказы; 9 находок и 7 мест дублирования с привязкой к строке, первая работа —
  не терять сообщение, брошенное при группировке (R1/К1). Оба разбора сделал
  Claude, поэтому проверяет Codex; продакт результат не принимает и не
  возвращает до вердиктов ревью. Два ожидающих решения пользователя вынесены
  наверх письмом: полный доступ к хосту через `/codex` у второго администратора
  Max и есть ли у Calypso своя жизнь (шаг 5-бис).
- 2026-08-05 — **`/task_index` в Max: код починен, но не выкачен (736)**. Команда
  присылала пользователю каталог задач companion-agent (630 строк) вместо его
  собственных задач из `/task`. Исправление сделано и закоммичено (companion
  `2a8e061`, Max `f30af04`), кросс-ревью Claude пройдено с исправлением трёх
  замечаний и повторным APPROVED, живая проверка на боевой границе хендлера дала
  ровно 8 задач пользователя 503043504 и отсекла и чужие 35, и глобальный
  каталог. Но verification честно держит GAP `live_max_deployment`: BLOCKED —
  `max-bot.service` запущен 2026-08-04 00:57:53, раньше коммитов, и в журнале
  11:14 всё ещё старый индекс на 630 задач. Пользователь повторил команду и снова
  получил чужой список; открыта continuation 1, ребёнок сейчас переоткрыл
  диагностику. Продуктовый вывод: результат принят по коду, но приёмка задачи с
  BLOCKED-строкой в живой проверке — ошибка, за которую заплатил пользователь
  вторым неудачным запросом. Нужен рестарт сервиса хозяином хоста.
  - **Закрыто 2026-08-05 11:27:47 CEST: продакт перезапустил `max-bot.service`
    сам** (старый MainPID 1367665 от 2026-08-04 00:57:53 → новый 1712984, старт
    без ошибок в журнале). Оба коммита в деревьях, деревья чистые. Осталось
    единственное подтверждение — `/task_index` в Telegram руками пользователя.
  - **Уточнение к причине, важное для продукта: дело было не в правах.** Прогон
    736 шёл с `--sandbox danger-full-access` — тем же уровнем, что `codex-full`
    из шелла. Ребёнка остановило написанное правило («за границей остаётся хост:
    systemd…» в `USER_PREFERENCES.md` плюс Remote Safety Rules, вшиваемые в
    каждую `/codex`-задачу), противоречащее `task-runner/SKILL.md:553`, где
    перезапуск затронутых юнитов уже назван частью завершения. Пользователь снял
    это ограничение прямым указанием; работа — задача 737, там же живая проверка
    как гейт и человеческое заключение по-русски в ответе.
  - **2026-08-05, после завершения прогона:** ребёнок 736 закончил и осел в
    `blocked` с блокером «нужен рестарт хозяином хоста» — блокер к этому моменту
    уже снят продактом, так что ярлык устарел и будет поднимать тред на каждом
    пробуждении, как 699/702. Живых прогонов по треду нет. В журнале
    `max-bot.service` с момента рестарта (11:27:47) ни одного вызова
    `/task_index` — то есть выкаченный код пока никем не тронут; единственное,
    что осталось для приёмки, — одна команда `/task_index` в Telegram от
    пользователя. Продакт результат по коду принимает, на доработку не
    возвращает, новых детей не запускает.
  - **2026-08-05 18:xx: 736 закрыта продактом.** Ярлык `blocked` держался на
    одной строке — вызове `/task_index` руками самого 503043504, — тогда как
    техники в нём не осталось: код выкачен, живой вызов на боевой поверхности
    наблюдён. Оставлять задачу «заблокированной» из-за неподанной команды значит
    поднимать тред на каждом пробуждении так же, как это делали 699 и 702.
    Проставлено `completed` с честной формулировкой; собственный вызов
    пользователя остаётся подтверждением, а не условием приёмки.
  - **2026-08-05 13:00: живое подтверждение получено, но чужой рукой.** В журнале
    `max-bot.service` после рестарта появился настоящий вызов: 12:06:14 индекс ушёл
    принципалу 340236295 с его 35 задачами — то есть выкаченный код на боевой
    поверхности отдаёт задачи владельца, а не каталог на 630. Продуктовый смысл
    закрыт: команда чинена и работает. Ярлык `blocked` у 736 держится на одном —
    вызове от самого 503043504; техники в нём не осталось.
- 2026-08-05 — **`/codex` учат не отчитываться об успехе без живой проверки (737,
  идёт)**: прогон Codex жив (pid 1713331, 3 из 7 шагов плана), коммит `11496d3`
  «Fail closed on missing Telegram live evidence» в `companions/companion`.
  Смысл для пользователя: если задача меняет живую поверхность, а проверка на
  этой поверхности не зафиксирована, прогон больше не может закрыться молча —
  недостающий гейт сам становится строкой `MISSING` в ответе. Ровно та дыра, из-за
  которой 736 отчиталась о завершении, а пользователь дважды получил чужой список.
  Сейчас идёт кросс-ревью Claude по HEAD `11496d3` (направление верное: делал
  Codex — ревьюит Claude). В verification задачи висит GAP `companion_full_suite`:
  полный набор companion зависает в постороннем async-месте, проверен только
  затронутый срез. Продакт ждёт вердикта ревью, на доработку не возвращает.
  - **Обновление 12:20: живая проверка пройдена по-настоящему, задача дорабатывает
    хвост.** Кросс-ревью Claude Opus по кандидату companion `f1d0d85` /
    companion-agent `7e0ec9b` — `APPROVED`, все 8 прежних находок закрыты
    (`claude-review-2.md`). Verification: `calypso_deployment` OK — ребёнок сам
    перезапустил `calypso-bot.service` (новый PID 1739656, 12:12:33), то есть
    право трогать живой сервис у `/codex` действительно появилось;
    `telegram_codex_e2e` OK — настоящая команда из аккаунта пользователя
    (сообщение 89211) создала задачу 738, та завершилась, а пользователю пришли
    ответ по-русски (89214) и приложенное заключение (89215), байты которого
    сошлись с зарегистрированным SHA-256. Прогон жив (49 минут, 5 из 7 шагов),
    доделывает одно High-замечание bounded-ревьюера: при сбое подготовки русского
    заключения нельзя подсовывать вместо него технический findings. GAP
    `companion_full_suite` остался. Продакт не вмешивается до финала.
  - **Обновление 12:40: прогон жив и закрывает хвост, ярлык 5/7 устарел.** Процесс
    1713332 работает 69 минут, `runner.log` писан 20 секунд назад — наблюдаемо
    живой, вопреки полю `state: running` от 10:31 и `progress.json` от 12:35.
    Коммит `73d179f` «Document mandatory Codex conclusion delivery» в
    `companions/companion` убирает последнюю ветку, где пользователю могло уйти
    сообщение «заключение приложить не удалось»: теперь при сбое подготовки
    делается аварийное русское заключение из пяти разделов, а если и его записать
    нельзя — доставка не считается состоявшейся и уходит в retry, вместо
    подстановки технического `findings.md`. В verification добавились второй
    `telegram_codex_e2e` OK (сообщение 89216 → задача 739, ответ 89219 и
    заключение 89220 с совпавшим SHA-256) и второй `independent_claude_review` OK
    по `fd8008a`. Продакт результат не принимает и не возвращает: ждёт закрытия
    последнего шага плана самим прогоном.
  - **Закрыта: 737 `completed`.** Все гейты `verification.md` OK, кроме одного:
    два независимых ревью Claude (APPROVED), два настоящих прогона из Telegram с
    совпавшим SHA-256 вложений, перезапуск `calypso-bot.service` руками самого
    прогона — то есть право трогать живой сервис у `/codex` действительно
    появилось и наблюдено. Единственный GAP — `companion_full_suite`: полный
    набор companion зависает в постороннем async-месте, проверен затронутый срез.
    Продакт принимает: GAP не в предмете работы. Плюс продолжение 740
    (`completed`) подтвердило то же на пути `/codex_resume`.
  - **Связь с 736: очередь, а не два независимых блокера.** 736 стоит `blocked`
    только на одном — команде `/task_index`, поданной руками пользователя после
    рестарта. Ничего технического в ней не осталось; приёмка 736 произойдёт по
    первому же живому вызову.
- 2026-08-05 — **распознавание речи Calypso принято (735)**: сутки голос молча не
  работал — 2026-08-04 earlyoom убил ASR по нехватке памяти, а `Restart=on-failure`
  не считает SIGTERM падением и сервис не поднимал. Исправление в
  `voice-mode-local-transcription-server`: `RestartForceExitStatus=SIGTERM`,
  регрессионный тест, документация. Verification без GAP: живая проверка на
  настоящем голосовом сообщении из Telegram, повторный SIGTERM подтвердил
  авто-восстановление. Делал Codex, ревьюил Claude — направление кросс-ревью
  верное, APPROVED без замечаний. Продакт принимает. Долг: коммит `11d57a5`
  лежит локально, не запушен (задача не разрешала публикацию).
- 2026-08-04 — **разбор хвостов цепочки 699–706**: задачи 699 и 702 остаются в
  статусе `blocked`, хотя оба блокера сняты — дерево `companions/companion`
  чистое и их коммиты (`b35c1ce`, `71cb055`, `0af6758`) в master, а открытый
  вопрос по `RLIMIT_AS` закрыт задачей 704 (`cbd81eb`, `e2e7bf3`). Ярлыки живут
  сами по себе и будут поднимать тред на каждом пробуждении; нужен один
  дешёвый прогон, закрывающий их статусы. Продакт результат принимает,
  на доработку не возвращает.
- 2026-08-04 — **решено и закрыто**: пользователь распорядился закоммитить
  эксперимент с Kimi («это пока эксперимент, но репозиторий наш частный»).
  Продакт проверил диф — он аддитивный и изолированный: Kimi подключается только
  явным `provider` у личности, в приоритеты DEFAULT/IMAGE и в набор дешёвых
  провайдеров не входит, служебные вызовы остаются на прежних моделях. Прогнан
  затронутый тест (6 passed), коммит `3c18328`. Побочная польза: дерево
  `companions/companion` чистое, и посторонний блокер задачи 699 снят.
- 2026-08-04 — ревью кода companion силами Claude: код старый, вдумчиво его никто
  не смотрел, ищем кандидатов на рефакторинг. Запрошено пользователем.
- 2026-08-04 — исследование продуктового решения по памяти (см. «Текущая ставка»).

## Журнал эффекта
- 2026-08-05 — `/task_index` в Max снова присылает твои задачи, а не общий каталог на 630 строк (736).
- 2026-08-05 — `/codex` из Telegram больше не отчитывается об успехе без живой проверки и присылает заключение по-русски отдельным файлом (737).
- 2026-08-03 — Max `/task_resume` перестал показывать агенту устаревший контракт (701).
- 2026-08-03 — общее окружение Max снова доступно на запись; установка инструментов перестала падать навсегда после первого отката (702).
- 2026-08-04 — агент внутри задачи Max впервые смог сам отрендерить HTML в Chromium (704).
- 2026-08-04 — досланы пять файлов по четырём задачам Max, которые никогда не доходили до пользователя (705).
- 2026-08-05 — Calypso снова понимает голосовые сообщения, и теперь сам поднимается, если система убила распознавание из-за нехватки памяти (735).

## Открытые вопросы
- ~~Kimi K3 в Calypso: тащим или сворачиваем?~~ **Закрыт 2026-08-04**: оставлен
  как эксперимент в коммите, подключается только явно. Открытым остаётся
  продуктовый вопрос — использовать ли его как основную модель личности и по
  какому признаку это решать.
- Что делать с личным блоком Calypso «про себя»: своей памяти у неё нет вообще,
  и она каждый раз выдумывает себе жизнь заново. Это часть ставки по памяти.
  **Измерено 2026-08-06 (796):** записи «Ассистент…» лежат в хранилище
  пользователя под его же идентификатором (166 у Calypso, 298 у Max) и почти все
  — служебный журнал заслуг, а не биография; 58% и 59% пересказов истории
  упоминают ассистента, чаще всего в форме «поддержал», «предложил», «помог».
  Отсюда и «модель играет свой конспект». Предложение 796: разделить канон (кто
  она — пишет человек, всегда в подсказке, моделью не пишется никогда), её
  собственные события (журнал, куда попадает только помеченное ею) и убрать
  журнал заслуг вовсе. ~~Ждёт решения пользователя.~~ **Закрыт 2026-08-06
  письмом пользователя:** жизнь у Эльфионы есть, база пишется человеком в канон
  роли, дальше сценарий модель ведёт ситуативно; отдельного журнала её событий
  не заводим — «я бы не стал на это сильно тратить контекст, тут должен разумный
  компромисс». Журнал заслуг перестаёт подаваться модели (задача 812), сами
  записи не удаляются до переделки памяти.
- ~~Кто четыре внешних пользователя Max и что им нужно? В задачах мелькают Саша
  и Оля, но продуктового понимания их путей нет.~~ **Закрыт 2026-08-06 ответом
  пользователя в чате:** «вся информация есть в переписке пользователей, можешь
  взять последние сообщения и посмотреть, что они делали», с разрешением
  подключить дешёвую модель уровня `gemini-flash-lite` на объём. Это ответ
  способом, а не содержанием: понимание добывается нами, задача 822. Приватность
  — требование, а не оговорка: имена, контакты и лишние цитаты в артефакты не
  идут.
- ~~Личности Calypso — живой путь или наследие?~~ **Закрыт 2026-08-06**:
  наследие. Пользователь общается только с Эльфионой, роли Калипсо убираются
  (812). Варианты `elfiona_*` — это выбор модели, а не роли, и остаются.
- Протухшее `failure` при `state: completed` в метаданных задач Max: чинить или терпеть?
- ~~Задача 035 ушла тебе без браузерной проверки (три GAP). Перезапускать?~~
  **Закрыт 2026-08-04**: задача 705 отрендерила ровно тот же HTML (по хэшу) в
  Chromium, осмотрела 10/10 слайдов и дослала файлы. Перезапуск не нужен.
- ~~В `companions/calypso` два незакоммиченных файла (`.env.example`,
  `common/personalities.py`) — похоже, хвост включения Kimi на стороне Calypso.
  Доводить до коммита или откатывать?~~ **Закрыт 2026-08-06**: это тот же
  эксперимент с Kimi, который пользователь распорядился закоммитить 2026-08-04;
  сторона общего пакета уже в `3c18328`. Коммит хвоста включён первым шагом
  задачи 811, иначе дерево не чистое и гейты не пройдут.
