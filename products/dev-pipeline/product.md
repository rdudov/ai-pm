# Dev Pipeline

## Концепция
Непрерывный владелец для сложной, длительной и чувствительной к восстановлению
работы: один нативный сеанс держит причинную цепочку, а ограниченные ревьюверы и
гейты свидетельств решают, можно ли принять результат. Публичное ядро —
`/opt/projects/dev-pipeline`, транспорт и уведомления — адаптер companion-agent.

## Пользовательские пути
| путь | кто | чем запускает | что получает | состояние |
| --- | --- | --- | --- | --- |
| запустить сложную работу в фоне | пользователь | явный запрос dev-pipeline | детач-прогон с владельцем и ревью | работает |
| верить, что работу проверил не тот, кто её писал | пользователь | ничего не делает: правило действует при каждом запуске проверки | запуск проверяющего того же семейства, что автор, и любого запуска на Cursor отклоняется до появления ребёнка, с названной причиной в трёх артефактах | **ставится (854 сделана 2026-08-09, встречная проверка 1003 вернула `rework`).** Работает для запусков, объявленных проверкой: свой над своим, Cursor над обоими семействами, нечитаемый предмет и нечитаемый автор отклонены настоящими запусками, парное направление дошло до ребёнка, 371 тест зелёный. Дыра названа поимённо и воспроизведена запуском, а не рассуждением: если запускающая сторона не поставила `--require-review-verdict` и не завела `review_verdict` в контракте, гейт не вызывается вовсе — задача с Claude-автором ушла в подготовку с кодом 0 и без записи гейта. Ровно так 6 августа прошло ошибочное ревью 846 на Cursor. Правки поставлены задачей 1007 (Claude), повторная проверка снова за Codex. |
| получать письмо только когда есть что сказать, и не терять при этом свой вопрос | пользователь | ничего не делает: каждое пробуждение продакта проходит через `scripts/outbound.py` | письмо только на выбор пользователя, изменение пользы или законченную заказанную работу; повтор по содержанию склеивается, прогон и коммит уходят пушем | **ставится (861 сделана 2026-08-09, встречная проверка 1024 вернула `rework`).** Работает и снято настоящими запусками: объявленный вопрос проходит и порог, и склейку и выпускает удержанное; сорванная отправка не считается отправленной, а становится удержанной; машинная сессия без человека не глушит нужное письмо; оба обычных пути отказа (расхождение контракта с `task_runner` и сорванное пробуждение) идут через шлюз; 347 тестов зелёные, зеркало отправленного за время проверки не выросло. Обе дыры, найденные проверкой 1024, закрыты задачей 1025 и сняты запуском: блокировка переехала на отдельный непеременяемый файл, и двухпроцессный пробник теряет запись на старом коде и не теряет на новом; признак вопроса приходит структурной строкой `ВОПРОС: да|нет` от сочинителя плюс независимая текстовая проверка, поэтому просьба выбрать с точкой снова вопрос. Добавлены резервная копия реестра и журнал решений, который только дописывается; тестов 368, живьём прошли два настоящих тика таймера. Ждём повторной встречной проверки Codex (1028) — до её вердикта путь считается неподтверждённым |
| видеть прогресс | пользователь | Telegram-уведомления | throttled progress + терминальное событие | работает |
| получить сам документ, а не сообщение о работе | пользователь | ничего не делает: прогон, сделавший файл для человека, отправляет его сам | документ в назначенном канале с подписью по названию задачи и квитанцией (канал, идентификатор сообщения, sha256) | **ставится (834 сделана 2026-08-07, ждёт встречной проверки 843).** До неё путь не существовал вовсе: 242 квитанции доставки за всё время, все до одной про жизнь прогона, ни одной про дошедший документ — человек получал результат только тогда, когда продакт вспоминал отправить его руками. Сейчас живьём снято всё, кроме последнего шага: настоящий адаптер на настоящем потоке событий ядра доставил `deliverables/report.html` и записал `document_delivered` (`message_id 18999`, sha256 `2285482e…c89b25`), заменена только граница aiogram; при недоступном Telegram прогон отказывает наблюдаемо (`document_delivery_refused` с причиной), а не молчит; повторный запуск не шлёт второй раз, и решает содержимое, а не имя файла. **GAP закрыт встречной проверкой 843 (2026-08-07): настоящее сообщение в настоящем канале наблюдалось.** Обычный прогон дошёл до `attempt_completed` и следом записал `document_delivered` с `message_id 18552`; файл, квитанция и независимо скачанная вложением копия — все 42 байта с одним sha256 `590f8bb4…4422fb`, аккаунт пользователя видит помеченный документ в 07:44:13 UTC. Путь остаётся «ставится», но уже по другой причине: то же ревью нашло четыре дефекта надёжности — упавшая запись квитанции после удавшейся отправки роняет принятый прогон и разрешает вторую отправку тех же байтов; обещанный отказ «канал не назначен» недостижим обычным путём; задача с двумя документами, где один отказан, показывается доставленной; ошибки обхода каталога и хеширования молча означают «документа не было». **Все четыре закрыты задачей 849 (2026-08-07) и проверены внесённой поломкой, а не тестом** — по `verification.md`: удавшаяся отправка с упавшей квитанцией не роняет прогон и не повторяет отправку, отсутствие канала отвергается на входе, нечитаемый файл даёт поимённый отказ, неполный набор документов не считается доставленным. Путь остаётся «ставится» по трём причинам, названным той же задачей: правки не закоммичены, наблюдатель табло на стороне продакта ещё не пропатчен (патч готов и проверен вне дерева), и живой отправки документа в назначенный канал после этих правок никто не наблюдал — у ребёнка не было маршрута до `api.telegram.org`. Повторное ревью на Codex не запускалось |
| получить ревью изменений | владелец | digest-bound review | структурное решение approved/rework | работает с хоста, но не изнутри дочернего прогона: 2026-08-06 задача 759 записала пробу (`live-evidence/codex-reviewer-launch-probe.md`/`.log`) — из песочницы владельца `/root/.codex` недоступен на запись, а прокси отдаёт 403 на `chatgpt.com` восемь раз подряд, то есть ревьювер не доходит до модели. С хоста тот же самый вызов стартует нормально. Далее: работает на обеих осях, оба вердикта получены 2026-08-05: Codex сам написал `findings.md` и `Verdict: rework` по коммиту `f9f8be5` (задача 734, шестой запуск, 396 тестов внутри read-only границы), Claude — по коммиту `656de1c` (задача 753, 8 находок, 404 теста). Оба прогона пережили конец родительской службы в собственных systemd-областях. Починка по этим находкам проверена повторно и принята: 2026-08-05 задача 753 дала `Verdict: approved` по коммиту `8141c37`, все пять блокирующих находок закрыты наблюдением на живом хосте, регрессии 406 passed. Обязательное завершающее ревью кандидата пройдено 2026-08-05 с хоста (задача 758): отдельная сессия `019fd385`, код 0, решение `approved`, находок нет, восемь улик перечислены поимённо. До этого: причина пяти смертей 734 названа по хостовым уликам (служба запуска гасила всю группу процессов), дочерний прогон переведён в отдельную systemd-область — наблюдено на 749 (`supervision_boundary.mode=systemd_scope`, группа ребёнка вне службы), выживание после конца родителя ждёт финальных файлов ребёнка; отказ завершать ревью без `findings.md` проверен живьём (749: код 0 без findings → `blocked`, `rejected_completion_contract`). До этого: пять запусков 734 подряд убиты SIGTERM (последний — через 40 секунд, `watcher_stop_reason=signal_sigterm`); граница записи read-only при этом рабочая (probe-файлы). Внутри живого прогона с полными правами ревью проходит, артефакты пишутся и задача закрывается (737: `claude-review-1..7.md`, последнее APPROVED, `exit_code: 0`) |
| продолжить прерванный прогон | пользователь | resume | тот же нативный сеанс и артефакты | работает |
| bounded-ревьювер по вопросу | агент | `dev-pipeline context` + `agent` | решение с находками | работает |
| не терять работу, когда у Claude кончается лимит | пользователь | ничего не делает: прогон сам уходит в ожидание и сам продолжается после сброса | та же задача, тот же исполнитель и та же попытка, уведомление о паузе и о продолжении | работает по проверке 966 (все восемь разделов `OK`, в том числе ровно одно возобновление и отказ создавать второй прогон), но **встречной проверки ещё не прошло**. Ревью 968 вернуло `rework` по шести замечаниям; правки сделаны задачей 970, а повторное ревью 975 (Claude) 2026-08-08 дало письменный вердикт `rework` с двумя остатками: запланированное ожидание по лимиту нельзя отменить (`stop` и `reconcile_abandoned_run` не знают про `quota-wait.json`, поэтому таймер возобновит остановленную задачу), и два примера запуска в `skills/task-runner/SKILL.md` (строки 429 и 452) по-прежнему отказывают без `--destination`. Четыре замечания из шести (D1, D4, D5, D6) закрыты полностью. Правки — задача 977; по её `verification.md` оба остатка закрыты 2026-08-08 и приняты отдельным read-only проходом Claude (задача 979, ровно один `Verdict: approved`, хеши кандидата не менялись): запланированное ожидание теперь отменяется вместе с задачей (`stop` гасит таймер, удаляет файлы ожидания и публикует отказ, а поздний вызов таймера видит «уже занято»), просроченное ожидание без юнита признаётся неудачей возобновления, оба примера в `SKILL.md` проходят сухой запуск с кодом 0. Коммит `e21b65b`. Регрессии — 344 теста плюс 6 подтестов, `repo-health` и опись задач чисто. Сама 977 закрыта 2026-08-08 (`completed`, код 0): помимо 979, коммит `e21b65b` отдельно проверен по своим байтам Claude-задачей 980 (`Verdict: approved`) и завершающим каноническим `review-candidate` (`approved`, замечаний нет); на самом коммите — 335 тестов плюс 6 подтестов. Миграции нет и не требуется: схема ожидания версии 1 уже содержала все нужные поля, а неполная запись отказывает, а не угадывает. Живьём наблюдалось одно: сторож не дал запустить standard-задачу Claude без адреса уведомления. Дальше по той же цепочке: проверка 978 нашла две подделки в задел 976 — «умер рантайм ребёнка» опознавался голой подстрокой в любом тексте ребёнка, а отказ по адресному пространству пропускался при `26G` и `none`; обе закрыты задачей 981 (2026-08-08, `completed`) и проверены не тестом, а перезапуском пробников самого ревьюера на настоящих Codex-детях, плюс закрыт найденный живьём смежный путь, где ребёнок с `--memory-limit none` наследовал 4 ГиБ и умирал на старте. 344 теста, 17 подтестов, `repo-health` и опись чисто. Встречной проверки этих правок ещё не было — запущена задача 982 на Codex |
| говорить с продактом, когда у Claude кончились обе модели | пользователь | команда `codex-pm` | тот же продуктовый владелец из `/opt/projects/product-owner`, но на Codex `gpt-5.6-sol` | работает по проверке 966 (живой запуск показал нужный каталог, модель и доступ, продуктовые файлы не изменились). Ревью 968 нашло, что обёртка `codex-pm` не давала вызвать `--status` и `--show-command`; правка сделана задачей 970 (коммит `abcc8a3`) и принята встречной проверкой 975 (2026-08-08): `codex-pm --status` и `--show-command` работают, замечание D6 закрыто |
| вести сложную работу на Cursor, а не только на Codex и Claude | пользователь | `--runner agent --workflow dev-pipeline` | тот же владелец, тот же журнал, то же продолжение прерванного прогона | работает на сервере — доказано живым прогоном 825 (`runtime: cursor`, продолжение через `native_session_resume`, отказ продолжать удалённый чат до запуска процесса); в публичном шаблоне `task-agent` **ещё не выпущено**: коммит `f8412d0` лежит локально, публикацию держит сторож из-за резервной ветки `refs/backup/pre-rewrite-2026-08-06` (задача 828). Ограничение рантайма: работает только с `--memory-limit none`, потому что Cursor резервирует 26 ГиБ адресного пространства до разбора аргументов. Встречная проверка 827 (2026-08-07) подтвердила наблюдением все четыре заявленных факта на живом Cursor, но вернула `rework` по трём: обещанный отказ по памяти живёт не в заявленном публичном коммите, а в companion (`e8150d1`); измеренный на одной версии порог 26 ГиБ применяется ко всем версиям без правила для неизвестной; при настоящем лимите памяти проверка адресного пространства идёт раньше проверки чата, и в журнале вместо «чат удалён» появляется «не хватило памяти». Правки — задача 850 |

## Текущая ставка
Сделать ревью дешевле для владельца: subject должен связывать все доставляемые
репозитории change-set'а, иначе раунды уходят на бухгалтерию кандидата, а не на
риск (задача 704 — три раунда из четырёх, задача 706 — AR-003).

## Не делаем
- Возврат ролевых цепочек legacy `multi-agent-dev`.
- Новый планировщик или вторая машина состояний поверх публичного ядра.

## В работе
- 2026-08-09 08:22 UTC — **Правки 1025 по шлюзу писем закончены Claude, зелёные и закоммичены (`ada1b3e` в `product-owner`); повторная встречная проверка запущена задачей 1028 на Codex — автор Claude, значит проверяет Codex.** По артефактам 1025, а не по прозе: шесть семейств улик в `verification.md` — все `OK`, `status.json` — `completed`, код выхода 0. HIGH-1 закрыт так: блокировка переехала на отдельный файл `state/outbound.json.lock`, который никогда не подменяется, данные читаются уже под блокировкой, временное имя уникально на писателя. Собственный двухпроцессный пробник (`evidence/high1_probe.py`) на продовом `Ledger` показывает разницу в обе стороны: на до-фиксном коде оба писателя выходят с 0 и в реестре остаётся только `moex` (запись `process` потеряна), на текущем — обе записи на месте. HIGH-2 закрыт структурно, а не пунктуацией: сочинитель обязан второй строкой писать `ВОПРОС: да|нет`, и рядом стоит независимая текстовая проверка просьбы выбрать; просьба, кончающаяся точкой, теперь вопрос, а вопрос уходит всегда. MEDIUM-1 — резервная копия реестра и видимое восстановление; MEDIUM-3 — журнал `state/outbound-journal.jsonl`, который только дописывается; MEDIUM-2 — утверждение 861 про число сессий с человеком исправлено (19, а не 63). Тестов стало 368 против 347, и новые 15 регрессий на до-фиксной копии падают 14 из 15 (проходит только отрицательный контроль) — то есть они ловят именно эти дефекты. Живьём: два настоящих тика системного таймера `product-thread@moex` в 09:50 и 10:10 CEST прошли на развёрнутом коде, оба вторым строкой дали `ВОПРОС: нет`, оба решения лежат в журнале, а продовый реестр за время проверки не изменился (sha256 совпал). Запуск 1028 подтверждён наблюдением: pid 4102153 жив, `state: running`, и на этот раз парный гейт связан правильно — `subject 1025 written by Claude; Codex is the reviewer the pairing requires` (в 1024 предмет не был объявлен и вердикт машинерия связать отказалась). Недельное окно Codex израсходовано на 6%. Остальные свободные задачи (1000, 957, 839, 836, 803, 754, 746) не запущены по той же физической причине, что и вчера: все пишут в `/opt/projects/companion-agent`, где 19 незакоммиченных отслеживаемых файлов принадлежат прерванной работе 918, и второго ребёнка туда пускать нельзя.
- 2026-08-09 07:42 UTC — **Встречная проверка 1024 (Codex) вернула `rework` по шлюзу писем 861: обещание «вопрос не теряется» не держится, и обе дыры воспроизведены запуском. Правки запущены задачей 1025 на Claude — он автор 861, значит замечания закрывает он, а перепроверять снова будет Codex.** По артефактам: все шесть семейств улик в `verification.md` — `PASS`, седьмой раздел `completion_gate` — `BLOCKED`. HIGH-1: блокировка берётся на сам `state/outbound.json` (`Ledger.__enter__`, строки 380-385), а выход пишет соседний временный файл и подменяет заблокированный путь через `os.replace` (строки 393-403); второй писатель ждёт на старом inode, просыпается, читает устаревшие данные и затирает чужую запись. Двухпроцессный пробник на продовом `Ledger`: писатель A положил удержанное письмо для `process`, писатель B — для `moex`, оба вышли с кодом 0, в файле остался только `moex`. Продакт открыл сам код и подтвердил механику, а не поверил прозе: временное имя вдобавок общее у всех писателей. HIGH-2: `asks_user` (строки 168-180) признаёт вопросом только точную метку `ПОВОД: вопрос` или строку со знаком вопроса, поэтому обычная русская просьба выбрать, кончающаяся точкой, ушла в `drop` ниже порога и в `hold` при совпадении по содержанию. Три средних: удаление реестра теряет всё удержанное без следа; заявленные в 861 «63 сессии с человеком из 362» продовым разбором не воспроизводятся (тот же корпус даёт 19); из двух продовых тиков durable-проход через шлюз показывает только второй. Отдельно — своя же процедурная дыра: 1024 запущена без объявленного предмета, поэтому машинерия отказалась связать её вердикт (`verdict_binding: forbidden`), и это тот же незакрытый GAP, что 1007 — запускающая сторона не поставила `--require-review-verdict`. Вердикт при этом лежит на диске поимённо, и продакт действует по нему, а не по метке. Запуск 1025 подтверждён наблюдением: pid 4088211 жив, `state: running`, репозиторий `/opt/projects/product-owner`. Недельное окно Codex израсходовано на 6%. Остальные свободные задачи (1000, 957, 839, 836, 803, 754, 746) не запущены по одной физической причине — все пишут в `/opt/projects/companion-agent`, а его 19 незакоммиченных отслеживаемых файлов принадлежат прерванной работе 918 и второму ребёнку туда отдавать нельзя.
- 2026-08-09 06:25 UTC — **Встречная проверка 1010 не идёт: положенного проверяющего останавливает фильтр поставщика модели, и переписывание задачи это не лечит. Дерево отдано задаче 861 по жалобе пользователя на повторяющиеся письма.** По артефактам, а не по прозе. Первый прогон (pid 4010825) прожил 2 минуты 29 секунд и умер с кодом 1; в `.runner/runner.log` последними двумя записями лежат `error` и `turn.failed` с текстом «This content was flagged for possible cybersecurity risk». Сам парный гейт при этом отработал штатно: `decision: accepted`, `pairing_outcome: required_reviewer`, `verdict_binding: allowed`. Лечение, сработавшее на 992, применено и здесь — в `task.md` добавлен раздел «Область работы» (защитная проверка нашего же процесса в нашем же репозитории, предмет read-only), пункт «найти обход» переформулирован в проверку полноты покрытия контроля; предмет проверки не сузился, все четыре контроля на месте. Второй прогон (pid 4051366) умер тем же кодом и тем же сообщением через 3 минуты 22 секунды, и место обрыва теперь названо точно: в новой части журнала ровно один `turn.failed`, и он идёт сразу за чтением исходника самого парного гейта (`cross_review.py`), а не за формулировкой задачи. То есть срабатывает фильтр поставщика на содержимом нашего же защитного кода, и нашими средствами он не снимается. Правило пользователя от 2026-08-07 здесь прямое: работу Claude проверяет только Codex, другого проверяющего не бывает, а при недоступном проверяющем работа останавливается и об этом сообщается. 1010 остановлена в `failed`, 1007 остаётся с незакрытым GAP. Освободившееся дерево `/opt/projects/companion-agent` отдано задаче 861 (Claude, `standard`, `danger-full-access`) — прямая жалоба пользователя письмом `19fdbd48115b0c00` на письма про одно и то же; запуск подтверждён наблюдением: pid 4052859 жив 1 минуту 43 секунды, `status.json` — `running`. Семь остальных свободных задач (1000, 957, 839, 836, 803, 754, 746) не запущены по единственной физической причине — все пишут в то же дерево. Недельное окно Codex израсходовано на 5%.
- 2026-08-09 05:01 UTC — **1007 закрыла обход парного гейта, и на неё запущена обязательная встречная проверка Codex (1010).** По артефактам 1007, а не по прозе: `status.json` — `completed`, код возврата 0; `verification.md` держит настоящие запуски `task_runner.py start`, а не только тесты. Отказ наступает до записи `runner.json`: путь из раздела «Как проверить самостоятельно» задачи 1003 теперь даёт код 1, `runner.json` отсутствует, `status.json` — `blocked`, запись отказа лежит в `.runner/cross-review.json` и `trace.md` и называет автора (Claude), положенного проверяющего (Codex) и оба средства. Необъявленный запуск не отклоняется, а связывается полем `verdict_binding`, и решение опирается на замер по настоящему корпусу: из 51 задачи с разрешимым родителем 22 запущены тем же семейством, то есть безусловный отказ сломал бы обычные продолжения работы. Настоящая защита стоит не на запуске, а в общем владельце завершения `task_completion.completion_ready`: строка `Verdict:` в `findings.md` при связке `forbidden` закрытие не проходит. Обычный запуск не сломан — проверено живым ребёнком без `--dry-run` (pid 3955452, `git status` до и после побайтно совпал). Тесты: 382 против базовых 371. **GAP в самой 1007 — встречной проверки нет, автор Claude, и Claude-ребёнок дать вердикт не может.** Это и запущено: 1010 на Codex, `standard`, `danger-full-access` по `/opt/projects/companion-agent`. Запуск подтверждён наблюдением, а не выводом команды: pid 4010825 жив, `status.json` — `running`, запись гейта `decision: accepted`, `pairing_outcome: required_reviewer`, `verdict_binding: allowed`. Побочно новый гейт проверен на самом продакте: первая попытка запуска 1010 была отклонена, потому что предмет проверки не назван, и потребовала `--review-subject`. Восемь свободных задач (1000, 957, 861, 839, 836, 803, 754, 746) не запущены по единственной физической причине — все пишут в тот же `/opt/projects/companion-agent`, где дерево держит 1010. Недельное окно Codex израсходовано на 4%.
- 2026-08-09 04:21 UTC — **встречная проверка 1009 закончилась вердиктом «принято», и на освободившееся дерево запущена 1007 — правка последней дыры в парном гейте кросс-ревью.** По `status.json` 1009: `completed`, вердикт Codex `approved`, замечаний нет; проверено самостоятельно, а не по отчёту 1008: ни в `task_runner.py`, ни в `skills/task-runner/SKILL.md`, ни в `docs/task-execution.md` не осталось безусловного обещания «ребёнок не может писать в этот поток» и оно не пересказано другими словами; маршрут потока снят настоящим запуском; оговорка про полный доступ и общего root осталась во всех трёх поверхностях; кандидат из шести файлов перевязан самим проверяющим и совпал с записанным `b485cfba…de3240e1`; 371 тест task-runner, 544 теста репозитория, `repo-health` и опись чисто; правки в двух докстрингах тестов поведения не поменяли. Цепочка 985 → 987 → 988 → 992 → 1008 → 1009 закрыта, чинить по ней нечего. Следом запущена 1007 (Claude, `standard`, `danger-full-access` по `/opt/projects/companion-agent`, уведомления в назначенный канал): ревью 1003 воспроизвело запуском, что парный гейт вызывается только когда запускающая сторона сама объявила запуск проверкой — задача с Claude-автором, поднятая без `--require-review-verdict`, дошла до подготовки с кодом 0 и без записи гейта, ровно как ошибочное ревью 846 на Cursor. Автор правок Claude, значит повторную проверку снова ведёт Codex. Запуск подтверждён наблюдением: pid 3946748 жив, `status.json` `running`, собственная systemd-область. Восемь остальных свободных задач (1000, 957, 861, 839, 836, 803, 754, 746) не запущены по единственной физической причине — все пишут в тот же `/opt/projects/companion-agent`, где теперь держит дерево 1007. Недельное окно Codex израсходовано на 3%.
- 2026-08-09 04:02 UTC — **правка 1008 закончилась сама (`completed`, код 0), оба замечания ревью 992 сняты, и её встречная проверка запущена задачей 1009 на Codex (ребёнок pid 3932555 жив).** По `verification.md` пять разделов из шести — `OK`. Тексты: девять безусловных утверждений «ребёнок не может писать в этот поток» убраны из трёх поверхностей (`task_runner.py`, `skills/task-runner/SKILL.md`, `docs/task-execution.md`) и заменены на то, что измерено — супервизор держит читающий конец, пишущий конец у самого рантайма, решение принимается на живом потоке без чтения файлов из каталога ребёнка; оговорка про полный доступ и ребёнка под тем же пользователем осталась (11/10/6 упоминаний), поиск проверен обратным контролем — он срабатывает на всех девяти прежних местах. Предмет: привязан один диф шести файлов, sha256 `b485cfba…de3240e1`, 147308 байт на HEAD `e21b65b`; привязка до правок воспроизвела финальную привязку 992 (`b22fcea1…e3419829`) байт в байт, то есть между концом ревью и началом правки предмет не двигался, а перевязка после всех гейтов дала те же хеши — 544 теста репозитория, 371 тест task-runner и `repo-health` зелёные ровно на том дереве, которое отдано на проверку. Единственный `GAP` — сама встречная проверка: правку писал Claude, значит проверяет Codex, и именно это запущено 1009. Восемь остальных свободных задач (1007, 1000, 957, 861, 839, 836, 803, 754, 746) не запущены по единственной физической причине — все пишут в тот же `/opt/projects/companion-agent`, где сейчас держит дерево 1009, а параллельная работа в одном дереве — это ровно тот дефект B2, из-за которого 992 пришлось переделывать. Недельное окно Codex израсходовано на 3%.
- 2026-08-09 03:21 UTC — **встречная проверка 992 дошла до конца и вернула `rework` по двум поимённым замечаниям; правки поставлены задачей 1008 на Claude и запущены живьём.** По `verification.md` 992 шесть пунктов из восьми — `OK`, и все сняты настоящими прогонами настоящего Codex-ребёнка: подделка полной записи рантайма из каталога ребёнка вердикт не меняет (`runtime_failure=null`, `kind=premature_completion`), сдвиг границы прогона со 121 на 0 тоже не меняет, настоящая поломка рантайма под ограничением адресного пространства в 4 ГиБ по-прежнему диагностируется наблюдателем живого потока (`observed_by=supervisor_runtime_stream`, совпадений на потоке рантайма 1, на потоке ребёнка 0), обычный поток ребёнка в поток рантайма не попадает вовсе (4 совпадения в журнале ребёнка, 0 в журнале рантайма), 544 теста и 17 подтестов зелёные, `repo-health` чисто. Разбор пробника 978 принят: он оставлен байт в байт как исторический артефакт снятого интерфейса. Провалились два пункта. Первый — тексты обещают больше доказанного: `task_runner.py` говорит, что писателя устанавливает владение дескриптором и что супервизор единственный держит stderr рантайма (на деле у него только читающий конец), `SKILL.md` — что stderr «свободен от всего, что писал ребёнок», `docs/task-execution.md` — что это «поток, в который ребёнок не может писать»; доказано более узкое: обычный вывод ребёнка идёт в отдельный поток, а супервизор читает stderr рантайма живьём и не восстанавливает решение из файлов, доступных ребёнку. Второй — предмет ревью двигался во время проверки: диф шести файлов ушёл с `a0ffb1bf…e628f6` на `b22fcea1…e3419829` посторонней параллельной работой в том же дереве, сама 992 исходников не правила. Заведена 1008 на Claude (автор 988 — Claude, значит повторную проверку снова ведёт Codex), запущена, ребёнок pid 3903672 жив; приёмка требует узких формулировок в трёх поверхностях, одного привязанного дифа шести файлов с хешами и зелёных регрессий на нём же. Девять свободных задач (1007, 1000, 957, 861, 839, 836, 803, 754, 746) не запущены по единственной физической причине — все пишут в тот же `/opt/projects/companion-agent`, где сейчас работает 1008. Недельное окно Codex израсходовано на 3%.
- 2026-08-09 03:05 UTC — **встречная проверка 1003 закончилась вердиктом `rework`: парный гейт есть, но включается только тогда, когда запускающая сторона сама назвала запуск проверкой; правки поставлены задачей 1007, а перезапущенная 992 идёт живьём.** По `status.json` 1003: `completed`, исполнитель Codex, пара `Codex над работой Claude` записана в `cross_review` как `accepted`. Семь пунктов `verification.md` — `PASS`, и все сняты настоящими запусками: свой над своим отклонён, три запуска на Cursor отклонены (Claude-автор, Codex-автор, неопознанный предмет), парное направление дошло до ребёнка pid 3873948, нечитаемый предмет и нечитаемый автор падают закрыто, порядок записи контракта проверен, 371 тест и 17 подтестов зелёные. Восьмой пункт — `undeclared_review_bypass_reproduced`: задача с Claude-автором в `Parent Task`, запущенная через `--runner agent` без `--require-review-verdict` и без `review_verdict` в контракте, дошла до подготовки с кодом 0 и состоянием `ready`, записала `runner.json` и не создала записи гейта вовсе. Это ровно тот путь, которым 6 августа прошло ошибочное ревью 846 на Cursor. Заведена 1007 на Claude (автор правок 854 — Claude, значит повторную проверку снова ведёт Codex), `Parent Task` — 1003, приёмка требует отказа до `runner.json` по тому же пути и отдельной проверки, что обычные не-ревью запуски не сломались; не запущена: дерево занято. 992 перезапущена третьим прогоном, pid 3892482, жива на 3 минуте — прошлые два прогона обрывал не наш код, а поставщик модели сообщением «flagged for possible cybersecurity risk», второй раз уже после того, как ребёнок снял два самых тяжёлых контроля. Поэтому в `task.md` 992 внесён раздел с уже снятыми результатами (`candidate_identity`, `b1_file_bypass_repeat_does_not_fire`, `b1_run_boundary_move_does_not_fire` и завершённый пробник изоляции потока: `runner_matches=4`, `runtime_matches=0`), а живые пробники с дописыванием записей из оставшейся работы убраны — повторять снятое незачем. Остальные восемь свободных задач (1000, 957, 861, 839, 836, 803, 754, 746) и новая 1007 не запущены по единственной физической причине — все пишут в тот же `/opt/projects/companion-agent`, где сейчас работает 992. Недельное окно Codex израсходовано на 3%.
- 2026-08-09 01:41 UTC — **854 закончилась: запрет брать проверяющим то же семейство, что писало работу, стал отказом при запуске, а не строкой в правилах; результат не принят до встречной проверки, она запущена задачей 1003 (Codex).** По `status.json` 854: `completed`, код выхода 0, исполнитель Claude. Все три обязательных пункта `verification.md` — `PASS`, и все сняты настоящими попытками запуска, а не тестами: Claude над работой Claude (808) отклонён до появления ребёнка, отказ назвал автора, положенного проверяющего, средство `--runner codex` и правило «нет проверяющего — работа стоит, и об этом говорят пользователю», и виден в `status.json`, `trace.md` и `.runner/cross-review.json`; два запуска на Cursor отклонены — и при опознанном авторе, и при неопознанном предмете; парное направление (Claude над работой Codex, 977) прошло гейт и дошло до запуска ребёнка. Гейт стоит после выбора исполнителя и до записи контракта ревью, ревью опознаётся по флагу запуска или уже стоящему в контракте требованию вердикта, а автор — по полю `runner` в `.runner/runner.json` предмета, то есть по записи запуска, а не по прозе. Два случая закрыты «в минус» намеренно и названы вслух: нечитаемый предмет и нечитаемый автор отклоняются, иначе удаление одной строки из `task.md` вернуло бы прежнее состояние. Регрессии — 371 тест task-runner, включая 22 новых. Ревью ведёт Codex, потому что писал Claude; 1003 запущена `standard`, `danger-full-access` по `/opt/projects/companion-agent`, предмет править запрещено словами в задаче, и в неё заранее внесён раздел «Область работы» — на 992 поставщик модели уже обрывал такую проверку как «flagged for possible cybersecurity risk». Запуск подтверждён наблюдением: pid 3872566 жив, `thread_state.py` показывает один живой прогон. Остальные восемь свободных задач (1000, 957, 861, 839, 836, 803, 754, 746) не запущены по единственной физической причине — все пишут в тот же `/opt/projects/companion-agent`, где теперь работает 1003. Недельное окно Codex израсходовано на 2%.
- 2026-08-09 01:01 UTC — **запущена 854: запрет брать проверяющего того же семейства, что и автор работы, переезжает из прозы в машину.** Дерево `/opt/projects/companion-agent` освободилось (прогоны 992 и 974 не живы, `process_alive: false`), поэтому запущен один ребёнок: pid 3853675, раннер `claude`, `standard`, `workspace-write`, назначение `telegram_user_503043504`. Раннер выбран `claude` намеренно — тогда встречную проверку ведёт Codex, у которого свободно 98% недельного окна, а не Claude, чья недоступность и держит сейчас восемь задач. Наблюдение живости снято с `thread_state.py`, а не с вывода команды запуска. Заведена 1000 (`planned`, не запущена — дерево занято 854): убрать старый долг проверки чистоты репозитория, из-за которого одобренная 4 августа задача 711 стоит `blocked` пятые сутки.
- 2026-08-09 00:41 UTC — **встречная проверка 992 не была остановлена нами: её прервал поставщик модели Codex, пометив содержимое как «flagged for possible cybersecurity risk»; после уточнения формулировки предмета проверка перезапущена, и параллельно пошла 974 по публичному репозиторию.** Наблюдение из `.runner/runner.log` прошлого прогона: `turn.failed` с этим текстом через 1 минуту 52 секунды после старта, ребёнок вышел с кодом 1 — то есть дело не в работе и не в нашем гейте. В `task.md` 992 добавлен раздел «Область работы», прямо называющий работу защитной и происходящей над своим кодом на своём сервере. Новый прогон: pid 3837171, `codex`, `standard`, `danger-full-access` по `/opt/projects/companion-agent`, предмет read-only. Вторым запущена 974 (pid 3837677, `codex`): сверить, что в публичный `task-agent` не попало ничего из раздела «не переносим» плана 631 — совместимость с Cursor, личные интеграции, история задач; предмет — другой репозиторий, поэтому оба ребёнка не делят рабочее дерево. Остальные семь свободных задач (957, 861, 854, 839, 836, 803, 754, 746) правят `/opt/projects/companion-agent`, где сейчас живёт 992, и ждут освобождения дерева. Недельное окно Codex израсходовано на 2%.
- 2026-08-08 23:41 UTC — **988 закончилась: диагноз «умер инструментальный хост ребёнка» больше не читается из файла в каталоге самого ребёнка, а решается наблюдателем на живом потоке; на её место запущена встречная проверка 992 (Codex по правкам Claude).** По `status.json` 988: `completed`, `exit_code: 0`. В `verification.md` семь пунктов, все `OK`, и все сняты живыми прогонами, а не тестами: настоящий Codex-ребёнок под обычным грантом записи в свой каталог дописал в `.runner/runtime.log` две полных записи о смерти рантайма — вердикт остался `premature_completion`, `runtime_failure=None`; тот же ребёнок сдвинул границу прогона в `.runner/runner.json` со 121 байта на 0 — вердикт не изменился; настоящая поломка (ребёнку изнутри опустили предел адресного пространства до 4 ГиБ) по-прежнему диагностируется как `child_runtime_host_died` с пометкой, что наблюдал её надзиратель на потоке, — 6 записей на потоке рантайма и ноль на обычном выводе ребёнка. Функция `child_runtime_failure`, читавшая файл, удалена. Тексты трёх документов сужены до доказанного и прямо называют границу защиты: дети с полным доступом идут под тем же root, поэтому настоящая защита от подделки требует разделения прав, а не ещё одного файла. Регрессии 522 теста, `repo-health` и проверка индекса чисто. Задача не принята: её вёл Claude, поэтому 992 идёт на Codex (`standard`, `danger-full-access` по `/opt/projects/companion-agent`, предмет неприкосновенен), запуск подтверждён наблюдением — pid 3822091 жив, `status.json` `running`. Остальные девять свободных задач (974, 957, 861, 854, 839, 836, 803, 754, 746) не запущены по единственной физической причине: они правят тот же `/opt/projects/companion-agent`, где сейчас работает ребёнок 992, а двух детей в одном рабочем дереве держать нельзя. Отдельным хвостом остаётся 990 — встречная проверка 956 закончилась вердиктом «на доработку» (панель ведёт себя верно и 305 тестов проходят, но `AGENTS.md` всё ещё предписывает снятую архитектуру); правки пойдут следующим запуском, когда дерево освободится.
- 2026-08-08 23:20 UTC — **956 закончилась, и панель впервые показала настоящее время следующей проверки; результат не принимается до встречной проверки, она запущена задачей 990 (Codex).** По `status.json` 956: `completed`, код выхода 0, исполнитель Claude. Оба замечания ревью 900 закрыты и оба проверены живьём, а не тестом. HIGH-1: опрос systemd перенесён из записи тика в сборщик панели (`scripts/process_map_state.py`), поля `next_at`/`next_src` убраны из записи тика и из контракта снимка — живой `/data.json` после перезапуска службы вернул настоящее время по всем четырём направлениям (`process` 01:00, `companion` 01:05, `moex` 01:10, `deep-research` 01:15, источник `NextElapseUSecRealtime` своего таймера), тогда как до правки во всех четырёх стоял `null`; настоящий браузер прочитал эти же четыре времени в колонках. Отдельно замерено, почему прежний порядок не мог работать: пока идёт сам тик, служба направления в состоянии `activating`, и `NextElapseUSecRealtime` пуст около 4,5 с — то есть тик физически снимал значение о себе ровно в дырке. Ветка «идёт проверка» проверена на плановом тике `companion` 01:05: для него честное «неизвестно» с наблюдённой причиной, для остальных трёх — настоящие времена. MEDIUM-1: причина «неизвестно» показана обычным видимым текстом двумя строками, снята браузером и осмотрена на снимке экрана, а не только в атрибуте `title`. Регрессии — 305 тестов, `repo-health` чисто. Ревью ведёт Codex, потому что правки писал Claude; 990 запущена `standard`, `danger-full-access` по `/opt/projects/product-owner`, предмет ревью править запрещено словами в задаче. Запуск подтверждён наблюдением: pid 3807836 жив, `status.json` `running`. Задача 988 (правки по блокирующим 987) продолжает идти в `/opt/projects/companion-agent`, pid 3764979 жив. Остальные девять свободных задач (974, 957, 861, 854, 839, 836, 803, 754, 746) не запущены по одной физической причине — все пишут в занятый 988 `companion-agent`. Недельное окно Codex — израсходован 1%.
- 2026-08-08 22:42 UTC — **Ревью 987 вернуло 985 на доработку, и правки запущены задачей 988 (Claude); параллельно в свободном дереве продакта пошла 956 (Claude).** По `status.json` 987: `completed`, код выхода 0, исполнитель Codex; в `findings.md` вердикт `rework` и два блокирующих. B1: разведение потоков сделано верно и все четыре потоковых контроля проходят, но классификатор после прогона открывает `.runner/runtime.log` по имени файла, а запускатель отдаёт ребёнку на запись весь каталог его задачи — настоящий Codex-ребёнок под обычным `workspace-write` дописал туда полную запись штатным `apply_patch`, и рабочий классификатор засчитал её как смерть рантайма; `runner.json` с границей прогона тоже доступен на запись. B2: `task_runner.py`, `SKILL.md` и `docs/task-execution.md` обещают, что поток устанавливает автора записи, и тут же рядом признают обратное. Всё остальное подтверждено: `exec --json` необходим, потребителей старого формата нет, разбор лимита Claude привязан к своему исполнителю, регрессии 345 тестов и 17 подтестов, `repo-health` и опись 881 каталога чисто, предмет ревью не менялся (совпали шесть контрольных сумм и сумма сфокусированного диффа). Правки ведёт Claude, автор 985; повторное ревью после них принадлежит Codex. 988 запущена `standard`, `danger-full-access` по `/opt/projects/companion-agent`, pid 3764979 жив. Заодно 956 (доработка панели по ревью 900: `next_at` снимается не в тот момент, причина «неизвестно» доступна только по наведению) запущена `standard`, `danger-full-access` по `/opt/projects/product-owner` — 954 там завершилась, дерево свободно; pid 3762913 жив, оба `status.json` `running`. 974 была запущена первой и остановлена через минуту руками, когда обнаружился вердикт 987: она пишет в то же дерево `companion-agent`, а доработка блокирующих замечаний важнее сверки переноса; 974 снова `planned` и берётся следующей. Остальные восемь свободных задач (957, 861, 854, 839, 836, 803, 754, 746) не запущены по той же физической причине — общий рабочий каталог с 988. Недельное окно Codex — израсходован 1%.
- 2026-08-08 22:01 UTC — **985 закрыла последнее блокирующее замечание 982 и ушла на положенное встречное ревью задачей 987 (Codex).** По `status.json` 985: `completed`, код выхода 0, исполнитель Claude. Происхождение записи «умер рантайм ребёнка» больше не определяется формой строки: запускатель разводит stdout ребёнка (`.runner/runner.log`) и собственный stderr рантайма (`.runner/runtime.log`), классификатор читает только второй файл от границы прогона `runtime_log_baseline_bytes`, а Codex-ребёнок переведён на `exec --json` — живьём измерено, что обычный `codex exec` печатает весь транскрипт ребёнка именно в stderr, то есть одно разведение потоков без этого сделало бы хуже. Четыре контроля сняты на настоящих Codex-детях: проза с цитатой — `null`; полная запись, напечатанная самим ребёнком и повторённая из stderr его команды, — 4 строки в `runner.log`, 0 в `runtime.log`, `null`, отказ `premature_completion` (против неисправленного исходника тот же пробник давал 3 срабатывания и `child_runtime_host_died`); настоящая поломка под `ulimit -v 4 ГиБ` — 2 записи, детектируется; те же записи до границы прогона — `null`. Пробник самого ревьюера из 982 прогнан без изменений и тоже даёт `null`. Регрессии: 345 тестов и 17 подтестов, `repo-health`, опись 879 каталогов чисто. В `verification.md` один GAP, записанный намеренно: пробник `runtime_spoof_probe.py` из 978 больше не проходит неизменным, потому что вносит запись рантайма в общий лог ребёнка — ровно тот вход, которому правка перестала верить; судить о нём должен встречный проверяющий, и это отдельным пунктом внесено в 987. Ревью ведёт Codex, потому что правки писал Claude; 987 запущена `standard`, `danger-full-access` по `/opt/projects/companion-agent`. Запуск подтверждён наблюдением: pid 3749676 жив, `status.json` `running`, живой прогон треда один. Остальные десять свободных задач (974, 957, 956, 861, 854, 839, 836, 803, 754, 746) не запущены по той же физической причине: все пишут в тот же `/opt/projects/companion-agent`, где теперь работает 987. Недельное окно Codex — израсходовано 0%.
- 2026-08-08 21:21 UTC — **982 закончилась вердиктом «на доработку», и правки по единственному оставшемуся замечанию запущены задачей 985 (Claude).** По `status.json` 982: `completed`, код выхода 0; в `findings.md` ровно один вердикт — `rework`. Два из трёх пунктов подтверждены живьём и закрыты: отказ по жёсткому пределу адресного пространства идёт раньше любого возврата по настроенному значению (под пределом 3 ГиБ отказали все четыре случая — `4G`, `26G`, ровно пол и `none`, ни один ребёнок не родился), и ребёнок без ограничения памяти больше не наследует смертельные 4 ГиБ вызывающего, а получает 27 917 287 424 байта, выполняет команду и доходит до конца с кодом 0. Осталось одно блокирующее: признак «умер рантайм ребёнка» по-прежнему опознаётся формой строки в общем потоке вывода, которым владеет сам ребёнок, — настоящему Codex-ребёнку велели напечатать полную запись рантайма, и классификатор засчитал все пять его собственных строк как аварию среды. Регрессии ревьюера: 344 теста и 17 подтестов за 36,76 с, `repo-health` и опись 876 каталогов чисто; предмет ревью не менялся — контрольная сумма пяти файлов совпала до и после (`4a4e13a3…`). Правки ведёт Claude, потому что автором проверенных правок 981 тоже был Claude и повторное ревью после них принадлежит Codex; 985 запущена `standard`, `danger-full-access` по `/opt/projects/companion-agent`. Запуск подтверждён наблюдением: pid 3717003 жив, `status.json` `running`, живой прогон треда один. Остальные десять свободных задач (974, 957, 956, 861, 854, 839, 836, 803, 754, 746) не запущены по той же физической причине: все они пишут в тот же `/opt/projects/companion-agent`, где теперь работает 985. Недельное окно Codex сброшено — израсходовано 0%.
- 2026-08-08 20:41 UTC — **981 закрыта, на её место запущена встречная проверка 982 (Codex по правкам Claude).** По `status.json` 981: `completed`, код выхода 0. Оба блокирующих замечания проверки 978 закрыты не пересказом, а перезапуском пробников самого ревьюера без правок: подделка сломанного рантайма — на настоящем Codex-ребёнке, где его собственная проза дала шесть срабатываний по старому правилу и ноль по новому, при этом настоящая запись рантайма (две строки `ERROR codex_core::tools::router` под `ulimit -v`) по-прежнему находится, а запись из прошлого прогона не засчитывается; отказ по адресному пространству — на настоящем запуске под унаследованным жёстким пределом 3 ГиБ, где `4G`, `26G`, значение выше пола и `none` все четыре отказали с кодом 1, назвав оба числа, и ни один ребёнок не родился. По дороге живой запуск нашёл смежный путь, которого в замечаниях не было: ребёнок с `--memory-limit none` наследовал мягкий предел вызывающего 4 ГиБ, умирал с пятью записями рантайма и признавался «умер рантайм ребёнка»; после правки тот же запуск получает 26 ГиБ, ноль записей и доходит до конца. Регрессии — 344 теста плюс 17 подтестов (у 978 базой были 340 и 6), `repo-health` и опись задач чисто. Вердикта в 981 нет намеренно: правки вёл Claude, повторное ревью принадлежит Codex — оно и запущено задачей 982 (`standard`, `danger-full-access` по `/opt/projects/companion-agent`, потому что ревью поднимает собственную сессию исполнителя). Запуск подтверждён наблюдением: pid 3696135 жив, живой прогон треда один, `status.json` `running`. Остальные десять свободных задач (974, 957, 956, 861, 854, 839, 836, 803, 754, 746) не запущены по той же физической причине, что и весь день: все они пишут в тот же `/opt/projects/companion-agent`, где теперь работает 982. Недельное окно Codex израсходовано на 27%.
- 2026-08-08 20:01 UTC — **977 закрыта и доставлена коммитом `e21b65b`; на её место запущена 981 — ответ на встречную проверку 976, которая нашла две подделки.** По `status.json` 977: `completed`, код выхода 0. Кроме уже записанного одобрения задачи 979, приёмку подтвердили ещё два независимых прохода: отдельная Claude-задача 980 проверила именно байты коммита `e21b65b` (`git archive`) и дала ровно один `Verdict: approved`, а завершающий канонический `review-candidate` связал тот же коммит с уликами задачи и решил `approved` без замечаний. Регрессии на самом коммите — 335 тестов плюс 6 подтестов (прежние 344 относились к рабочему дереву вместе с заделом 976); `repo-health` и опись задач чисто. Совместимость названа явно: миграции нет и не нужно, схема `quota-wait.json` версии 1 уже содержала все поля, а неполная запись намеренно отказывает вместо угадывания. Дальше по цепочке: встречная проверка 978 (Codex по работе Claude) вернула по 976 `rework` с двумя блокирующими замечаниями — признак «умер рантайм ребёнка» ищется голой подстрокой в любом тексте ребёнка, поэтому обычная проза, процитировавшая известную диагностику, объявляется сломанным рантаймом и доходит до статуса, журнала и уведомления «чините рантайм»; и отказ по адресному пространству пропускается при настройках `26G` и `none`, потому что жёсткий предел читается только в одной ветке, — то есть при унаследованных 3 ГиБ Codex снова попадает в мёртвый на старте рантайм. Правки ведёт автор 976, то есть Claude (задача 981, `standard`, `workspace-write` по `/opt/projects/companion-agent`), повторное ревью после них пойдёт на Codex; запуск подтверждён наблюдением — pid 3667402 жив, `status.json` `running`, живой прогон треда один. Остальные десять свободных задач (974, 957, 956, 861, 854, 839, 836, 803, 754, 746) не запущены по той же физической причине, что и весь день: все они пишут в тот же `/opt/projects/companion-agent`, где теперь работает 981.
- 2026-08-08 19:20 UTC — **977 доделала оба остатка ревью 975 и получила одобрение отдельного проверяющего: ожидание по лимиту теперь отменяется вместе с задачей, а оба примера запуска в инструкции работают.** По `verification.md`: `stop` гасит таймер, удаляет файлы ожидания и назначения, публикует отменённый и заблокированный статус, а поздний вызов таймера сообщает «уже занято» и ничего не запускает; просроченное ожидание, у которого юнита нет вовсе, честно становится неудачей возобновления и уведомляет только адрес с подтверждённой привязкой; оба примера в `skills/task-runner/SKILL.md` (строки 429 и 452) проходят сухой запуск с кодом 0. Автор правок — Codex, поэтому повторную проверку вёл Claude: отдельная read-only задача 979 дала ровно один `Verdict: approved` по неизменившимся хешам кандидата. Регрессии — 344 теста плюс 6 подтестов, `repo-health` и опись задач чисто; коммит `e21b65b`. Задача пока не закрыта: ребёнок жив 40 минут и дописывает завершение, статус в `task.md` ещё `in_progress`, поэтому приёмка — на следующем пробуждении. Отдельно про 711 (она сегодня снова показалась в очереди как `blocked`): её артефакты от 4 августа не менялись, обязательное независимое ревью закрыто ещё задачей 716 с вердиктом `approved`, и держит её единственный чужой долг — `repo-health` ругается на старые снимки задач 255/586/587/683, к правкам 711 отношения не имеющие. Десять свободных задач (974, 957, 956, 861, 854, 839, 836, 803, 754, 746) не запущены по той же физической причине, что и весь день: все они пишут в `/opt/projects/companion-agent`, где ещё живёт 977.
- 2026-08-08 17:41 UTC — **976 закончилась, и её главное свойство проверено не прозой, а живым запуском следующего же ребёнка: Codex стартовал без ручного `--memory-limit` и получил 26 ГиБ адресного пространства вместо смертельных 4.** Снято с `/proc/3588333/limits` — `Max address space 27917287424`; в `.runner/runner.json` задачи 978 стоит поле `address_space_bound`. То есть обход `--memory-limit 26G`, которым сегодня велись все запуски, больше не нужен. По артефактам 976: `ADDRESS_SPACE_FLOOR_BYTES` — общая таблица для `codex` и `agent` вместо прежнего одиночного порога Cursor, унаследованный мягкий `ulimit -v` больше не считается непреодолимым (мягкий предел поднимается до жёсткого), и у смерти инструментального хоста появился собственный финал отказа `child_runtime_host_died`, который решается раньше двух прежних, — раньше такой прогон выглядел как «отклонено преждевременное завершение», то есть как плохая работа исполнителя, и задача 971 потеряла на этом три запуска подряд. Регрессии — 340 passed (добавлено 9 тестов), `repo-health` и проверка индекса задач чисто. Сама 976 не принята: её вёл Claude, поэтому встречная проверка идёт на Codex задачей 978 (`standard`, `danger-full-access` по `/opt/projects/companion-agent`, предмет неприкосновенен), запуск подтверждён наблюдением — pid жив, `status.json` `running`. Два `GAP` названы автором честно и на приёмку вынесены ревью: точный порог Codex не бисектился между 8 и 16 ГиБ (взят общий с Cursor 26 ГиБ), а резидентный предел через `MemoryMax` systemd-области не сделан — из песочницы ребёнка он не проверяется живьём (`Failed to connect to bus`). Отсюда честная цена, записанная и в коде, и в `SKILL.md`, и в `AGENTS.md`: на хосте с 7,8 ГиБ ОЗУ предел 26 ГиБ не ограничивает Codex- и Cursor-ребёнка практически ни в чём — защита не переехала, а исчезла, и фактическим ограничителем остаются `memory_relief.sh` и `earlyoom`. Остальные десять свободных задач (977, 974, 957, 956, 861, 854, 839, 836, 803, 754, 746) не запущены по той же физической причине, что и раньше: все они пишут в тот же `/opt/projects/companion-agent`, где теперь работает 978. Первой после неё идёт 977 — её собственный текст держал старт до конца 976.
- 2026-08-08 16:45 UTC — **Повторное ревью 975 закончилось вердиктом `rework`, и в работу пошла та поломка среды, из-за которой сегодня падали все Codex-дети: задача 976, исполнитель Claude.** Ревью 975 (Claude по правкам Codex из 970) выдало письменный вердикт в `findings.md`: D1, D4, D5, D6 закрыты полностью и проверены по связанным коммитам и живым запуском; D2 закрыт по главному свойству — ни один из четырёх путей возобновления после лимита больше не молчит, — но согласованный автором подпункт не сделан: `stop` и `reconcile_abandoned_run` не знают про `quota-wait.json`, поэтому остановленную задачу возобновит её же таймер (раздел `verification.md` — `GAP`); D3 — три названных примера в `skills/task-runner/SKILL.md` починены, а два того же класса на строках 429 и 452 остались без обязательного `--destination` и дали ревьюеру дословный отказ с кодом 1 (раздел — `FAIL`). Правки по этим двум остаткам поставлены задачей 977 на Codex (автор предмета — он), повторное ревью после них — Claude. Запущена не она, а 976: наш собственный `RLIMIT_AS` 4 ГиБ (`DEFAULT_CHILD_MEMORY_LIMIT_BYTES`) убивает инструментальный хост нового Codex ещё до первой команды, и наружу это выглядит как «отклонено преждевременное завершение», то есть как плохая работа исполнителя. Пока это не починено, каждый Codex-запуск идёт с ручным `--memory-limit 32G`, а любая смерть среды неотличима от провала работы — поэтому среда идёт первой. Исполнитель — Claude (`--memory-limit 26G`), `standard`, `workspace-write` по `/opt/projects/companion-agent`; встречную проверку после неё ведёт Codex. Запуск подтверждён наблюдением: pid 3527521 жив, `thread_state` показывает `running`. Задача 973 отменена как дубль 976 — тот же дефект описан дважды. Остальные десять свободных задач (977, 974, 957, 956, 861, 854, 839, 836, 803, 754, 746) не запущены по физической причине: все они пишут в тот же `/opt/projects/companion-agent`, где сейчас работает 976.
- 2026-08-08 16:01 UTC — **Правки по шести замечаниям ревью 968 сделаны и проверены, кроме одного: сам повторный проверяющий не выдал вердикта. Поставлено заново задачей 975 на Claude.** По `verification.md` задачи 970 четыре раздела из пяти `OK` (входы продакта маршрутизируются через `claude_product_owner.py` в связанном коммите `abcc8a3`; все четыре молчащих пути ожидания по лимиту получили терминальный исход и уведомление; эффективная модель переживает возобновление; 302 теста продакта, полный набор task-runner, `repo-health` и опись 868 каталогов задач — чисто). Пятый, `claude_rereview`, — `BLOCKED`: отдельная сессия Claude (Fable) прочитала предмет и коммиты `abcc8a3`/`db43040`, но три ограниченных продолжения кончились внутри размышления, не написав решения; артефакта одобрения нет. Это не отказ по существу и не находка — это несостоявшийся проход, поэтому предмет не трогаем, а повторяем ревью: задача 975, исполнитель Claude на Opus (маршрутизатор в 16:01 UTC показал `no_opus_specific_limit`, недельное окно израсходовано на 70%), только чтение, полный доступ к песочнице ради второго репозитория предмета, требование написать вердикт в файл до любых дополнительных проверок. Запуск подтверждён наблюдением: pid 3511252 жив, `status.json` — `running`. Остальные одиннадцать свободных задач не запущены по физической причине: они пишут в тот же `/opt/projects/companion-agent`, где сейчас читает 975, и правки предмета во время его ревью недопустимы.
- 2026-08-08 15:33 UTC — **Причина, по которой Codex-дети сегодня умирали за полминуты, найдена и воспроизведена: их убивал наш собственный лимит памяти. 970 снова идёт на Codex.** Наблюдение, а не догадка: точная команда запуска ребёнка, выполненная руками из родительского контекста, отрабатывает нормально; та же команда под `ulimit -v 4194304` (это ровно наш `DEFAULT_CHILD_MEMORY_LIMIT_BYTES` = 4 ГиБ, который `child_resource_limiter` ставит ребёнку через `RLIMIT_AS`) немедленно даёт `code-mode host exited with status signal: 5 (SIGTRAP)` и «Blocked by the execution environment», а под `ulimit -v 33554432` (32 ГиБ) та же команда с тем же файлом подсказки читает файлы и работает. То есть новый механизм вызова инструментов Codex резервирует адресное пространство при старте, как раньше это делал Cursor (для него в коде уже стоит порог 26 ГиБ, `CURSOR_ADDRESS_SPACE_FLOOR_BYTES`), и наши 4 ГиБ его убивают. Прошлая запись 15:02 утверждала обратное — «контрольный запуск под тем же ограничением памяти 4 ГБ отработал успешно»; это утверждение снимается: контрольный запуск шёл без `ulimit -v`, флаг `--memory-limit` на ручной вызов `codex` не действует. Задача 970 (правки по кросс-ревью 968, автор предмета Codex) перезапущена в 15:33 UTC с `--memory-limit 32G`, pid 3491217; живость подтверждена процессом в `/proc` и тем, что ребёнок уже прочитал `skills/task-executor/SKILL.md` — то есть вызовы инструментов проходят. Постоянная починка умолчания поставлена задачей 973; до неё каждый запуск Codex-ребёнка идёт с явным `--memory-limit 32G`. Остальные восемь свободных задач (957, 956, 861, 854, 839, 836, 803, 754, 746) не запущены по физической причине: они пишут в тот же `/opt/projects/companion-agent`, где живёт 970.
- 2026-08-08 15:02 UTC — **Codex как исполнитель сейчас не работает вовсе: три прогона подряд умерли за полминуты на отказе его собственного механизма вызова инструментов, поэтому 944 перезапущена на Claude, а правки по ревью 968 запускать некому.** Наблюдение, а не пересказ: в `tasks/970-process-map/.runner/runner.log` и `tasks/944-proactive-product-owner/.runner/runner.log` каждый вызов инструмента кончается `code-mode host exited with status signal: 5 (SIGTRAP)` либо `code-mode host closed its stdout`; ребёнок 970 умер так трижды (14:03, 14:21 у 944 и 15:01 у 970 повторно), не прочитав ни одного файла задачи, и каждый раз это выглядело как «отклонено преждевременное завершение», хотя работа не начиналась. Отдельный контрольный запуск `codex exec` из родительского контекста, в том числе под тем же ограничением памяти 4 ГБ и с тем же рабочим каталогом, отработал успешно и прочитал `skills/task-executor/SKILL.md` — значит ломается не Codex целиком, а его путь вызова инструментов внутри нашего дочернего конверта. Бюджет недельного окна ни при чём: израсходовано 25%, сброс 2026-08-14. Последствие для очереди: 970 (правки по кросс-ревью 968) ведёт строго Codex как автор предмета 966, и запускать её нельзя; 944 перезапущена на Claude (`standard`, `--sandbox-mode danger-full-access`, `--destination telegram_user_503043504`, pid 3463556, живость подтверждена процессом в `/proc` и `live_runs` направления). Остальные восемь свободных задач (957, 956, 861, 854, 839, 836, 803, 754, 746) не запущены по физической причине: они пишут в тот же `/opt/projects/companion-agent`, где теперь живёт 944.
- 2026-08-08 14:21 UTC — **Запустил починку того, из-за чего ребёнок не может закоммитить собственную одобренную работу: рабочее дерево companion-agent четвёртый день несёт четырнадцать чужих несохранённых правок.** Задача 944, запущена на Codex (`standard`, `--sandbox-mode danger-full-access`, pid 3446532, живость подтверждена процессом в `/proc`, `state: running` и появлением в `live_runs` направления). Полный доступ здесь не разрешение править предмет, а необходимость: приёмка требует живого дочернего прогона, который сам правит файл и сам его коммитит, а из вложенной песочницы `workspace-write` такой прогон не поднимается. Наблюдаемый повод, а не пересказ исполнителя: `git status` в companion-agent показывает четырнадцать изменённых отслеживаемых файлов, среди них владельцы `task_runner.py`, `dev_pipeline_adapter.py`, `pipeline_notify.py` и `gmail_client.py` с их тестами, — то есть каждый следующий ребёнок стартует поверх чужой незакрытой работы. Проверяющим пойдёт строго Claude: предмет пишет Codex, а окно Claude сброшено в 15:10 CEST и израсходовано на 9%. Бюджет Codex: 25% недельного окна, сброс 2026-08-14. Прежний прогон 970 к этому моменту уже не жив (`process_alive: false`, отклонён на преждевременном завершении), поэтому рабочее дерево было свободно; остальные девять свободных задач (957, 956, 861, 854, 839, 836, 803, 754, 746) не запущены по той же физической причине — они пишут в тот же `/opt/projects/companion-agent`, где теперь живёт 944.
- 2026-08-08 14:15 UTC — **Две работы, которые механизм двадцать пятые сутки зовёт недоставленными, доставлены не будут — и это решение, а не забывчивость.** По 631 (план отложенного переноса процессной схемы в публичный `task-agent`) и 606 (план усиления процессных правил) решения записаны в каталоги задач файлами `product-owner-decision.md`. Основания наблюдаемые: перенос из 631 уже состоялся задачей 492 — в `/opt/projects/task-agent` лежат ровно те владельцы, чей перенос план объявлял будущим и условным (`task_completion.py`, `task_contract.py`, `dev_pipeline_adapter.py`, `pipeline_notify.py`, головной коммит `f8412d0`); документ 606 был отправлен общим ботом ещё 31 июля с `message_id=17775`, а его нынешняя редакция на диске (34 498 байт против доставленных 31 458) описывает правила, которые с тех пор реализованы задачами 612, 621, 627. Пользовательской пользы у обеих доставок ноль. Сам механизм наблюдения этих решений не видит и продолжает будить: он ищет записку о доставке или квитанцию в каталоге задачи, а свидетельство 606 лежит только в тексте `verification.md`. Это ровно предмет задачи 939, которая стоит `blocked` в ожидании обязательного повторного ревью Claude.
- 2026-08-08 14:05 UTC — **Встречная проверка резервного продакта закончилась «на доработку», и главное замечание — что доставленный кандидат не включает то, ради чего всё делалось: фоновые пробуждения по-прежнему запускают модель напрямую.** Ревью 968 (Claude, автор предмета — Codex) закрыто исполнением, вердикт `rework` в `tasks/968-process-map/findings.md`. Что подтвердилось само: команда `codex-pm` действительно поднимает того же владельца на `gpt-5.6-sol` из каталога продакта (проверено разрешённой командой запуска, а не отчётом автора); выбор резерва консервативен на девяти видах ответа о квоте — неизвестная схема, строковый процент, отказ сети и 99,9% общего лимита оставляют Opus, Codex выбирается только при наблюдаемом исчерпании; задача Claude при лимите исполнителя не меняет, возобновляется той же попыткой и той же сессией ровно один раз, повторный тик отвечает «уже занято»; секретов в артефактах нет; регрессии 325+2, 161 и 10 воспроизведены руками ревьюера. Два блокирующих замечания: **D1** — в коммите `f968d66` файл `scripts/thread_tick.py` всё ещё вызывает модель напрямую, без маршрутизатора, а маршрутизация лежит только в незакоммиченном рабочем дереве, которое опись кандидата не связывает; при этом все четыре таймера `product-thread@*` живут именно на этом пути, то есть обещание «фоновые входы уходят на резерв» сейчас не работает. **D2** — четыре пути возобновления (срок не наступил, файл адреса нечитаем, адрес не совпал, прежний прогон жив дольше 30 секунд) оставляют задачу в положении «ждёт», ничего не отправляют и расходуют одноразовый таймер, перевзвести который некому: задача может замолчать навсегда, а это прямое нарушение условия задачи. Плюс четыре мелких: примеры запуска в описании навыка сами падают с кодом 1 из-за нового обязательного адреса уведомления, возобновлённый прогон может сменить модель, две записи об уликах сильнее самих улик (в сохранённой заготовке стоит `pending` там, где отчёт говорит «доставлено», а переживание перезагрузки прочитано в коде, а не проверено), и обёртка `codex-pm` не даёт себя опросить. Правки ведёт Codex как автор предмета — запущена **970** (Codex, `standard`, `--sandbox-mode danger-full-access`, pid 3439688, живость подтверждена процессом в `/proc` и `state: running`), порядок в задаче задан строгий: анализ с «согласен/не согласен» по каждому из шести пунктов, план, правки, потом повторное ревью строго Claude. Бюджет Codex: израсходовано 25%, сброс 2026-08-14. Отдельно за собой: я завёл **969** на то же самое ревью 966, не посмотрев, что оно уже проведено 968 сорока минутами раньше; прогон остановлен через минуту после старта, предмет не тронут, задача помечена отменённой. Остальные десять свободных задач (957, 956, 944, 861, 854, 839, 836, 803, 754, 746) не запущены по прежней физической причине: они пишут в `/opt/projects/companion-agent`, где теперь живёт 970.
- 2026-08-08 13:41 UTC — **Резервный продакт на Codex и переживание лимита Claude собраны целиком, и я отдал их встречной проверке — а заодно проверил живьём на самом себе.** Задача 966 (её вёл Codex) кончилась со всеми восемью разделами проверки в положении `OK`: настоящая команда `codex-pm` поднимает того же продуктового владельца из `/opt/projects/product-owner` на модели `gpt-5.6-sol` и не трогает продуктовые файлы (шесть контрольных сумм и состояние репозитория совпали до и после); маршрутизатор оставляет Opus при недоступной проверке квоты и уходит на Codex только при наблюдаемом исчерпании обеих моделей; отказ по лимиту переводит задачу Claude в ожидание с точным сроком и уведомлением, а по наступлении срока возобновляет её ровно один раз — повторное срабатывание таймера ответило `already claimed` и второго прогона не создало; регрессии 302+10, 161 и 325+2 теста, здоровье репозитория и опись 860 задач прошли. Предмет проверки — коммиты `f968d66` (продакт) и `5e503cc` (публичное ядро) плюс семь незакоммиченных файлов companion-agent, чьи контрольные суммы записаны в `implementation-evidence.json`. Сам прогон 966 остановлен мной на подходе к проверке, поэтому работу принимаю не я, а проверяющий: писал Codex — значит проверяет строго Claude, запущена **968** (Claude, `standard`, `--sandbox-mode danger-full-access`, pid 3429288, живость подтверждена процессом в `/proc` и `state: running`). Побочное живое свидетельство того, что 966 работает: мой собственный запуск 968 был отклонён её новым сторожем со словами «standard Claude требует адрес уведомления, чтобы ожидание по лимиту было кому сообщить», и прошёл только после указания авторизованного адреса. Пятичасовое окно Claude сбросилось в 15:10 CEST (израсходовано 9%, следующий сброс 18:10 UTC), поэтому проверяющий сегодня доступен — на этом же сбросе снимается и вынужденная пауза 939, ждавшая ровно его. Остальные десять свободных задач (957, 956, 944, 861, 854, 839, 836, 803, 754, 746) не запущены по одной физической причине: они пишут в `/opt/projects/companion-agent`, где сейчас лежат незакоммиченные файлы — предмет ревью 968, и правка предмета под проверяющим запрещена.
- 2026-08-08 10:01 UTC — **Список «сделано, но не доставлено» будит меня двадцать четвёртый раз одним и тем же, и вместо очередного разбора вручную запущена правка самого наблюдателя.** Пробуждение принесло ровно тот же перечень из 24 задач (928, 924, 919, 916, 909, 905, 895, 890, 889, 885, 881, 871, 866, 864, 860, 859, 848, 837, 835, 832, 829, 826, 631, 606), что и прошлые; разбор 835 (`tasks/835-undelivered-tail-audit/deliverables/tail-audit-2026-08-06.md`) уже показал состав: почти всё это `conclusion-ru.md` и `review-NNN-ru.md` — внутренние заключения прогонов, которым доставка человеку не нужна вовсе, настоящих пользовательских документов в списке два (606, 631). Причина в коде наблюдателя: `scripts/process_map_state.py` объявляет задачу недоставленной по отсутствию файла `delivery.md` рядом (`DELIVERY_NOTES`, строка 607) и не смотрит ни на вид документа, ни на уже наблюдаемые доставки, хотя отправленная почта продакта лежит на диске с 6 августа. Запущена 939 (Codex, `--sandbox-mode danger-full-access`, `--repo /opt/projects/product-owner`, pid 3363282); живость подтверждена процессом в `/proc` и `state: running` в `status.json`, а не выводом команды запуска. Codex взят как автор по чередованию — предыдущая правка того же наблюдателя (916) шла на Claude, значит повторное ревью 939 пойдёт строго к Claude. Бюджет Codex: израсходовано 22%, сброс 2026-08-14. Остальные десять свободных задач (957, 956, 944, 861, 854, 839, 836, 803, 754, 746) не запущены по одной физической причине: они пишут в те же деревья, где теперь живёт 939.
- 2026-08-08 09:05 UTC — **Оба закончившихся прогона разобраны по артефактам, и ни один не требует моего решения: 951 сдана исполнением и уже под встречной проверкой, 934 закрыта как отменённая.** У 951 `status.json` — `completed`, код возврата 0, и `verification.md` держит семь разделов, каждый со словом `OK` и с уликой на диске: отказ воспроизведён по журналу systemd до починки, `thread_state.py` отработал на всех четырёх направлениях, прогон в исчезнувшем пространстве имён теперь считается мёртвым (на настоящей записи 934), нарочно испорченное имя роняет регрессию, весь набор — 271 тест, файлы состояния снова пишутся штатным таймером с нулевым кодом на всех четырёх юнитах, а панель после перезапуска показывает новый смысл, а не старый из памяти процесса. Ни одного `GAP` в проверке нет. Писал Claude — проверяет Codex: 954 жива по pid 3321949, прогресс писан 10 минут назад, шаг — «опись всех зависимостей product-owner от companion-agent и проводка регрессии». У 934 `status.json` показывает `failed` только потому, что ребёнок умер в чужом пространстве имён без кода возврата; сама задача в `task.md` стоит `cancelled` с причиной «то же ревью проведено задачей 937», то есть предмет не потерян и переделывать нечего. Ничего не запущено по одной физической причине: единственное живое дерево этого направления — `/opt/projects/companion-agent` — занято 954, а `/opt/projects/product-owner` сейчас предмет её ревью, и правка предмета под ревьюером запрещена; в самой 956 это записано условием запуска. Все одиннадцать свободных задач очереди пишут ровно в эти два дерева.
- 2026-08-08 09:0x UTC — **Пользователь выбрал чинить путь пробуждения почты, а не переписывать обещание, и назвал устройство: асинхронно, с независимыми проверками.** Письмо `19fe08da0a8f5b0d`: «кажется нормально пробуждать асинхронно и снова включать счётчик, чтобы проверки не зависили друг от друга… а новая проверка уже берёт следующие письма — и там хоть несколько потоков в параллель». Его догадка о причине верна, и причин оказалось две. Первая известна: `poll()` в `mail_product_owner.py:427` держит `single_instance(poll.lock)` на всё время прогона агента. Вторая крупнее и раньше не наблюдалась: cron здесь — systemd-cron, строка crontab развёрнута в `cron-root-root-0.service` с `Type=oneshot`, и пока единица активна, следующее срабатывание таймера не поднимает второй процесс вовсе — оно ставится в очередь. Улики из `journalctl` за сегодня (CEST): старт 10:15:00 → конец 10:24:16 → немедленный старт слота 10:20; старт 10:35:00 → конец 10:45:17 → старт слота 10:40; конец 10:51:34 → старт слота 10:50. Это объясняет счёт в `cron.log`: 925 `empty`, 36 `processed` и ровно один `already_running` на 962 строки — заблокированный тик следа не оставляет. Числа по `ledger.sqlite3` сняты мной самим и потому предварительные: медиана разбора 555 с, максимум 1016 с, задержка больше 300 с у 11 писем из 39, худшая живая 526 с; само письмо пользователя поднято через 301 с. Заведена **957** — проверка забирает письма и завершается за секунды, разбор уходит отдельным переживающим родителя процессом, параллельность ограничена настройкой и считается по живости pid, журнал переводится в многописательский режим, запрет на автоматическое переигрывание упавшего письма сохраняется. Не запущена по физической причине: `/opt/projects/companion-agent` занят живой 954. Ответ отправлен — `19fe096868010a51`, sha256 тела `ca6ba7919fef0d8f3eef16242247725ae4fb6a97da73fe1e65ff311020cd6233`.
- 2026-08-08 08:50 UTC — **Форма отчёта пользователю изменена по его прямому указанию, и это правило теперь живёт в `AGENTS.md`, а не в разговоре.** Письмо `19fe0852d81028af`: при куче параллельных треков он не понимает, о чём отчёт — «прибегаешь и говоришь: я всё проверил, всё получилось — и убегаешь. У тебя контекст, а у меня его нет»; и отдельно — «прозу читать долго, писем много… можно списками — крупные пункты с расшифровкой». Внесены два пункта в раздел «Как разговаривать с пользователем»: письмо начинается с шапки «над чем работаем» (задачи с номерами и одной фразой на каждую), подача идёт списками без разметки и рамок. Урок 9 закрыт этим же владельцем. Ответ отправлен в новой форме — `19fe0915ca2322f9`, sha256 тела `ee5210fcfc6c6edead506b9c171e3ca4b9e9d601457a7d3c46caa88ad89def7d`.
- 2026-08-08 08:50 UTC — **Панель по-прежнему не отвечает, когда продакт проверит статус в следующий раз: ревью 900 (Codex) вернуло правку 890 на доработку, и я ошибочно доложила пользователю, что проверка ещё ждёт очереди.** Замечания читаны в `tasks/900-review-890-panel-next-check/findings.md`: HIGH-1 — `thread_tick.py` снимает `NextElapseUSecRealtime`, пока сервис `product-thread@<тред>.service` активен, а тогда таймер не взведён, поэтому во всех четырёх `state/threads/*.json` стоит `next_at=null`; между тиками настоящее время существует и было прочитано сухим прогоном `companion` (`2026-08-07T20:05:00+02:00`). MEDIUM-1 — причина «неизвестно» уходит только в атрибут `title` шаблона панели, то есть без наведения указателя недоступна. Сама 900 помечена `blocked` не по существу, а гейтом (не установлены семейства политик контракта); вердикт при этом записан. Правку 890 делал Claude, ревью вёл Codex — доработку ведёт Claude, повторное ревью Codex. Заведена **956**, `planned`: ждёт освобождения рабочего дерева `product-owner`, где сейчас работает 954.
- 2026-08-08 08:46 UTC — **Наблюдение продакта снова работает, и это проверено исполнением, а не отчётом автора: 951 закрыта, все четыре направления считаются без ошибки, файлы состояния свежие.** Своими командами: `scripts/thread_state.py` отработал на `process`, `companion`, `moex` и `deep-research` с нулевым кодом, а `state/threads/*.json` записаны между 10:26 и 10:42 местного времени вместо прежнего провала с 00:27. Напомню предмет: 938 переименовал в `task_runner.py` функцию `runner_pid_namespace_visible` в `runner_pid_namespace_state` и поменял её смысл, а потребитель в `product-owner` пары к этому не получил — и панель, и фоновые пробуждения работали вслепую 9ч49м–10ч07м — от последних успешных тиков 00:10:02–00:27:28 CEST (22:10:02–22:27:28 UTC предыдущих суток) до снятия наблюдения в 08:17 UTC. Первая запись называла «почти восемь часов», смешав CEST с UTC; исправлено 961 по MEDIUM-1 ревью 954. Писал 951 Claude, значит проверяет Codex: заведена и запущена **954**, живая по pid. Ревью проверяет не пересказ, а четыре вещи исполнением — что состояние настоящее (сверка живых прогонов с `ps`), что перенесён смысл, а не только имя (невидимое пространство имён не объявляется мёртвым), что второго такого расхождения не осталось (нужен перечень проверенных мест, а не вывод «других нет»), и что обещанная автором проверка существует и падает на настоящем расхождении. Рабочее дерево `product-owner` в момент запуска не чистое — там незакоммиченные продуктовые записи, и в задаче сказано, что это фон, а не находка.
- 2026-08-08 08:45 UTC — **Застрявший навсегда прогон 934 закрыт обычной командой из родительского контекста: первый из двух блокеров 938 снят наблюдением, второй держится занятым деревом.** Кандидат 938 лежит в рабочем дереве незакоммиченным (четыре отслеживаемых пути навыка `task-runner` и `docs/task-execution.md`), и на нём я снял живую проверку, которую ребёнок сделать не мог физически: изнутри песочницы у него PID 1 — `bwrap`, и хостового пространства имён он не видит. Наблюдалось: на машине существует ровно одно пространство имён процессов `pid:[4026531836]`, записанное в 934 `pid:[4026532326]` отсутствует; штатный `task_runner.py status tasks/934-...` без единой ручной правки метаданных вернул `signal: not_running`, сам переписал `.runner/runner.json` (`ab8edb5a…` → `8be7dba5…`), а `live-run-status` сменил `visibility` с `different_pid_namespace`, при котором отказывали и start, и stop, и reattach, на `recorded_namespace_absent` с `live: false`. То есть приёмочное условие 938 наблюдается выполненным; статус задачи я не двигаю — его снимает исполнитель, наблюдение записано в `tasks/938-runner-dead-namespace/product-owner-live-observation.md`. Одноразовое состояние при этом израсходовано: повторить проверку на самой 934 больше нельзя, нужен новый искусственно осиротевший прогон. Второй блокер остаётся: код 938 написан Codex, значит ревьюер — только Claude, и запустить его сейчас нельзя — в `/opt/projects/companion-agent` живёт прогон 951 (pid 3306412, прогресс писан 13 минут назад). По той же единственной причине не запущены и девять свободных задач очереди: все они пишут в то же дерево. Событие «934 числится running, но процесс мёртв» закрыто этой же проверкой, 938 в `blocked` — по второму блокеру, а не по первому.
- 2026-08-07 22:22 UTC — **Одобренный кандидат почтовых хвостов закоммичен, 928 закрыта, и вместо неё в дерево запущена 938 — дефект, из-за которого этот тупик и возник.** Закрывающий прогон 928 (pid 3226881) умер на последнем шаге со своим блокером в `status.json`: «`.git` доступен ребёнку только на чтение, git не смог создать `index.lock`». Кандидат при этом был цел и одобрен — пересчитанный отпечаток отслеживаемого diff поверх `7666af1` дал ровно `a15903011ff69ea042417a40f28d1ec2d6c21078c3c353440c0983d00fa9f733`, тот же, что записан в `candidate-manifest.json` и что сверял ревьюер 937 до и после работы, — поэтому я закоммитил его из родительского контекста: `f7bdc2f`, четыре отслеживаемых пути навыка `gmail-client`, 168 строк добавлено, 7 удалено, ничего сверх. Запись о коммите дописана в `trace.md` задачи 928, статус 928 — `completed`. Сняты два мёртвых номера: 934 (`cancelled` — её ребёнок остался в исчезнувшем пространстве имён, то же ревью проведено 937) и 942 (`cancelled` — я завёл её на уже сделанное ревью). Заведена 944 на настоящую причину последнего тупика: ребёнок с `workspace-write` не может закоммитить собственный одобренный результат, и работу за него закрывает родитель — приёмка живым прогоном, который правит файл и коммитит его сам. Дерево `/opt/projects/companion-agent` освободилось коммитом, и в него сразу запущена 938 на Codex (`--sandbox-mode workspace-write`, pid 3238398, живость подтверждена в `/proc` через 25 секунд после старта): разделить «пространство имён не наблюдаемо отсюда» и «пространство имён больше не существует», приёмка — живое закрытие или перезапуск прогона 934 без ручной правки `.runner/runner.json`. Остальные восемь свободных задач не запущены по той же физической причине, что и раньше: они пишут в то же единственное дерево, где сейчас идёт 938. Бюджет Codex — израсходовано 19%, сброс 2026-08-14.
- 2026-08-07 22:03 UTC — **Ревью почтовых хвостов пришло одобрительным, и 928 доводится до конца; своя ошибка — завёл дубль ревью, не прочитав строку выше.** 937 закрылась `completed` в 21:59 UTC с единственной строкой `Verdict: approved`; тождество кандидата ревьюер сверил до и после работы, отпечаток `a15903011ff69ea042417a40f28d1ec2d6c21078c3c353440c0983d00fa9f733` поверх `7666af1` совпал, все пути `candidate-manifest.json` — поштучно. Единственный GAP ревью, `calypso_message_reread`, задачей не требовался: перечитать уже отправленное сообщение Telegram нечем — Bot API такого метода не имеет, копия или пересылка были бы новым сообщением, — поэтому проверялась логика пробника и подстановка идентификатора в текст. Поэтому 928 перезапущена на Codex (её же авторе, pid 3226881, живость подтверждена в `/proc`, `--sandbox-mode workspace-write`, дерево `/opt/projects/companion-agent`) с дописанным продолжением: снять блокировку, заменить BLOCKED-строку `claude_cross_review` ссылкой на 937, закоммитить кандидат, закрыть задачу. Ждать 934 больше не нужно ни минуты. Отдельно и честно: перед этим я завёл и запустил 942 на то же самое ревью, потому что пошёл читать артефакты 934 раньше, чем строку «В работе» ниже, где 937 уже была названа; ребёнок 942 остановлен через пятьдесят секунд (`stopped_pid: 3226023`), дерево не тронуто, но лишний прогон в боевом репозитории — цена моей же перевёрнутой очерёдности. Тупик по пространству имён не обошёлся: `start`, `stop` и `reattach` по 934 отказали и корневому контексту тоже, хотя гибель записанного ребёнка доказана отсутствием любого процесса в `pid:[4026532326]`, — лечит 938. Свободные задачи (938, 939 и семь прочих) не запущены по одной физической причине: все они пишут в то же дерево `/opt/projects/companion-agent`, где сейчас живёт закрывающий прогон 928 и лежит его незакоммиченный кандидат. Бюджет Codex — израсходовано 18%, сброс 2026-08-14.
- 2026-08-07 21:42 UTC — **Обязательное ревью почтовых хвостов умерло вместе с песочницей, которая его запустила, и поднято заново из корневого контекста.** 928 сделала все пять правок и закрылась `blocked`: в `verification.md` точечные регрессии почты (32 теста), полный набор Gmail (48 тестов), живая тревога Calypso с идентификатором письма и здоровье репозитория стоят PASS, а строка `claude_cross_review` — BLOCKED. Причина наблюдаемая: ревью 934 Codex запустил изнутри своей песочницы, её пространство имён процессов `pid:[4026532326]` умерло вместе с ним, лог ребёнка так и остался нулевого размера. На машине сейчас существует ровно одно пространство имён — корневое `pid:[4026531836]`, то есть смерть записанных pid 10 и наблюдателя pid 9 доказана; но защита в `task_runner.py` сравнивает записанное пространство имён на равенство с нынешним, поэтому `start`, `stop` и `reattach` отказывают всем, включая корневой контекст, и обхода не предусмотрено. Кандидат при этом цел: отпечаток отслеживаемых правок пересчитан и совпал с записанным в `candidate-manifest.json` — `a15903011ff69ea042417a40f28d1ec2d6c21078c3c353440c0983d00fa9f733` поверх `7666af1`. Поэтому ревью того же кандидата ведёт новая 937 (Claude, `--sandbox-mode read-only`, pid 3208848, живость подтверждена процессом в `/proc` и корневым пространством имён — этот прогон при смерти будет закрываем). Пара «автор — проверяющий» соблюдена: писал Codex, проверяет Claude. Заведены две задачи по найденному: 938 — тупик защиты по пространству имён, 939 — область «сделано, но не доставлено» зовёт недоставленными внутренние заключения ревью. Ни одна из них и ни одна из семи свободных задач не запущена по единственной физической причине: все они пишут в дерево `/opt/projects/companion-agent`, где живёт обязательное ревью 937, а 938 к тому же правит сам механизм, которым этот прогон сейчас идёт. Бюджет Codex — израсходовано 18%, сброс 2026-08-14.
- 2026-08-07 21:20 UTC — **Почтовый путь закрыт целиком, следом запущены его эксплуатационные хвосты.** 918 завершена наблюдением, а не прозой: `status.json` — `state: completed`, `exit_code: 0`, `status_detail: exact_commit_7666af1_all_gates_passed`; в `verification.md` строка `claude_cross_review` стоит PASS с привязкой к коммиту `7666af12f2871acb495e312f4d8b9880c4a78628`, а последняя проверка политики закрылась без замечаний с полными путями доставленных источников. Освободившееся дерево `/opt/projects/companion-agent` сразу занято 928 (Codex, `--sandbox-mode workspace-write`, pid 3188306, живость подтверждена процессом в `/proc` и `state: running` в `status.json`): пять непреграждающих находок ревью 927 — регрессия на «нужно внимание» вместо «писем нет», идентификатор застрявшего письма в тексте тревоги, документированный способ закрыть разобранное состояние без ручной правки базы, вывод команды тревоги вне машиночитаемого потока, обрубленная фраза в ожидаемом эффекте 727. Автор правок — Codex как автор всего почтового пути, повторное ревью после них пойдёт строго к Claude. Остальные семь свободных задач (861, 854, 839, 836, 803, 754, 746) не запущены по единственной физической причине: они пишут в то же дерево, где теперь живёт 928. Бюджет Codex — израсходовано 18%, сброс 2026-08-14.
- 2026-08-07 21:0x UTC — **Одобрение почтового пути теперь привязано к самому коммиту: проверяющий сверил байты, сам прогнал регрессии и подтвердил поведение.** 930 (Claude, строго read-only) закончилась сама: `status.json` — `state: completed`, `findings.md` несёт ровно одну строку `Verdict: approved`, восемь проверок `verification.md` из девяти PASS. Проверено наблюдением, а не словами автора: коммит `7666af12f2871acb495e312f4d8b9880c4a78628` меняет ровно шесть путей (+233/−28, ни добавлений, ни удалений, ни переименований); blob-идентификаторы и sha256 каждого из шести файлов сняты дважды — из дерева коммита и из рабочего файла — и совпали; рабочее дерево совпадает с коммитом, ни один из шести файлов не переписан после 21:53:50 +0200, то есть проверяющий 927 и проверяющий 930 видели одни и те же байты. Проверяющий заново прогнал 46 почтовых регрессий (46 passed, 0,69 с, журнал `evidence/pytest_gmail_client.log`) и поставил собственную восьмитактовую пробу на закоммиченном опросчике: шесть тиков без кандидатов подряд дают «нужно внимание» с одним неразобранным письмом и никогда не «писем нет»; канал тревоги дважды отказал и на третий раз прошёл, квитанция ровно одна, дальше тишина; модель поднималась 1,0,0,0,0,0,0,1 раз — упавшее письмо предлагалось трижды и ни разу её не подняло, а новое письмо подняло, что подтверждается и двумя независимыми замками в коде. Цитата в четырёх настоящих ответах воспроизвела таблицу 927 по всем пяти строкам (29 абзацев против 15, 4 вложенные цитаты против 0, 4 торчащих знака против 119, ноль внесённой разметки против 3, ноль потерянных строк против 3), двухуровневый источник даёт сбалансированную вложенную цитату, враждебный ввод экранируется целиком. Задержка пробуждения перемерена по боевому журналу, открытому только на чтение: 9 писем из 34 позже пяти минут, худшее 526,5 с — совпало с 927. Единственный GAP непреграждающий и вне задачи: боевой путь тревоги целиком сегодня не срабатывал (в боевом ящике ноль неразобранных писем, DNS в среде проверки недоступен), повторы и квитанция доказаны на подменённом канале. 918 сейчас живёт (Codex, pid 3142070, живость подтверждена процессом, `verification.md` обновлён минуту назад) и делает ровно то, что осталось: сверить одобрение, перепрогнать тесты и закрыть строку `claude_cross_review`. Ничего нового не запущено по одной физической причине: все восемь свободных задач (928, 861, 854, 839, 836, 803, 754, 746) пишут в `/opt/projects/companion-agent`, где живёт этот ребёнок. Формальное закрытие 921 по той же причине откладывается. Бюджет Codex — израсходовано 15%, сброс 2026-08-14.
- 2026-08-07 20:4x UTC — **Проверенный код почтового пути лёг в репозиторий, и одобрение теперь привязывается к самому коммиту, а не к словам о нём.** Codex-продолжение 918 сделало коммит `7666af1` «Keep unresolved mail visible and preserve quotes» в ветке `task/918-proactive-product-owner`; наблюдаемый состав — ровно шесть путей кандидата (`SKILL.md`, `references/runtime.md`, `gmail_client.py`, `mail_product_owner.py`, `tests/test_mail_product_owner.py`, `tests/test_send_email.py`, +233/−28), чужие правки 821 и 856 в него не попали, отслеживаемых грязных файлов в дереве ноль. Прежнее одобрение 927 относилось к рабочему дереву, а не к коммиту, поэтому отсюда запущена отдельная строго read-only проверка 930 на Claude (pid 3152647, живость подтверждена процессом): сверить полный SHA и SHA каждого из шести файлов, убедиться, что байты и поведение те же, самостоятельно прогнать 46 почтовых регрессий и проверить отсутствие автоматического повторного проигрывания; вердикт — одна строка в `findings.md`. На момент записи она идёт: журнал прогона `evidence/pytest_gmail_client.log` уже есть, `findings.md` ещё нет, `status.json` — `state: running`. Пара автор—проверяющий соблюдена: писал Codex, проверяет Claude. Ничего нового не запущено: рабочее дерево companion-agent занято сразу двумя живыми детьми (918 и 930), и восемь свободных задач очереди (928, 861, 854, 839, 836, 803, 754, 746) ждут освобождения дерева, а не решения. Бюджет Codex — израсходовано 15%, сброс 2026-08-14.
- 2026-08-07 20:2x UTC — **Встречная проверка почтового пути состоялась и одобрила правки; осталось положить проверенный код в репозиторий.** Перезапущенная отсюда 927 (Claude, предмет неприкосновенен) закончилась сама: `status.json` несёт `state: completed` и `exit_code: 0`, `findings.md` — ровно одну строку `Verdict: approved`, время 20:11 UTC. Проверяющий не поверил на слово ни одной находке автора: заново прогнал 46 почтовых регрессий, сам воспроизвёл пробу упавшего письма на копии боевого журнала с дважды отказывающим каналом тревоги (пометка «нужно внимание» на каждом тике, тревога повторяется до квитанции, модель при повторном предложении не поднимается), сам пересобрал исходник четырёх настоящих ответов Gmail и прогнал через старый и новый путь (новый — 29 абзацев, 4 вложенные цитаты, ноль внесённых знаков разметки, ноль потерянных строк; старый — 15 абзацев, 119 торчащих `&gt;`), сам перемерил задержку пробуждения на живом журнале (9 писем из 34 позже пяти минут, худшее 526,5 с — совпало с исходным измерением 876) и отдельно проверил, что предмет проверки за время прогона не изменился. Пять непреграждающих находок записаны. Строку `claude_cross_review: BLOCKED` в 918 закрывает продолжение, запущенное сейчас на Codex (pid 3142070, собственная системная область, живость подтверждена процессом): закоммитить **только** шесть файлов кандидата (рядом в дереве лежат чужие правки 821 и 856), заменить BLOCKED на PASS со ссылкой на наблюдаемые признаки 927, развести пять находок ревью на «закрыто здесь» и «вынесено задачей», и лишь потом закрыть задачу. **Табло: пробел прошлой проверки закрыт, правки 916 приняты** — 924 открыла живую страницу настоящим Chromium в четырёх состояниях (1440×900 и 390×844, карточка закрыта и открыта) и получила везде три нуля: ни вложенных прокруток, ни обрезанного текста, ни элементов за границей окна; контрольные суммы трёх защищённых файлов продукта не изменились. Первый запуск браузера падал из-за мягкого ограничения адресного пространства в 4 ГиБ, снятого без изменения жёсткого предела. Формальное закрытие 921 этой записью не делается: рабочее дерево companion-agent сейчас занято продолжением 918, а два пишущих ребёнка в одном дереве уже стоили нам приёмки. Бюджет Codex — израсходовано 15%, сброс 2026-08-14.
- 2026-08-07 20:0x UTC — **Правки почтового пути собраны и все живые проверки прошли; «проверяющий недоступен» оказался не нехваткой Claude, а сломанным запуском, и проверка перезапущена.** 918 (Codex) закрыла все три провала ревью 876 и сама себя оставила открытой: восемь проверок из девяти PASS — упавший разбор письма больше не выдаётся за пустой ящик (одно упавшее письмо, тревога, три последующих тика с пометкой «нужно внимание», сообщение в читаемый канал 18664, ноль повторных проигрываний), на четырёх настоящих ответах Gmail сохранены 115 строк цитаты в 29 абзацах без единого внесённого знака разметки и без торчащих префиксов, обещание пяти минут заменено честным «ближайший свободный пятиминутный тик» с прямо названной ценой отсутствия жёсткого срока, живой пустой опрос боевого ящика не поднял модель (34 вызова до и после, тики 814→815), регрессии почты 46 из 46, здоровье репозитория чисто по 821 каталогу. Девятая строка — `claude_cross_review: BLOCKED`. Её причина проверена наблюдением, а не принята со слов: обе прошлые попытки шли из песочницы самого ребёнка (`supervision_boundary: process_session`, «host systemd manager is not available in this PID namespace», pid 9 и 10, пустой лог, ноль артефактов), то есть проверяющего никто не запускал по-настоящему. Claude из моего окружения отвечает. Поэтому 927 перезапущена отсюда на Claude с полным доступом при неприкосновенном предмете (pid 3133940, собственная системная область, живость подтверждена процессом и попаданием в `live_runs`), а шесть правленых файлов намеренно оставлены незакоммиченными, чтобы проверяющий видел ровно тот кандидат, который проверяет. Бюджет Codex — израсходовано 15%, сброс 2026-08-14. Семь свободных задач треда не запущены по одной физической причине: все они пишут в `/opt/projects/companion-agent`, где сейчас идёт эта проверка и лежит непринятый кандидат 918.
- 2026-08-07 19:4x UTC — **Защита почты от подделки отправителя принята встречной проверкой; проверка табло уперлась не в дефект, а в невозможность запустить браузер, и досняется отдельно.** 919 (Claude, только чтение) независимо перепроверила коммит `2c5ec0b` Codex и вынесла один вердикт `approved`: 63-октетный образец теперь отбивается, парная прогонка 71 680 случаев против прежнего кода не находит ни одного заголовка, который старый код отвергал, а новый принимает; независимый перебор принимает ноль физически возможных подделок против 2 000 до правки; 35 из 35 настоящих сохранённых писем принимаются, регрессии 44 из 44, предмет за время ревью не менялся (sha256 обоих файлов совпал до и после). Один пробел назван вслух самим ревью: живой пустой опрос ящика заново не проводился, потому что 919 запрещено трогать состояние Gmail, — он остаётся свидетельством 909. По табло 921 (Codex) закрыла собственными пробами все три поведенческих замечания — вопрос, отвеченный письмом более ранним, чем сам вопрос, больше не считается закрытым, настоящий поздний ответ его закрывает, все 117 исходящих читаются с диска без пропусков, в области «сделано, но не доставлено» 52 строки и все со статусом «завершено», отменённая 669 туда не попадает, 265 регрессий зелёные и семь из них падают ровно на откате проверяемых правок, — но вернула `rework` из-за единственного несделанного измерения: Chromium в её read-only песочнице не поднимается (падает с кодом 133 до подключения). Дефекта вёрстки не найдено, не хватает замера. Поэтому заведена и запущена 924 на Codex с полным доступом к песочнице при неприкосновенном предмете (pid 3111446, живость подтверждена попаданием в `live_runs`) — доснять четыре состояния настоящим браузером. Освободившееся дерево `/opt/projects/companion-agent` сразу занято 918 на Codex (pid 3112092, живость подтверждена там же): правки по трём провалам почтового пути — упавший разбор письма, схлопнутая html-цитата, невыполненное обещание пяти минут. Бюджет Codex перед запусками — израсходовано 15%, сброс 2026-08-14.
- 2026-08-07 19:2x UTC — **Правка почтовой подлинности собрана и уже проверяется другим семейством; правки табло отправлены на положенную им встречную проверку.** 909 (Codex) закрыла незакавыченный выход в разборе отметки подлинности: второй clause `dkim`, `spf` или `dmarc` теперь отбивается как дубль метода независимо от того, спорит он с первым или повторяет его, а обратная косая черта считается экранированием только внутри кавычек и комментария. Свидетельства снял сам исполнитель: точный образец на 63 октета с подписанным доменом атакующего отвергнут, 35 из 35 сохранённых настоящих писем по-прежнему принимаются, полный набор почтовых регрессий 44 из 44, живой пустой опрос ящика модель не поднял (счётчик вызовов остался 34), repo-health чист; кандидат лежит коммитом `2c5ec0b`. Приёмки нет: 909 сама держит себя открытой до независимой проверки, и та уже идёт — 919 на Claude, только чтение, pid 3086723. Отдельно запущена 921 на Codex (только чтение, pid 3092958, живость подтверждена попаданием в `live_runs`): она перепроверяет правки табло 916, сделанные Claude, — вопрос пользователя больше не считается отвеченным письмом, отправленным раньше самого вопроса, и отменённая работа больше не числится сделанной и недоставленной; правки намеренно не закоммичены в `/opt/projects/product-owner`, чтобы проверяющий видел ровно то состояние, которое проверяет. Бюджет Codex перед запуском — израсходовано 14%, сброс 2026-08-14. Не запущено по наблюдаемой причине: 918 и 854 требуют записи в `/opt/projects/companion-agent`, где живут 909 и её проверяющий; 839 требует записи в `/opt/projects/product-owner`, который сейчас читает 921.
- 2026-08-07 19:0x UTC — **Проверка почтового пути продакта закончилась: одиннадцать обещаний подтверждены наблюдением, три провалены, правки заведены и очередь пошла.** 876 (Claude, только чтение) проверяла работу Codex по 727 и 760 не прозой, а живым ящиком: 214 писем, 50 разобранных отправленных, 35 входящих, промышленный счётчик поллера, побайтная сверка зеркала с Gmail. Подтверждено: расписание живо (792 тика, ни одной ошибки), пустой ящик не поднимает модель (760 пустых тиков — ноль вызовов), входящее письмо и вложения совпадают побайтно, контроль отправителя отбивает семь подмен настоящего заголовка, ответ встаёт в тред Gmail, plain-часть чиста от разметки, кириллическая тема больше не теряет пробел, одно письмо не будит агента дважды. Хвост 760 закрыт наблюдением, а не самоотправкой: абзац в 900 символов лежит настоящим письмом `19fd5ad393c06893` одной логической строкой, а сегодняшняя переписка везёт 926–1219 символов, узкую колонку рисует клиент получателя. Три провала: упавший разбор письма делает его неотвеченным навсегда, а поллер на следующем предъявлении печатает «писем нет» (воспроизведено настоящим кодом на копии промышленного ledger; тревога требует трёх подряд упавших тиков при 96% пустых и уходит в syslog, которого никто не читает); html-цитата переписки схлопывается в один абзац с торчащими знаками «больше» и переписывает разметку исходного письма (четыре из семи последних ответов с цитатой); обещание пятиминутного пробуждения не выполняется — девять из 34 писем подняты позже, худшее 8 минут 47 секунд. Правки заведены задачей 918 на Codex как автора 727 и 760, повторное ревью уйдёт строго на Claude. Запущена не она, а 909 (Codex, полный доступ, pid 3084181, живость подтверждена попаданием в `live_runs`) — она закрывает незакавыченный выход на 63 октетах в разборе отметки подлинности и стояла в очереди с прошлого пробуждения; 918 ждёт освобождения того же рабочего дерева `/opt/projects/companion-agent`, где 909 сейчас и правит соседний код той же почты. Бюджет Codex проверен до запуска: израсходовано 13%, сброс 2026-08-14.
- 2026-08-07 18:4x UTC — **Встречная проверка табло закончилась и вернула «доработать»; правки запущены той же минутой.** 826 (Codex, только чтение) проверила работу Claude по 817 не прозой, а собственными пробами: 171 регрессия на точном коммите `8a1d194`, 258 на текущем состоянии во временной копии, живая страница на 1440×900 и 390×844 настоящим Chromium — ноль вложенных прокруток, ноль обрезанных текстов, ноль элементов за горизонтальной границей, фон светлый. Два дефекта. Главный: ответом на вопрос считается любое письмо пользователя того же календарного дня, поэтому письмо, отправленное раньше самого вопроса, прячет настоящий неотвеченный долг — на живой переписке это уже случилось, вопросы ушли письмом `19fd7e7ea2c3f7fb` в 16:28:48 UTC, а «ответом» записано письмо `19fd75ff10dd7605` от 14:00:02 UTC, на 2 часа 28 минут раньше, и текст его про забытый документ. Второй: в область «Сделано, но не доставлено» попадает любой конечный статус, поэтому отменённая 669 (перекрыта задачей 722) числится сделанной и недоставленной — сейчас там 47 строк, 46 завершённых и одна отменённая. Правки заведены 916 и запущены на Claude (автор 817), предмет — `/opt/projects/product-owner/scripts/process_map_state.py`, повторное ревью уйдёт строго на Codex; живость подтверждена по pid 3068416 и попаданием в `live_runs`, а не выводом команды запуска. 876 (Claude, почтовый путь 727 и 760) продолжает идти и уже написала в `findings.md` вердикт «доработать» при одиннадцати подтверждённых наблюдением обещаниях — принимать её нечего, пока ребёнок жив. Не запущены по наблюдаемой причине: 909 и 854 требуют записи в `/opt/projects/companion-agent`, где час живёт пишущий ребёнок 904 треда deep research (pid 2993507, полный доступ, сейчас на обязательном ревью политики), и коммит `3ff07dc` в событиях пробуждения — тоже его работа, к процессному контуру отношения не имеющая.
- 2026-08-07 18:2x UTC — **Простой при непустой очереди снят: запущены две встречные проверки, обе на чтение, обе к другому семейству.** 826 (Codex) перепроверяет работу Claude по табло 817 — не прячет ли область «Ждёт решения пользователя» настоящий вопрос человеку, верен ли признак «сделано, но не доставлено» на живом состоянии, держатся ли измерения вида при независимом перемере браузером; условие запуска, стоявшее на бюджете Codex (80% на 6 августа), снято наблюдением — сейчас израсходовано 12%, сброс 2026-08-14. 876 (Claude) перепроверяет работу Codex по почте продакта — 727 (опрос ящика, приём письма пользователя, вложения) и 760 (ответ в тот же тред Gmail, цитата, text/plain и text/html из одного источника), по настоящим отправленным и входящим письмам из зеркала на диске, а не по прозе артефактов. Живость обеих подтверждена по pid (3038574 и 3039281) и попаданием в `live_runs`, а не выводом команды запуска. Не запущены намеренно и по наблюдаемой причине: 909 (закрыть незакавыченный выход на 63 октетах в разборе отметки подлинности) и 854 (машинная проверка семейства проверяющего) обе требуют записи в `/opt/projects/companion-agent`, а там 42 минуты живёт пишущий ребёнок 904 треда deep research (pid 2993507); 839 правит `scripts/process_map_state.py` в `/opt/projects/product-owner` — то самое дерево, которое сейчас читает ревьювер 826.
- 2026-08-07 15:0x UTC — **849 прошла и закрыла все четыре дефекта доставки, но осталась `blocked` на двух шагах, которые ей физически не дались, и её работа лежит незакоммиченной.** По `verification.md`: шесть проверок OK, каждая — не тест, а внесённая поломка. Упавшая запись квитанции после удавшейся отправки больше не роняет принятый прогон: `consume()` вернул `True`, отправка была одна, на диске осталась заявка `document_delivery_started`, следующая попытка не отправила ничего и записала `document_delivery_unresolved`, третья промолчала. Упавшая запись самой заявки означает, что отправки не было вовсе, и это видно отказом. Отсутствие назначенного канала теперь отвергается на входе (`main()` без `--destination` выходит с кодом 2), а недостижимое обещание квитанции убрано из трёх документов. Нечитаемый каталог и нехешируемый файл превратились из тишины в поимённый отказ. Набор из двух документов, где один отказан, больше не показывается доставленным — на стороне отправителя. Два шага не сделаны: наблюдатель табло живёт в `/opt/projects/product-owner`, который этому ребёнку был доступен только на чтение (патч подготовлен и проверен вне дерева — `git apply --check` чисто, 224 теста продакта проходят, новый тест на неполном наборе падает на непропатченном модуле), и живой отправки в Telegram никто не видел: у ребёнка нет маршрута до `api.telegram.org` мимо прокси. Оба добираются одной задачей с полным доступом — но не сейчас: `/opt/projects/product-owner` занят живым прогоном 871 (pid 2819896), который правит те же `scripts/process_map_*`, куда ложится патч. Отдельно записан долг: восемь изменённых отслеживаемых файлов и два новых (`pipeline_delivery.py`, `test_pipeline_delivery.py`) — это результат 849, и он не закоммичен; следующая задача в `companion-agent` обязана зафиксировать его первым шагом, иначе смешает со своим.
- 2026-08-07 13:2x UTC — **849 по-прежнему уступлена, и теперь тому есть второе, физическое основание.** Первое то же: интерактивный продакт (pid 2581045) живёт с 09:22 и активен — за последние два часа он провёл в том же рабочем дереве цепочку задач 865–869 (последний каталог тронут 15:21 местного, за шесть минут до пробуждения) и вернулся к 856. Прогона в эту секунду нет (`live_runs: []`, ни одного раннера в `ps`), но дерево у него в обороте, а 849 завёл он же. Второе основание новое: в `companion-agent` восемь незакоммиченных изменённых отслеживаемых файлов, из них четыре — ровно те файлы раннера, которые правит 849 (`task_runner.py`, `dev_pipeline_adapter.py`, `pipeline_notify.py`, `test_task_runner.py`, плюс `AGENTS.md` и три дока). Ребёнок в режиме записи смешал бы свои правки с чужой незавершённой работой, и гейт чистоты дерева соврал бы обоим. Событие пробуждения (коммит `e89b1bb`, A100 runbook) к процессному контуру отношения не имеет — это работа треда deep research в общем репозитории. Условие следующего запуска не изменилось и уточнено: 849 стартует, когда интерактивного продакта в этом дереве нет и отслеживаемые изменения зафиксированы или откачены.
- 2026-08-07 10:4x — **Условие запуска 849 снято, но запуск уступлен: в том же рабочем дереве работает второй продакт.** 852 закрыта, `companion-agent` по наблюдению свободен от детей (`live_runs: []`, ни одного процесса раннера в `ps`), то есть правки по доставке документа готовы стартовать. Не запущены фоновым пробуждением намеренно: интерактивный продакт (pid 2581045, живёт с 09:22) прямо сейчас по просьбе пользователя запускает накопившиеся задачи в этом же дереве — 849 и 850 завёл он же в 10:34 и сам записал им очерёдность. Двух детей в одном рабочем дереве быть не должно, и чужую планируемую задачу за владельца не запускают. Если к следующему пробуждению 849 так и не стартует и второго продакта не будет — запускает фон.
- 2026-08-07 08:4x — **Правило, из-за которого правки ждали слова пользователя, было нашим собственным, и оно убрано.** Пользователь ответил на вердикт письмом `19fdb553205d9bf0`: «фоновое пробуждение не запускает работу в режиме записи само — не понятно, поясни, и странно, что тут надо что-то подтверждать». Источник найден: одна строка в подставляемой самому себе инструкции пробуждения, `scripts/thread_tick.py`, «Не запускай новых детей в write-режиме без явного разрешения пользователя», — она пережила урок 2026-08-06, снявший то же условие в `AGENTS.md`. Строка заменена на настоящее ограничение: запуск правок по нашему коду в нашем репозитории разрешения не требует, единственный ограничитель — занятое рабочее дерево. Ответ отправлен письмом `19fdb5d5d3f2c408` с пояснением обоих терминов. 849 и 850 запускаются без вопроса и по очереди, потому что обе правят одни и те же файлы раннера: условия запуска записаны машиночитаемо в `status_detail` (849 — `starts_after=852 worktree=/opt/projects/companion-agent`, 850 — `starts_after=849`), 849 стартует, как только идущая в том же дереве 852 освободит его. Бюджет Codex после обновления подписки — 2% израсходовано, повторные ревью обеих правок пойдут туда.
- 2026-08-07 08:0x — **Обе встречные проверки закончились, обе с вердиктом «доработать», и обе подтвердили главное наблюдением.** 843 (Codex, ревью работы Claude) впервые увидела настоящий документ в настоящем канале — прогон отдал файл, квитанция, отправленное и скачанное совпали побайтно и по sha256, — и нашла четыре дефекта надёжности доставки. 827 (Codex, ревью работы Claude) воспроизвела на живом Cursor все четыре заявленных факта, включая измерение потолка памяти и границы прав, и нашла три дефекта привязки и очерёдности проверок. Правки заведены по правилу «анализ → план → правки → повторное ревью»: 849 по доставке документа, 850 по владельцу на Cursor, обе на Claude как автора, повторная проверка снова уйдёт на Codex. Запуск не сделан: фоновое пробуждение не запускает детей в режиме записи без слова пользователя. Задача 827 остаётся `blocked` до правок и свежего одобряющего ревью политики.
- 2026-08-07 07:2x — **Две встречные проверки dev-pipeline, стоявшие на бюджете Codex, запущены.** Окно подписки открылось заново и проверено наблюдением (0% израсходовано, сброс 2026-08-14). 843 проверяет 834 — правда ли, что прогон теперь сам доставляет свой документ человеку, включая отказ вместо тишины и неотправку одного и того же файла дважды. 827 проверяет 820 — правда ли, что Cursor стал полноценным третьим владельцем. Обе на Codex, предмет не правят, живость подтверждена по pid.
- 2026-08-07 — **прогон научился отдавать человеку сам документ, а не только сообщать о своей жизни, и всё, кроме последнего шага до Telegram, снято живьём** (834, Claude, `dev-pipeline`). Отправная точка измерена по всему репозиторию: квитанций доставки было 242, и все до одной — про жизнь прогона (`attempt_started`, `attempt_completed`, `attempt_completed_rejected`, `attempt_failed`); ни одна не говорила, что документ дошёл до человека. То есть за всё время линия ни разу не доставила результат сама — всё, что пользователь получал, отправлял руками продакт. По `verification.md`: настоящий `dev_pipeline_adapter.main()` на настоящем потоке событий ядра доставил `deliverables/report.html` с подписью по названию задачи и записал квитанцию `document_delivered` (`message_id 18999`, sha256 `2285482e…c89b25`), заменена была только граница aiogram; отказ проверен на настоящей сети — при недоступном Telegram появляется ровно одна квитанция `document_delivery_refused` с причиной в одну строку и порядком пушей «сначала завершение, потом отказ», то есть тишины нет; повторная отправка не происходит, и признак — содержимое, а не имя: переименованный байт-в-байт файл не шлётся, переписанный шлётся заново. Наблюдатель табло проверен неизменённым и на трёх состояниях читает квитанцию правильно. Единственный GAP назван автором честно: настоящего сообщения в назначенном канале не видел никто — у ребёнка не было маршрута до `api.telegram.org` (DNS не резолвит, прокси даёт 403 на трёх маршрутах). Продакт проверил маршрут из своего контекста: с хоста `api.telegram.org` отвечает 302, значит GAP закрывается первым же обычным прогоном с документом, отдельной работы под это не нужно. Кросс-ревью не запущено намеренно: писал Claude, значит проверяет Codex, а `codex_budget.py` даёт 85% израсходованного окна и ненулевой код (сброс 2026-08-10) — задача 843 заведена `planned` с названным условием запуска.
- 2026-08-06 — **Cursor стал третьим владельцем dev-pipeline, и это доказано живым прогоном, а не тестами** (820, Claude, `dev-pipeline`). Проверка — в `verification.md` задачи: одноразовая 825 прошла под `--runner agent --workflow dev-pipeline` до `completed` с вердиктом `approved`, а её прерванный прогон был именно продолжен, а не начат заново: в журнале одна попытка с `runtime: cursor` и вторая run с `run_operation=native_session_resume`, и возобновлённый чат ответил по памяти первой сессии, не перечитывая файлы. Отдельно закрыта ловушка самого инструмента: голый `cursor-agent --resume` на неизвестный идентификатор выходит с кодом 0 и **создаёт** такой чат — через нашу границу это отказ `native_resume_unavailable` (`condition: not_found`) без единого `process_started`. Измерен и потолок памяти: рантайм резервирует адресное пространство до разбора аргументов и стартует только с 26 ГиБ виртуальной памяти, поэтому `RLIMIT_AS` для него не выражает ограничение вовсе — лаунчер отказывает до запуска и печатает измеренное число. Регрессии: 155 в публичном ядре (10 новых на Cursor), 276 в companion, 130 в публичном шаблоне против свежего пина. Публичное ядро выпущено (`9301f14`). Кросс-ревью не запущено намеренно: писал Claude, значит проверяет Codex, а `codex_budget.py` даёт 80% израсходованного окна и ненулевой код (сброс 2026-08-10) — задача 827 заведена `planned` с условием запуска по бюджету.
- 2026-08-06 — **Публичный шаблон с этой поддержкой лежит незапубликованным, и держит его наш собственный сторож.** `task-agent` закоммичен локально (`f8412d0`, перепин на новое ядро), `origin/main` стоит на `c08a2ac`; `check_pre_push.py` отказывает из-за `refs/backup/pre-rewrite-2026-08-06` — страховки, оставленной переписыванием публичной истории (795/804) и указывающей на `572bbfa`. Пользователю выбор не выносится: удаление этой ветки — не единственный ход. История выгружается bundle'ом в приватное место вне публикующего клона, восстановимость доказывается клонированием из него, и только потом ветка убирается из клона — тогда и сторож прав, и единственная копия цела. Поставлено задачей 828.
- 2026-08-06 — 759 закончена как `blocked` с готовой правкой: машинный ключ отказа в завершении переименован в нейтральный `owner_ended_after_incomplete_published_progress`, словарь ветки теперь живёт в одном месте (`pipeline_notify`), старое значение читается как та же ветка и ни одной поверхностью не выпускается; коммит `5034e01`, 414 тестов, живая проба обоих окончаний через настоящий финализатор и уведомитель. Не выполнено было только повторное кросс-ревью: дочерний Codex не доходит до модели изнутри прогона владельца (прокси 403 на `chatgpt.com`, плюс `/root/.codex` только на чтение) — это среда, а не правка. Продакт проверил сеть из своего контекста (405, то есть доступ есть) и перезапустил ревью 774 оттуда штатным входом (Codex, standard, read-only, `--require-review-verdict`, предмет — `5034e01`); запуск подтверждён наблюдением: ребёнок жив и записал первую строку `trace.md`, а не только вывод команды запуска.
- 2026-08-06 — **два «решения пользователя» по 711 закрыты и больше не ждут: проверено по коду, а не по прозе.** Оба выполнены самой работой в коммите `b45ab49` («Both user decisions are followed literally»): исторические записи решений дополняются в полях Status и Consequences, а не переписываются, и `--runner agent` остался как путь совместимости с Cursor Agent для обычных задач — он и сегодня в `CLI_RUNNERS` в `task_runner.py`. Выносить их пользователю нечего; строка ниже остаётся как след, а не как ожидающий выбор. Проверка сделана по правилу «раньше второго варианта проверяется, не сделано ли это уже» (AGENTS.md продакта).
- 2026-08-04 — 711: анализ 2 HIGH, 6 MEDIUM и 6 LOW и план записаны до правок. Работа ждала двух решений пользователя (закрыты 2026-08-06, см. строку выше): можно ли обновлять current-state части historical decision records и нужен ли внешний `--runner agent`; исходники пока не менялись.
- 2026-08-04 — 711: прогон завершён, процесс не жив, задача в терминальном `blocked`; путь «получить ревью изменений» остаётся деградировавшим до ответа на два вопроса, правок и повторного ревью.
- 2026-08-04 — 711: правки по всем принятым находкам сделаны, регрессии зелёные (266 тестов); блокируют два внешних гейта, а не работа. Продакт проверил оба сам: `ENOTIMP` в Claude был транзиентным (связь есть), `repo-health` FAIL реален, но все хиты — старые снапшоты задач 255/586/587/683, к 711 отношения не имеют. Запущена 716 — повторное read-only ревью Claude, предмет ограничен 12 файлами 711 без параллельных правок moex.
- 2026-08-04 — 716: первый запуск умер через 35 секунд, ничего не написав (`Execution error`, watcher остановлен по SIGTERM вместе с родительской сессией). Продакт воспроизвёл ровно ту же команду ребёнка вручную — она отрабатывает штатно, значит причина в обрыве родителя, а не в Claude и не в задаче. Перезапущено отдельной сессией (`setsid`).
- 2026-08-04 — 716: **второй запуск умер так же**. Наблюдение: `.runner/runner.log`
  содержит два `Execution error`, `runner.json` — `watcher_stop_reason=signal_sigterm`,
  `watcher_stopped_at 11:36:03` против `child_started_at 11:35:01`, то есть 62 секунды;
  `status.json` до сихпор врёт `running` при отсутствии процессов claude. Отсюда:
  прошлый вывод «причина в обрыве родителя» **не подтверждён** — отвязка сессии не
  помогла, гибнет одинаково при двух разных родителях. Ни одного вердикта Claude по
  правкам 711 нет; путь «получить ревью изменений» деградировал не по содержанию, а
  потому что ревьювер не доживает до первой минуты.

- 2026-08-04 — 716: причина найдена, и она не сетевая. Третий запуск (отвязанный `setsid`)
  прожил 5,5 минут и вышел с кодом 0, но не написал ни одного артефакта:
  `outcome=rejected_completion_contract`, каталог задачи пуст. По коду
  `task_runner.py:629-633` режим `--sandbox-mode read-only` выдаёт ребёнку-Claude
  только `--tools Read,WebFetch,WebSearch` — без `Write`, `Edit` и `Bash`. То есть
  read-only ревьювер физически не может ни записать `findings.md`, ни выставить
  frontmatter `completed`, которого требует контракт завершения, ни проверить находки
  поиском. Путь «получить ревью изменений» на Claude сломан структурно, а не транзиентно:
  прошлые выводы про `ENOTIMP` и про обрыв родителя объясняли только первые два запуска.

- 2026-08-04 — 716: ревью **состоялось и вердикт approved**. Уточнение к прошлой записи:
  ребёнок не написал файлов, но полный разбор — границы прохода, находки M1/L2/L3/L4,
  ответы на все шесть вопросов ревью, вердикт — доставлен в финальном ответе и лежит в
  `.runner/runner.log`. HIGH нет, обязательных к исправлению пунктов нет; оба решения
  пользователя (записи решений, `--runner agent`) соблюдены буквально. Продакт перенёс
  текст дословно в `tasks/716-.../findings.md`, закрыл 716 как `completed` и записал в
  `verification.md` задачи 711, что гейт `independent_claude_review` закрыт. Правило
  кросс-ревью выполнено: работу Codex проверил Claude. Единственный оставшийся блокер
  711 — `repo_health` FAIL на чужих старых снапшотах, решение по нему за пользователем.
  Структурный дефект пути остаётся: read-only ревьювер физически не может записать свой
  findings.md, поэтому результат каждый раз придётся снимать вручную.

- 2026-08-05 — **тот же дефект найден на второй оси: Codex.** Кросс-ревью 723
  (задача 728) прошло на Codex в `read-only` и выдало вердикт `rework` с одной
  находкой HIGH — но записать его не смогло: у Codex-ребёнка в `read-only` нет
  **ни одного** записываемого корня, ни каталога задачи, ни `/tmp`. Отсюда же
  провалилась обязательная live-верификация: `pytest` не смог начать collection
  без временного каталога. Задача 723 починила эту границу только для Claude.
  Пока это не закрыто, путь «получить ревью изменений» остаётся деградировавшим
  на Codex ровно так же, как был на Claude: вердикт есть, артефакта нет, продакт
  снимает текст из `runner.log` руками. Поставлено работой (730).

- 2026-08-05 — 734 (кросс-ревью Codex по правкам Claude в 730, коммит f9f8be5):
  **два запуска подряд не дожили до вердикта.** Наблюдаемо по артефактам:
  `trace.md` показывает pid 1671660 (23:18 UTC) и pid 1673585 (23:37 UTC), оба
  процесса не живы; последняя запись ребёнка — 23:38:08, «began context discovery»,
  дальше ничего; `findings.md` в каталоге задачи нет, `status.json` до сих пор
  врёт `running`. Вердикта по 730 нет — ни принятого, ни снятого руками, поэтому
  правки 730 остаются непроверенными на ветке `task/708-lightweight-pipeline`.
  Отдельно из артефактов: `task_contract.json` задачи 734 — `gate_status:
  ungated`, «this contract gates on nothing: it cannot refuse a completion», то
  есть даже дошедший до конца прогон не был бы обязан предъявить `findings.md`.
  Третий запуск в том же конверте продакт не ставит: конверт уже дважды дал
  один и тот же исход.

- 2026-08-05 — 734: **третий запуск всё-таки состоялся и умер третий раз тем же
  способом.** Наблюдаемо по артефактам: pid 1678266 стартовал 00:38:40 UTC,
  последняя запись ребёнка — 00:38:59 «began context discovery», дальше ничего;
  `runner.json` — `watcher_stop_reason=signal_sigterm`, `watcher_stopped_at
  00:40:56` против `child_started_at 00:38:40`, то есть 2 минуты 16 секунд;
  `exit_code: null`, `outcome=terminated_without_finalization`. `findings.md`
  нет. Три разных pid, три одинаковых тихих смерти на первой минуте работы —
  конверт не транзиентно шумит, он не работает. Бюджет Codex исключён как
  причина: `scripts/codex_budget.py` — израсходовано 34%, код возврата 0.
  Совпадает с уже записанным GAP самой задачи 730
  (`end_to_end_child_run_on_the_real_model`): «реальную модель дочерний процесс
  здесь не достаёт — Proxy connection failed: HTTP CONNECT failed with status
  403 на wss://chatgpt.com/backend-api/codex/responses, у сессии пустой allowlist
  прокси; детач-вотчер лаунчера убивают за две секунды». То есть ревьювер не
  доживает до вердикта по хостовой причине, а не по содержанию работы.
  Вердикта по 730 по-прежнему нет, правки 730 остаются непроверенными на ветке
  `task/708-lightweight-pipeline`. Четвёртый запуск в том же конверте продакт не
  ставит и выносит вопрос пользователю: нужен доступ к хосту (allowlist прокси
  для codex и защита процесса ребёнка от SIGTERM), выдать его может только он.

- 2026-08-05 — **одна вещь на этом провале сработала как надо**: в отличие от
  первых двух запусков, `status.json` больше не врёт `running`. Прогон
  реконсилирован в терминальный `failed` с честной формулировкой «Child run
  ended without a recorded exit code» и записью в `trace.md` «neither the child
  nor its watcher is running and no outcome was ever recorded». Это ровно то
  поведение, которое ставила задача 723 и уточняла 730: гейт не утверждает
  больше наблюдённого. Наблюдено на брошенном прогоне, а не на тесте.

- 2026-08-05 — 734: продакт всё-таки поставил четвёртый запуск в том же
  конверте — как проверку, а не как надежду на новый коммит: `02b3e45` только
  выносит `never` в константу `CODEX_APPROVAL_MODE` и добавляет утверждение в
  тест, поведение ребёнка он не меняет. Наблюдаемо: pid 1728074 жив шестую
  минуту, `progress.json` обновлён в 10:02 UTC («Reconciling task 730, review
  728, and task 723 context»), `status.json` — `running`, `watcher_stop_reason`
  пуст, тогда как все три прошлых прогона были убиты SIGTERM на второй минуте.
  Значит гибель зависит от родителя запуска, а не от задачи и не от Codex.
  Вердикта по 730 всё ещё нет; путь «получить ревью изменений» остаётся
  сломанным на Codex, пока этот прогон не запишет `findings.md` сам.

- 2026-08-05 — 734: **четвёртый прогон тоже убит, но версия про прокси отпала.**
  Наблюдаемо по артефактам: `runner.json` — `child_started_at 10:00:48`,
  `watcher_stopped_at 10:07:16`, `watcher_stop_reason=signal_sigterm`, то есть
  6 минут 28 секунд против двух минут у прошлых. За это время ребёнок реально
  работал: в каталоге задачи лежат `own-write-probe.tmp` («OK»),
  `subject-write.err` и `tmp-write.err` (оба — `Read-only file system`), два
  временных worktree по коммиту f9f8be5 и `pytest-of-root/`. Значит модель он
  достаёт, и граница записи из 730 работает ровно как заявлена: свой каталог
  пишется, предмет ревью и `/tmp` — нет. Причина смерти одна и та же на всех
  четырёх запусках — SIGTERM вотчеру, и приходит он не по содержанию работы.
  Контрпример в том же часе: прогон 737 на Codex с `danger-full-access`, стартовавший
  из живущей сессии, работает 50-ю минуту без единого SIGTERM. `findings.md` по 730
  нет по-прежнему, `status.json` 734 остался врать `running` — реконсилировать было
  некому, вотчера убили вместе с ребёнком. Пятый запуск в том же конверте продакт
  не ставит: сначала отвязка прогона от родительской сессии, потом ревью.
- 2026-08-05 — 734: **пятый запуск умер быстрее всех, и это закрывает вопрос о
  причине.** Наблюдаемо по `runner.json`: `child_started_at 10:40:44`,
  `watcher_stopped_at 10:41:24`, `watcher_stop_reason=signal_sigterm` — 40 секунд
  против 6 минут 28 секунд у четвёртого. Ребёнок успел записать только
  `progress.json` и строку в `trace.md` («began durable-context lookup»);
  probe-файлы и worktree в каталоге остались от прошлого прогона, новых нет.
  Пять разных pid, пять SIGTERM вотчеру, длительность прыгает от 40 секунд до
  6,5 минут — то есть смерть не зависит ни от содержания ревью, ни от Codex, ни
  от бюджета, а совпадает с концом родительской сессии запуска. Контрпример тот
  же: 737 на Codex из живущей сессии работает без единого SIGTERM. `findings.md`
  по 730 нет пятый раз подряд; правки 730 (коммит f9f8be5) остаются
  непроверенными на ветке `task/708-lightweight-pipeline`. Шестой запуск в том же
  конверте продакт не ставит и выносит вопрос пользователю: нужна отвязка
  дочернего прогона от родительской сессии — это правка в `task_runner.py`, то
  есть write-прогон в companion-agent, а его продакт без разрешения не запускает.
  Отдельно из артефактов: `task_contract.json` задачи 734 по-прежнему
  `gate_status: ungated`, так что даже дошедший до конца прогон не был бы обязан
  предъявить `findings.md` — это второй дефект, независимый от SIGTERM.

- 2026-08-05 — 734 остаётся в `in_progress` при мёртвом pid 1749666: с 12:45 в
  `progress.json` и `trace.md` ни одной новой строки, `findings.md` нет пятый раз
  подряд, вердикта по правкам 730 (коммит `f9f8be5`) по-прежнему нет. Ярлык
  `status.json` = `running` недостоверен — процесса нет; наблюдаемое состояние
  терминальное, реконсиляции ещё не было. Шестой запуск в том же конверте продакт
  не ставит: вопрос об отвязке дочернего прогона от родительской сессии — правка в
  `task_runner.py`, то есть write-прогон в companion-agent, и он ждёт разрешения
  пользователя. Контрпример в ту же сторону усилился: 737 на том же Codex
  завершилась штатно (`exit_code: 0`, семь ревью Claude подряд, последнее APPROVED
  на `379b241`/`a4ca98f`, живые Telegram-прогоны 89224/89229 с проверкой SHA-256
  вложений) — значит гибнет не Codex и не ревью как жанр, а именно отдельно
  запущенный дочерний прогон.

- 2026-08-05 — 737: коммит `a4ca98f` «Document fail-closed Codex delivery» —
  продолжение той же работы, прогон жив. По диффу `docs/task-execution.md`:
  доставка теперь проверяет, что в русском заключении заполнены все обязательные
  разделы, при сбое подготовки пишет аварийное заключение из пяти разделов, а
  если и его записать нельзя — **не отправляет терминальное сообщение вовсе**,
  вместо того чтобы подсунуть `findings.md` или отрапортовать успех без
  вложения. Для пользователя это значит: пустого или технического «заключения»
  в Telegram больше не приходит; приходит либо читаемый русский разбор, либо
  ничего.

- 2026-08-05 — 737 (`/codex` из Telegram: полные права, живая проверка, русское
  заключение): коммит `7e0ec9b` — часть этой работы, прогон жив, 5 шагов из 7.
  По `verification.md`: живой сквозной путь пройден на настоящем аккаунте —
  сообщение 89211 создало задачу 738, ребёнок шёл с `danger-full-access` без
  memory-cap, задача закрыта после релиза раннера, ответ 89214 по-русски и
  вложение 89215 скачаны, SHA-256 совпал с зарегистрированным артефактом;
  независимое ревью Claude — APPROVED, все восемь прошлых находок закрыты.
  Единственный GAP — `companion_full_suite`: полный набор встал на 57-м тесте
  в неродственной async-области и был прерван, изменённые пути зелёные.

- 2026-08-05 — **пользователь разрешил write-прогон в раннере и выбрал
  исполнителя.** Дословно: «Про ревью опять не понял: чинили-чинили, опять
  почему-то Claude лажает. Надо этот тред довести до ума. Можешь сделать через
  Codex: как ни странно, он лучше разбирается в модели Claude по доступам и
  лучше проверяет, что получена действительно работающая схема». Это снимает
  блокировку, которая держала тред с 734: правка `task_runner.py` больше не
  ждёт разрешения.
- 2026-08-05 — **748 запущена: довести путь «получить ревью изменений» до
  вердикта.** Codex, dev-pipeline, `workspace-write`, repo `companion-agent`,
  pid 1805094, старт 16:30:59 UTC. Три дефекта в одной задаче: причина SIGTERM
  устанавливается наблюдением (кандидат — `PR_SET_PDEATHSIG` в
  `_parent_death_signal`, но прошлые версии причины уже дважды оказывались
  неверными, поэтому догадка не принимается); ревьювер обязан уметь записать
  `findings.md` из своего режима доступа минимальными правами, а не через
  `danger-full-access`; контракт ревью-задачи обязан требовать вердикт — у 734
  он был `ungated`. Доказательство — живое кросс-ревью коммита `f9f8be5` (правки
  730), а не тесты. Контракт 748 `gated`: 5 ограничений, 1 гейт ревью, 4
  обязательных наблюдения. Отдельным ограничением записана изоляция от соседа:
  748 не сигналит чужим прогонам, потому что параллельно идёт 747.

## Журнал эффекта
- 2026-08-04 — правки по 14 находкам кросс-ревью впервые прошли независимую проверку: Claude принял их без обязательных исправлений.
- 2026-08-04 — bounded-ревьюверы впервые применены к процессу, а не к коду: два вердикта по цепочке 699–705 (задача 706).

## Решено
- 2026-08-05 — 734: четвёртый запуск умер так же, но дальше всех. Наблюдаемо по
  артефактам: `runner.json` — `watcher_stop_reason=signal_sigterm`,
  `watcher_stopped_at 10:07:16` против `child_started_at 10:00:48`, то есть
  6 минут 28 секунд; `findings.md` нет, вердикта по 730 по-прежнему нет.
  Но каталог задачи не пуст, и оставленное в нём — свидетельство, а не проза:
  `own-write-probe.tmp` = `OK`, `subject-write.err` и `tmp-write.err` — оба
  `Read-only file system` на `skills/task-runner/` и на `/tmp`, плюс два
  git-worktree предмета на коммите f9f8be5 и каталог `pytest-of-root`. То есть
  граница записи, которую ставила 730, наблюдена живьём на настоящем
  Codex-ребёнке, а не на заглушке: свой каталог пишется, предмет ревью и `/tmp`
  — нет, pytest внутри ребёнка стартует. Это закрывает наблюдением ту часть
  GAP `end_to_end_child_run_on_the_real_model`, которая про песочницу; открытым
  остаётся только то, что ребёнок не доживает до вердикта. Продакт поставил
  пятый запуск в том же конверте (бюджет Codex 39%, код возврата 0): первые три
  смерти были на второй минуте до всякой работы, четвёртая — после шести минут
  реальной работы с probe-файлами, значит конверт сдвинулся и проверять его
  стоит наблюдением, а не рассуждением.
- 2026-08-04 — пуши о ходе прогонов пользователь хочет получать. Предложение сократить объём lifecycle-уведомлений снято; продакт не трогает этот путь.

- 2026-08-05 — 748/749: **причина пяти смертей 734 названа по хостовым уликам, и
  первая половина починки проверена живьём.** По `findings.md` 748: убивал не
  раннер и не Codex — служба запуска `product-thread@process.service` объявлена
  `Type=oneshot` без `KillMode`, а умолчание systemd — гасить всю группу
  процессов при конце службы; поэтому `setsid` и не помогал, смена сессии из
  группы не выводит. Совпадение по времени точное: вотчер 734 записал SIGTERM в
  10:41:24, деактивация службы — 10:41:24.1. Сделано две вещи: дочерний прогон
  теперь запускается в отдельной systemd-области с собственной группой, и
  контракт ревью получил обязательный вердикт — без `findings.md` завершение не
  принимается. Вторую вещь продакт проверил не тестом, а живым прогоном через
  обычный вход на одноразовой задаче 749: ребёнок вышел с кодом 0 и намеренно не
  написал `findings.md` — раннер отказал в завершении, поставил `blocked` и
  `outcome=rejected_completion_contract` с точной причиной
  (`live-evidence/review-contract-refusal.json`). То есть `blocked` у 749 — это
  успех проверки, а не сбой. Первую вещь нельзя было проверить изнутри дочерней
  песочницы (нужен доступ к systemd хоста), поэтому продакт запустил её сам с
  хостового треда в 17:02 UTC: раннер отчитался
  `supervision_boundary.mode=systemd_scope`, `durability=independent_cgroup`, и
  сам ребёнок записал свою группу — `/system.slice/companion-task-749-….scope`,
  то есть вне группы родительской службы. Ребёнок ждёт три минуты и пишет
  финальные артефакты уже после конца родителя; выжил он или нет — видно будет по
  его файлам на следующем пробуждении. Права на write-прогон в раннере
  пользователь дал 2026-08-05.

- 2026-08-05 — 753: повторная независимая проверка починки закончена вердиктом
  `approved` по точному коммиту `8141c37`. Правила кросс-ревью соблюдены: правки
  писал Codex (748), проверял отдельный read-only прогон Claude, и это не тот
  прогон, который делал первую проверку. Все пять блокирующих находок первого
  ревью закрыты наблюдением, а не декларацией: адрес доставки исчез из описания
  systemd-области (в журнале у новой области — фраза про задачу, у всех
  доремонтных — полная командная строка); у зависшей заявки на запуск появился
  владелец восстановления (`stop`); действие, которое в прошлый раз испортило
  состояние живого прогона, повторено — `status.json` и `trace.md` побайтово не
  изменились; владелец dev-pipeline теперь читает требование вердикта в своей
  инструкции; описание read-only границы Claude приведено к тому, что реально
  видно в монтированиях. Регрессии на точном кандидате: 406 passed (было 404,
  плюс ровно два новых теста). Осталось восемь мелких пунктов и два замечания —
  ни одно не меняет поведения гейта и не запирает задачу, они в бэклоге.
  Последний шаг — обязательное завершающее ревью кандидата с хоста — уже идёт
  внутри 748 и на момент этой записи живо.

- 2026-08-05 — 758/748: **последний обязательный шаг пройден, и он был не «уже
  живым», а несделанным.** Прошлая запись выше («ревью с хоста уже идёт») не
  подтвердилась артефактом: `reviewer-run.json` всё это время оставался неудачной
  попыткой изнутри задачи — код 1, сессии нет, потому что Codex не смог
  развернуться на read-only файловой системе. Из-за этого 748 простояла в
  ожидании около двух часов, а 758 ушла в `blocked` с прямой просьбой к продакту.
  Продакт выполнил её сам: тот же самый штатный вызов с хоста отработал за ~8
  минут, код 0, отдельная сессия `019fd385`, структурное решение `approved`,
  список находок пустой. Ревьювер перечислил восемь проверенных улик поимённо —
  разборы кода раннера и контракта плюс все четыре обязательных живых
  наблюдения. Исходники не трогались, второго владельца 748 не запускалось.
  758 закрыта; 748 остаётся за своим живым владельцем, которому этот вердикт и
  требовался. Урок, стоивший двух часов простоя: «прогон идёт» проверяется файлом
  результата, а не сообщением предыдущего тика.

- 2026-08-05 — **748 закрыта: путь «получить ревью изменений» доведён до
  вердикта.** По артефактам, а не по прозе: все четыре обязательных живых
  наблюдения OK — отдельный прогон пережил конец родительской службы (задача
  749, ребёнок записал последний файл через 133 секунды после конца родителя),
  ревьювер сам написал `findings.md` из read-only режима (задача 734), попытка
  закрыть ревью без вердикта отклонена контрактом в `blocked`, живое
  кросс-ревью коммита `f9f8be5` доведено до решения. Кросс-ревью соблюдено в обе
  стороны: делал Codex, независимую проверку делал Claude (753: сначала
  `rework` с пятью блокирующими находками по `656de1c`, затем `approved` по
  `8141c37`, 406 тестов), завершающее ревью кандидата — отдельная Codex-сессия
  `019fd385`, код 0, находок нет. HEAD `companion-agent` = `8141c37`, дерево
  чистое. Продакт принял результат.
- 2026-08-05 — два следствия 748, оба заведены в очередь продактом: (1) вердикт
  734 по `f9f8be5` — `rework` с одной MEDIUM-находкой (машинный ключ отказа
  `owner_stopped_with_work_outstanding` утверждает больше наблюдённого); заведена
  759, правки делает Claude как автор 730, повторное ревью — Codex. (2) сам
  завершающий гейт dev-pipeline закрыл попытку как «premature completion:
  completion review subject is stale» в 20:07, хотя ревьювер одобрил ровно
  текущий `8141c37` без находок, а `completion_ready` вернул True — это ровно
  тот отказ «в минус», который принадлежит 723.
- 2026-08-05 — 711, 723 и 727 стоят `blocked` исключительно из-за отсутствия
  рабочего пути независимого ревью Claude. Путь заработал; их разблокировка —
  следующий шаг треда.

## Открытые вопросы
- Какие из находок 706 (AR-001..AR-004) по гейтам и ревью превращаем в работу.
- `repo-health` сканирует старые каталоги задач и валит гейт на снапшотах 255/586/587/683 (`.env.example`-фикстуры, `runner.log`, `__pycache__`). Это блокирует завершение любой задачи, а не только 711. Рекомендация продакта: сузить область гейта до отслеживаемого дерева, а не чистить чужие артефакты. Решение за пользователем.
