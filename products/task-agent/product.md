# Task Agent

## Концепция
Долговременная память и дисциплина исполнения для работы с агентом: каждый
нетривиальный запрос становится папкой задачи с планом, проверкой и следом.
Заменяет разговор, который исчезает вместе с контекстом окна. Приватный контур —
`/opt/projects/companion-agent`, публичный форкаемый скелет — `/opt/projects/task-agent`.

## Пользовательские пути
| путь | кто | чем запускает | что получает | состояние |
| --- | --- | --- | --- | --- |
| создать задачу | пользователь | запрос в CLI-агенте | папка `tasks/NNN-*` с планом и следом | работает |
| делегировать работу ребёнку | пользователь | запрос на фоновую работу | детский прогон с прогрессом и артефактами | причина падений названа и закрыта наблюдением (749, 2026-08-05): ребёнок теперь запускается в собственной systemd-области (`companion-task-749-…scope`), пережил выход родителя и дописал свои артефакты через 3 мин 0 с после старта (`parent-exit-probe.json`: старт 17:02:22, финальная запись 17:05:22), прогон принят как `completed`; проверено на одноразовой задаче, на настоящем ревью подтверждается прогоном 734. Раньше: работает на старом конверте (727 — 15 мин, 730 — 34 мин, оба `outcome: succeeded`); на новом read-only конверте Codex из 733 деградировал: 734 умер 5 раз подряд, каждый — с `watcher_stop_reason: signal_sigterm` (41 с, 51 с, 2 мин 16 с, …, 40 с), ни один не написал `findings.md`; пятый прогон при этом жил без надзора ещё ~2 часа (964 КБ `runner.log`, 36 МБ временных копий репозитория в собственном каталоге задачи) и умер без кода возврата; при этом 728 и 733 в том же read-only конверте Codex отработали без единого SIGTERM. Сама граница записи наблюдаемо верна: пробы `subject-write.err` и `tmp-write.err` дают «Read-only file system», проба в собственный каталог — `OK`. **2026-08-06 20:0x — ребёнок перестал падать из-за самой песочницы (731, коммит `d851df0`):** во всех ограниченных режимах обоих раннеров временный каталог записываем и сеть доступна — измерено настоящими функциями доступа, `HTTP 204` в четырёх конфигурациях и `pytest` 19 passed изнутри read-only ребёнка там, где раньше он не начинал сбор тестов. Обратная сторона наблюдена в той же проверке и не закрыта: у read-only ребёнка Claude запись в корень репозитория проходит (`rc=0`), у Codex отказана — ремонт в 836 |
| найти прошлый контекст | пользователь | вопрос «что мы решали про X» | ссылки на задачи, проекты, решения | работает |
| закрыть задачу с проверкой | агент | завершение работы | completed только при пройденных гейтах | работает на ревью-задачах (749, 2026-08-05): ребёнок, вышедший с кодом 0 без `findings.md`, механически отвергнут («required review verdict is not established: findings.md is absent»), а тот же ребёнок со своим `Verdict: approved` принят; обе ветки наблюдены в `trace.md` одной задачи |
| индекс задач в Telegram | пользователь | `/task_index` у Max | `task-index.xlsx` | работает |
| поставить контур себе | пользователь на своей машине, дальше — команда | клонировать публичный `task-agent` и поставить окружение | рабочий пайплайн разработки на своей машине | **не работает как надо, и пользователь сказал об этом сам 2026-08-06**: «пайплайн разработки у меня ещё старый — multi-agent, и я реально им не пользуюсь из-за известных недостатков». Наблюдаемо: публичный `origin/main` стоит на `dd8243d` от 2026-07-07 и несёт `multi-agent`; целевой вариант с `dev-pipeline` собран и проверен 2026-07-27 (144 теста, `repo-health` и `pre-push` чисто), но лежит в локальной ветке `port-companion-agent-generic-work` (`6fd5dae`) и не опубликован. Чинится 771; на 2026-08-06 11:45 состояние пути не изменилось — целевая ветка сведена в **локальный** `main` (`6fd5dae`, дерево чистое), а `origin/main` всё ещё `dd8243d`, то есть на чужой машине по-прежнему ставится старый `multi-agent`. **2026-08-06 12:26 — путь заработал, проверено продактом наблюдением, а не прозой: `git ls-remote origin refs/heads/main` даёт `1442a4b`, это восемь коммитов поверх `dd8243d`, дерево чистое, `git grep multi-agent-dev HEAD` не находит ничего.** Живая проверка автора записана в `771/verification.md`: со второго свежего HTTPS-клона установка по README (`python3 -m venv .venv`, `pip install -r requirements.lock`, пакет `rdudov/dev-pipeline` подтянулся с закреплённой ревизии `6e054cb` без локального checkout), затем настоящий прогон `--workflow dev-pipeline` дошёл до `completed` — шесть событий жизненного цикла, код возврата 0, план 3 из 3, `verification` = OK, дерево продукта осталось чистым. Первая попытка честно упала до первого события (раннер звал голый `dev-pipeline`, а `.venv/bin` не было в `PATH`) — это дефект инструкции, он починен коммитом `1442a4b` с регрессией на раскладку README. Кросс-ревью Claude ещё не проведено (781, идёт), поэтому «работает» здесь означает «работает по свидетельству автора и по независимой проверке продакта, что опубликовано». **2026-08-06 12:05 — «работает» отзывается: кросс-ревью Claude (781) дало `changes-requested`, и две из трёх находок ломают именно этот путь на чужой машине.** Первая: обязательный гейт завершения `review-candidate` зовёт CLI мимо нового резолвера (`task_runner.py:1788`, плюс умолчание `--dev-pipeline-bin` в `dev_pipeline_adapter.py:585`), поэтому на раскладке из README задача с обязательным ревью кандидата не может закрыться вовсе. Вторая: второй же шаг Quick Start (`create_task.sh` → `tasks_index.py`) зовёт системный `python3`, а PyYAML README ставит только в `.venv` — на ноутбуке без системного PyYAML команда падает; живой прогон автора этого не поймал, потому что свежий клон переиспользовал системный python сервера, где PyYAML стоит. Третья находка пути не ломает, но её цена отдельная: в опубликованное дерево уехали настоящие имена личных проектов и задач (`moex-strategy-lab`, `greenfield-deepresearch`, `423-run-first-six-month-moex-strategy-cycle` с заголовком, `data/trips/2026-05-02-turkey-flotilla`) — секретов нет, но это ровно запрещённая к переносу категория, и сторож перед пушем её не ловит. Блокеров ревью нет: опубликованное не содержит секретов и не ломает уже опубликованный `main`. Правки поставлены задачей 785 (Codex как автор переноса), повторное ревью — Claude. До закрытия H1 и H2 путь считается **не работающим на чужой машине**, как бы чисто ни было на сервере. **2026-08-06 12:19 — правки опубликованы, но путь пока не переводится в «работает»: подтверждения ещё нет.** Наблюдаемо: `origin/main` в публичном репозитории стоит на `a4e55e7` («Repair foreign-machine task workflow»), дерево чистое. В `785/verification.md` уже записаны две проверки с обеими половинами: команда создания задачи на окружении, где системный интерпретатор не видит PyYAML, до правки падала и после правки прошла; имена личных проектов и задач из публикуемых исходников убраны, сторож ловит нарочно внесённое имя и пропускает нейтральный пример, посторонняя ссылка на приватный репозиторий удалена без переписывания истории. Не сделаны три последних шага плана, и без них «работает» говорить нельзя: настоящая установка со свежего клона на раскладке README с задачей, требующей ревью кандидата (это и есть H1), сверка опубликованного отпечатка из другого клона и независимое ревью Claude ровно опубликованного результата. Прогон 785 жив и идёт **2026-08-06 13:05 — правки по ревью опубликованы, но путь по-прежнему не переводится в «работает»: не хватает последнего свидетельства — независимой проверки чужими руками.** Наблюдаемо: `origin/main` стоит на `572bbfa` («Repair publication guard and Cursor grant»), локальное дерево чистое, `git ls-remote` даёт тот же объект. В `785/verification.md` на этой ревизии записаны свежий HTTPS-клон, установка по README без активации venv, настоящий прогон `review-candidate`, дошедший до `completed` через цикл доработки и одобрения (это и есть H1), создание задачи при системном python без PyYAML (H2), отрицательный контроль на имена личных проектов и на постороннюю ссылку публикации (H3), полный набор 270 тестов, `repo-health` и `pre-push` чисто. **2026-08-06 13:25 — путь принят как работающий: независимое ревью Claude по той же ревизии дало `Verdict: approved` (`785/review-claude-final.md`), а `git ls-remote origin refs/heads/main` продактом сейчас даёт тот же `572bbfa`.** Ревьюер воспроизвёл сам, а не поверил автору: отказ по метке приватной истории и по отсутствующему файлу меток, приём tags/notes/stash и отказ чужому remote, 270 тестов плюс 25 подтестов в одноразовой копии, `repo-health` и `pre-push` чисто, живой прогон `dev-pipeline` из свежего клона. Четыре замечания названы неблокирующими: нет способа легально завести второй remote (`upstream`), два теста подстрок потеряли нейтральный пример, явно указанный несуществующий файл меток падает трейсбеком вместо обычного `ERROR:`, один комментарий про Cursor устарел. **2026-08-06 13:53 — публичный `origin/main` теперь `c08a2ac` с переписанной историей (795): 13 коммитов без хостовых путей и личных имён, дерево прежнее (`52c585a5…`), в README — записка, как синхронизировать старый клон (`git fetch --all && git reset --hard origin/main`, с предупреждением о потере незакоммиченных правок). Клонировать и ставить контур можно уже с этой ревизии; кросс-ревью Codex идёт (804). |
| показать подход снаружи | посторонний человек | открыть карту контура в браузере | вид сверху во времени: кто работает, что произвёл, каким каналом это ушло, где затор, что ждёт человека | ставится (726): снимок состояния собирается за 1,7 с по 258 задачам и обезличивается с отрицательным контролем; самой карты ещё нет. **2026-08-06 09:26 — пользователь дал «поехали» и переопределил предмет письмом `19fd65f1d8407bd7`:** цель прямо названа вау-эффектом («трудно объяснить человеку по логам и переписке в разных каналах, что происходит»), и обязательными стали три свойства — динамика вместо снимка, опознаваемость конкретной задачи по настоящему названию, участники с артефактами и каналами связи как объекты. Сценарий демонстрации назван пользователем: он пишет продакту письмо, и на карте видно, как задача принимается, декомпозируется, уходит в dev-pipeline и ведётся по ролям — анализ, разработка, тесты, ревью, коммиты и пуши. Этап 2 переписан на писца ленты плюс рендер с осью времени; запущен на Claude через dev-pipeline (pid 2066838, своя systemd-область), доставка в `/opt/projects/product-owner`. Сам прогон выбран через dev-pipeline намеренно — он производит ту ленту событий по ролям, которую карта показывает. Наблюдаемая опора ленты проверена продактом до запуска: `dev-pipeline/core/events.jsonl` есть у 100 задач и несёт старт попытки с рантаймом, pid процесса, `checkpoint_completed` с sha256 артефакта, код возврата и причину отказа; роли доопознаются появлением `analysis.md`/`plan.md`/`verification.md`/`trace.md`/`findings.md` по mtime, каналы — `notification-receipts.jsonl` и каталог входящей почты. **2026-08-06 11:54 — карта есть, и это проверено наблюдением, а не прозой: коммит `4836a15` в `/opt/projects/product-owner`.** Запись `evidence/map-recording.html` (369 КБ, sha256 `3b3779656fb7a7ad…`) открывается по `file://` при принудительно выключенной сети — ноль сетевых запросов, ноль ошибок консоли и страницы, на 1440×900 и 390×844 переполнения нет; ось времени живая (пуск двигает ползунок 0 → 20–23 из 1000 за полторы секунды, пауза держит, перемотка переставляет момент, скорости ×0,5–×10). На ленте 109 наблюдений, из них 14 — сама 726 настоящим названием, с движением по станциям и с проходом по обоим каналам (Telegram и входящая почта). Живой режим тем же рендером отвечает 200 (`--serve 8791`, `data.json` 331 859 байт, режим `real`). Сборщик 1,79 с на 297 задачах, тик писца 0,34–0,56 с по 671 каталогу, транскрипты детей не читает ни один из них; обезличивание проверено отрицательным контролем на пути, почте и числовом идентификаторе; 20 регрессий зелёные. Оговорка записана честно, а не спрятана: браузер — Firefox, потому что ни одна из четырёх сборок Chromium в песочнице не стартует (SIGTRAP), и станции «коммит» у объекта задачи нет — на диске ничто не связывает коммит с номером задачи. Путь остаётся «ставится»: карта построена, но не показана ни одному постороннему человеку — приёмка по замыслу пользователя измеряется человеком, а не тестом. **2026-08-06 13:38 — приёмка человеком проведена и провалена, первым посторонним человеком оказался сам пользователь** (письмо `19fd6ddd0934a0c8`): «Я посмотрел карту сам. И ничего не понял… какие задачи в работе, на каком этапе — это уже невозможно». Ни на один из трёх вопросов приёмки карта не ответила. Предмет переопределён его же словами: сначала структура данных и структурированное представление в плашках, стилизация — потом, «переходить к другой визуализации, пока на овальчиках не отработаем, преждевременно». Раскладка задана пользователем: четыре вертикальные панели-направления, внутри области по статусам друг над другом, на плашке номер, название, статус, исполнитель (codex/claude) с текущей ролью и происходящим; каналы связи схематично при направлениях; коммиты-пуши на уровне продукта. Работа поставлена задачей 784 и запущена на Claude через dev-pipeline (pid 2133760, живость подтверждена наблюдением), доставка в `/opt/projects/product-owner`. **Два утверждения этой строки от 11:54 отзываются как неверные — их опровергло независимое ревью Codex (780, вердикт rework: 2 HIGH, 4 MEDIUM, 1 LOW):** обезличивание НЕ проверено отрицательным контролем на числовой идентификатор — `scrub()` не применяется к полям `title`/`task_title` вообще и не трогает целые числа, поэтому в файле с пометкой «ОБЕЗЛИЧЕНО», отправленном пользователю, живёт `433978200` в названии задачи и 298 полей `pid`; и записей задачи 726 на ленте не 14, а 18. Сверх того ревью нашло, что живое обновление срабатывает только при росте длины ленты (смена статуса без новой записи невидима), что исполнитель и роль приписываются существованием файла, а не наблюдаемым источником, и что рендер сам импортирует сборщик, то есть заявленная граница «рендер видит только JSON» держится соглашением о запуске, а не физически. Первые три из этих дефектов лежат прямо под доской и чинятся в 784. **2026-08-06 12:06 — доска собрана (коммит `dd9c6cd`), но 12:18 независимое ревью Codex (786) вернуло её на доработку: вердикт `rework`, 3 HIGH и 3 MEDIUM при 57 зелёных тестах.** Правдиво доска отвечает только на первый из трёх вопросов приёмки. «Ждёт решения человека 13» построено правилом «любая заблокированная задача плюс любой пункт Open Questions», и в него попали шесть задач без ожидающего выбора (760 и 727 — технические блокировки, 747 — ремонт уже вынесен в 757, 779 — в тексте прямо сказано, что выбора нет, 722 — инструкция на будущее, 723 — вопрос вместе с ответом), а 17 продуктовых вопросов из того же снимка не посчитаны вовсе. На «где затор и почему» доска отвечает только «где»: у названной ею задачи 686 причина лежит на диске (`status_detail: queued_behind_active_worktree_writers_669_689`) и не выводится ни плашкой, ни верхней строкой, а сама область затора на осмотренном снимке 1440×900 уехала ниже заполненной области «ждёт человека». Обезличивание течёт заново и по новой причине: писец ленты сохраняет исходные `task_title`/`label`, зовёт очистку и возвращает исходные значения обратно, поэтому отрицательный контроль ревью сохранил и номер, и путь, и почту во всех заголовочных полях (`786/evidence/review-probe.json`); сегодняшний файл чист случайно. Закрытым ревью подтвердило: физическую границу рендера, обновление по отпечатку показываемого состояния, байтовый курсор журнала и отказ подписывать исполнителя без наблюдаемого источника. Путь остаётся «ставится» и человеку не показывается: правки поставлены задачей 787 на Claude, повторное ревью — на Codex **2026-08-06 13:10 — правки по всем шести находкам ревью доставлены (коммит `b525eec` в `/opt/projects/product-owner`, восемь файлов), путь остаётся «ставится».** По артефактам задачи: у «ждёт решения человека» теперь один владелец — счёт упал с 14 до 3, все шесть названных ревью задач вышли (722 и 779 в очередь, 723, 727, 747 и 760 в затор с названной причиной), у каждой из оставшихся трёх есть живой неотвеченный вопрос, и 15 продуктовых вопросов вошли в тот же счётчик (заголовок читает «18 — 3 в задачах и 15 вопросов продуктов»). Причина затора выводится и имеет источник: на плашках 28 причин, ни одной без источника. Течь обезличивания закрыта у корня — восстановление заголовков после очистки удалено из писца, отрицательный контроль ревью (идентификатор, путь и почта в сохраняемом заголовке) больше не выживает ни в файле, ни на живом HTTP-пути, положительные контроли показывают, что проверка не выродилась. Живость прогона считается по личности процесса, а не по номеру. Три вопроса приёмки видны на первом экране по геометрии на 1440×900 и 390×844; про телефон сказано прямо, что три из четырёх направлений лежат ниже сгиба. Тестов 87 из 87, и оба теста, закреплявших прежнюю ложь, переписаны. Не сделано главное: доску снова не видел ни один посторонний человек — `human_acceptance` честно стоит GAP, потому что показ идёт после повторного независимого ревью. Оно поставлено задачей 789 и запущено на Codex (автор правок — Claude), только на чтение, живость прогона подтверждена наблюдением. **2026-08-06 13:19 — повторное ревью закончено, вердикт снова `rework`, но уже одной находкой из шести.** Пять семейств правок Codex подтвердил собственными проверками, а не по прозе автора: разбор 306 задач без вызова скрипта автора дал ровно те же три задачи с живым вопросом, отрицательные контроли обезличивания не выжили ни в писце, ни на живом HTTP-пути, живость идёт по личности процесса, коммит `b525eec` несёт ровно заявленные восемь файлов, хэши поставки совпали, 87 из 87 тестов прошли на замороженной копии. Незакрытым остался первый экран: доска называет четыре затора, а причина читается только у первого — при 1440×900 причина четвёртого уходит вправо до x=2536,9 при ширине окна 1440, при 390×844 за границу уходят третья и четвёртая, у 723, 727 и 747 текст обрывается многоточием, и подсказка его не возвращает. Дыру в проверке ревьюер назвал поимённо: `board_check.js:73-84` измеряет геометрию только `jamItems[0]`, поэтому зелёный набор совместим с дефектом. Правки поставлены задачей 792 на Claude, повторное ревью — снова на Codex. Путь остаётся «ставится». |
| посмотреть, что происходит с работой | пользователь и продакт | открыть табло в браузере | шесть ответов на одной странице: что зависло, что можно взять прямо сейчас, что стоит и за чем именно, что обещано в продукте и не заведено задачей, что ждёт решения человека, кто работает сейчас — и карточка задачи по нажатию, без ухода со страницы | **ставится (816 сделана, ждёт встречной проверки 818).** Встречная проверка 814 (Codex) вернула `rework` с одной блокирующей находкой: область «надо запланировать» считала непоставленной любую строку продуктовой записи без отдельно стоящего трёхзначного числа, и все четыре показанные ею строки были выдуманы. Задача 816 (Claude, коммит `718265f`) заменила числовой признак наблюдаемой связью строки с задачей — по номеру-ссылке, дословному названию, слагу; там, где связи нет, область теперь говорит «связь с задачей не установлена» вместо «задачи нет». По артефактам автора: 140 тестов (было 130, добавлено 10, из них оба отрицательных контроля находки), на настоящем состоянии 175 строк «В работе» пяти продуктов — 157 связаны с задачей с указанием, чем именно, 18 помечены неустановленной связью; живая служба перезапущена и проверена настоящим браузером (18 плашек в DOM, снимки 1440x1200 и 1440x2600). Не проверено независимо: не связана ли какая-то из 157 строк по случайному совпадению слова или числа и не потеряна ли задача у какой-то из 18 — это предмет 818. По артефактам автора, не по прозе: 130 регрессий, 36 независимых сверок с диском и таблицей процессов без единого расхождения, живая проверка в настоящем браузере на живом сервере — карточка открывается и закрывается без единой перезагрузки страницы, изменение файла на диске страница замечает за 12 секунд и перерисовывается на месте, открытая карточка это переживает. Наблюдатель сведён к одному: пробуждение по расписанию читает сборщик доски вместо второго собственного счёта, цена одного направления прежняя — 1,45 с. Два дефекта нашла сама живая проверка и оба закрыты с перемером: при семи областях в колонке плашки сжимались до 12 пикселей и не нажимались, а обёртка `bash -c` попадала в список разбуженных продактов под именем «/bin/bash». Автор честно назвал недомеренное: ветку «стоит за занятым репозиторием» он видел живьём один раз. Состояние не переводится в «работает», пока ревью не вернуло вердикт: эту же доску проверяли трижды и трижды возвращали на доработку, дважды — по течи обезличивания. **2026-08-06 18:2x — 817 закончена (Claude, `standard`), и по её артефактам доска отвечает на требования пользователя к виду и к честности областей.** Светлая тема основная; вложенных полос прокрутки нет ни у плашки, ни у области, ни у колонки, ни у карточки — карточка занимает страницу, обрезания по числу строк убраны; тёмной осталась только карта во времени. Измерено браузером (Chromium headless, DevTools) на 1440×900 и 390×844, на файле сборки и на живой службе `127.0.0.1:8791`, с закрытой и с открытой карточкой: элементов со своей прокруткой 0, обрезанного текста 0, уехавшего за окно 0; фон `rgb(242,245,249)`, страница 12 798 px и 32 910 px. Область «Ждёт решения человека» теперь требует наблюдаемой пометки «спрошено у пользователя, дата, канал, идентификатор сообщения» и снимает вопрос по ответу в том же треде; наши вопросы стоят отдельно в «Решает продакт», оба числа показываются рядом. На живом состоянии `process_map_state.py --questions`: 13 продуктовых строк, все продакту, пользователю 0 — до правки в его области стояло 16. Появилась область «Сделано, но не доставлено» (документ в каталоге при отсутствии `delivery.md` и квитанции о документе): 45 завершённых задач; случай 783 воспроизведён тестом на его тогдашнем состоянии. Тестов 171, `repo-health` чисто, служба перезапущена и отдаёт 200. Путь остаётся «ставится»: писал Claude, встречная проверка Codex не проведена (недельное окно Codex израсходовано на 80%, сброс 2026-08-10), а нуль в области пользователя был честным следствием того, что три его вопроса ему никто не задавал — задано письмом `19fd7e7ea2c3f7fb`. **2026-08-06 19:29 — 829 закрыла последнее место, где табло держало отвеченный вопрос:** отправленное письмо теперь ложится на диск рядом со входящими (`.state/gmail/product-owner/sent/<id>/metadata.json`, только метаданные, тел писем нет), поэтому по идентификатору исходящего восстанавливается тред и в нём находится ответ. По `verification.md` — живая отправка `19fd81822833abbc` записана на диск сразу после отправки; на настоящем письме `19fd7e7ea2c3f7fb` и настоящем ответе пользователя `19fd75ff10dd7605` от 17:00 MSK строка вопроса про дневной лимит убытка сама перешла из области пользователя в «Решает продакт»; почтовых вопросов пользователя на живом табло 0; 25 входящих тредов читаются как прежде; тестов 19 у почтового клиента и 172 у табло. Ни одного GAP. Путь остаётся «ставится»: писал снова Claude, встречная проверка Codex по всей почте (727, 760, 829) заведена задачей 832 и ждёт сброса недельного окна 2026-08-10 |
| написать продакту письмо | пользователь | письмо на ящик продакта | продакт просыпается сам и работает из содержания | работает вживую с 2026-08-05: письмо на холодную систему поднято плановым тиком за **3 мин 22 с**, вложение проверено круговым прогоном (md5 совпал байт в байт); стоимость покоя подтверждена журналом опроса на 2026-08-05 19:23 — 225 проверок ящика, из них 218 пустых, вызовов модели ровно 7 = числу пришедших писем, одна попытка совпала с уже идущей и корректно отступила (`already_running`); не закрыто кросс-ревью (727) |
| прочитать ответ продакта | пользователь | ответное письмо от продакта | понятный ответ в той же переписке | работает плохо, и пользователь сказал об этом дважды подряд. 2026-08-05: «включай в ответ исходное, чтобы можно было понять контекст переписки»; 2026-08-06: «выглядит, как привет из 90-х… plain-text с разрывами строк и странными заголовками… письма выглядят довольно узкой колонкой текста». Наблюдение по отправленному экземпляру (`19fd3fe622349912`): абзац уходит одной строкой длиной до 975 символов, то есть переносы рисует клиент, показывая простой текст фиксированной шириной; ветку письма собрать не из чего — `send_email.py` не ставит `In-Reply-To`/`References` и не цитирует, цитата вклеивается руками. Чинится задачей 760 (запущена 2026-08-06 на Codex): html рядом с обычным текстом, автоцитирование, связывание в переписку. **Состояние на 2026-08-06 08:05 — почти работает, проверено вживую продактом, а не прозой исполнителя.** Отправлены два настоящих письма (`19fd5ad393c06893` и ответ на него `19fd5add5286b4d1`), оба выгружены `export_email.py` и осмотрены в сыром MIME: письмо идёт `multipart/alternative`, абзац в 900 символов остаётся одной логической строкой, html-часть даёт настоящие заголовки, списки и выделение без ASCII-рамок, ответ несёт `Re:`, `In-Reply-To`, `References` и цитату исходного и в обычном тексте, и в html. Найденный этой же проверкой дефект темы закрыт: пробел на стыке кодированных кусков пропадал («контроль переносов и разметки» приходило как «контроль переносовиразметки»), задача 766 завершена, и 2026-08-06 09:05 это подтверждено живьём на настоящем ответе пользователю `19fd65171ced903e` — тема декодируется как «Re: Тиражирование companion-agent / dev-pipeline» с пробелами на месте, вместе с `In-Reply-To`, `References`, 24 строками цитаты и абзацем в 1137 символов одной строкой. Правки 760 и 766 по-прежнему не закоммичены и не прошли кросс-ревью Claude — долг продакта, заведён задачей 778; она стоит за 771, потому что перенос читает дерево companion-agent и посторонний коммит во время его работы мешает сверке. **2026-08-06 14:05 — правки закоммичены (`84fba25`) и прошли кросс-ревью Claude (794): вердикт `rework`, две находки HIGH.** Что заявлено — подтверждено самостоятельными пробами ревьювера на настоящих байтах письма: пробел в теме на стыке кодированных кусков, абзац в 900 символов одной логической строкой для ASCII и кириллицы, `multipart/mixed[alternative[plain,html],attachment]`, три отказа ответа, невозможность инъекции заголовка через тему. Сломано другое, и это регрессия против прежнего поведения: разбор разметки применяется ко всему тексту, поэтому обычный текст теряет символы («5 * 3» → «5  3», `__init__` → `init`, `/tmp/_cache_/x` → `/tmp/cache/x`, глобы в обратных кавычках теряют звёздочку), а блок кода, таблица, цитата и отступ склеиваются в абзац; в диффе «+ new line» приходит как «- new line». Отдельно: html-цитата разваливается со второго ответа в треде — plain-часть при этом остаётся верной. Путь остаётся «работает плохо»: письмо продакта в поддержанном подмножестве доезжает верно, но устойчивости к произвольному тексту нет. Чинится 803 (Codex), повторное ревью — Claude |
| продакт сам замечает, что работа встала или дошла | пользователь | ничего не делает | пуш и письмо на переходе: прогон закончился, задача встала, репозиторий двинулся | работает вживую: четыре направления опрашиваются systemd-таймером каждые 20 минут, тик читает только диск и процессы и молчит без перехода (0,1 с CPU на пустом), 2026-08-05 в 19:03 и 19:23 переходы подняли продакта и он сам прислал два вердикта. Дыра наблюдаемая: событие живёт только в снимке — тик перезаписывает снимок **до** пробуждения, поэтому неудачный запуск агента теряет событие безвозвратно; и переход, целиком уместившийся между двумя тиками, не виден вовсе | **Найдена вторая дыра, наблюдённая целиком 2026-08-06:** очередь по переходу не двигается, потому что условие запуска задачи существует только предложением в записи, а не полем. 831 стояла с пометкой «после 830, то же рабочее дерево»; 830 закрылась в 19:52 UTC, тик в 20:05 увидел переход и поднял продакта, тот принял 830 и написал вердикт — но 831 не запустил, прямо назвав причиной бодрствующего второго продакта и запрет на двух детей в одном дереве. Интерактивный продакт сигнала не получает вовсе: таймер будит процесс, а не разговор. Задача простояла сорок минут и стартовала по вопросу пользователя «как ты вообще отслеживаешь очередь». Тик знает четыре перехода (прогон завершился, прогон врёт о живости, задача ушла в blocked, репозиторий получил коммит), и «планируемая задача стала запускаемой» среди них нет и быть не может, пока условие не записано машиночитаемо. Поставлено задачей 840.

## Текущая ставка
Свидетельство, привязанное к наблюдению, а не к прозе исполнителя: гейт без
фактической команды и наблюдённого значения не должен считаться пройденным
(задача 706, находки AR-001..AR-004).

## Не делаем
- Новый оркестратор, DSL выражений или plugin-слой поверх существующих владельцев.
- Расширение контракта декларативными полями, которые заполняет тот же исполнитель.

## В работе
- 2026-08-06 20:1x — **Хвост «сделано, но не доставлено» разобран наблюдением (835), и он оказался почти обратен своему названию: из 89 задач с документом человек получил 63, ещё 11 документов — внутренние, и никто не получил только 15.** По артефактам, не по прозе: Telegram-выгрузка под учётной записью пользователя дала 6619 сообщений с файлом, отправленная почта продакта — 6 писем с вложением, сопоставление шло по имени **и точному размеру**, а само правило проверено содержимым: десять совпадений из разных мест хвоста скачаны и сравнены по sha256 с файлом в каталоге задачи — совпали все десять. Канал доставки почти монопольный: бот Calypso в личном диалоге, 65 совпадений против 4 почтовых. Причина пустоты названа наблюдением, а не догадкой: все 242 квитанции репозитория — про жизнь прогона, ни одной про документ, поэтому доставку руками наблюдатель видеть и не мог. В каталоги легли 63 `delivery.md` (канал, идентификатор сообщения, sha256) и 11 отметок «## Доставка» словами, без единой перезаписи; область табло упала с 46 до 15 повторным прогоном того же наблюдателя продакта, а не пересчётом своей же таблицы. GAP один и с владельцем: 11 внутренних задач с табло не уходят, потому что у наблюдателя нет понятия «внутренний документ», а подделывать `delivery.md` там, где доставки не было, нельзя — чинится в 834 и 826. Ничего не отправлено намеренно: отправка третьей группы — решение продакта. **Решение продакта принято здесь и вслух:** из девяти живых документов человеку идут три (`memory-redesign-analysis.md` задачи 796 — обязательно вместе с замечанием кросс-ревью 805, `board.html` задачи 790 и пакет замера Deep Research на GigaChat задачи 821), остальные шесть — исторические (первые циклы MOEX, опись дублей, план переноса) и уходят в архив без отправки; три документа третьей группы помечены самой 835 как устаревшие, три — машинный вывод, а не текст для чтения. Отправка тремя письмами — ближайшая работа продакта, не задача исполнителя.
- 2026-08-06 20:0x — **Права ограниченного ребёнка (731) приняты: у него теперь есть временный каталог и сеть, и обе четверти прогонов, которые на этом стояли, разблокированы.** По артефактам, не по прозе: живая граница снята настоящими функциями доступа `task_runner.py` до и после правки, по четырём конфигурациям. Было — `/tmp` закрыт у обоих раннеров, сеть закрыта у обоих, `pytest skills/gmail-client` изнутри read-only ребёнка Codex умирал ровно так же, как в ревью 728 (`No usable temporary directory`). Стало — `/tmp` записываем и сеть отвечает `HTTP 204` во всех четырёх конфигурациях, `pytest` даёт 19 passed изнутри read-only ребёнка обоих раннеров, а предмет ревью у Codex по-прежнему закрыт на запись (`rc=1`). Регрессии: 276 у раннера, 419 по всем скиллам, `repo-health` чисто; ни одного GAP в `verification.md`. Продакт закоммитил дельту, которую прогон оставил в дереве, — `d851df0`, шесть файлов, дерево отслеживаемых чисто. **Найден и честно вынесен открытый дефект вне названной дельты: у read-only ребёнка Claude запись в корень репозитория проходит (`rc=0`), потому что `sandbox.filesystem.allowWrite` дополняет умолчание рабочего каталога, а не заменяет его** — то есть утверждение из итога 723 было неверным, документы исправлены на наблюдённое, ремонт заведён задачей 836. Практический смысл: кросс-ревью на Claude сегодня не защищает предмет ревью от записи ревьюером. Кросс-ревью самой 731 заведено задачей **837** — писал Claude, проверяет Codex, — и ждёт сброса недельного окна Codex 2026-08-10 (на сейчас израсходовано 83%, `codex_budget.py` даёт ненулевой код). Отдельно про 723: её единственный GAP был не в работе, а в среде — живого read-only ребёнка Claude нельзя было наблюдать из-за отсутствия сети; теперь сеть есть, и это наблюдение можно сделать настоящим ревью, а не пробой. Ближайший кандидат — заблокированная 711, которая с 4 августа ждёт именно рабочего пути ревьювера на Claude; запускать её на этом пробуждении продакт не стал: сперва 836, иначе ревьюер физически может править предмет.
- 2026-08-06 19:4x — **Область табло «сделано, но не доставлено» на 50 задач объяснилась одним наблюдением: линия ни разу за всё время не доставила документ сама.** Квитанций во всём репозитории 242, и все до одной про жизнь прогона — 121 «работа началась», 77 «работа кончилась», 42 отказа приёмки, 2 падения. Ни одной про то, что документ дошёл до человека. Значит всё, что пользователь получил, отправлено руками продакта с хоста, а область честно показывает отсутствие свидетельства, а не потерянную работу. Отсюда две работы. **834** — прогон обязан доставлять свой документ в назначенный канал и записывать квитанцию с идентификатором сообщения и sha256, а неудачу показывать отказом, а не тишиной. **835** (идёт) — разобрать нынешний хвост наблюдением и разделить его на три группы: доставленное руками (искать в Telegram — из почтового ящика продакта за всё время ушло всего пять писем с вложением), внутренние заключения прогонов, которые человеку не нужны вовсе, и настоящие недоставленные; по доказанным доставкам положить `delivery.md` по образцу 822, третью группу выдать продакту списком. Отправлять ничего сам разбор не будет — отправка остаётся решением продакта.
- 2026-08-06 19:4x — **Хранение отправленной почты (829) принято: ответ пользователя теперь виден наблюдением, и табло само снимает вопрос.** По артефактам, не по прозе: `verification.md` — пять строк, все OK, ни одного GAP; живая отправка `19fd81822833abbc` и живой ответ пользователя `19fd75ff10dd7605` в треде письма `19fd7e7ea2c3f7fb`; на живом табло почтовых вопросов пользователя стало 0; входящие 25 тредов не тронуты; в каталоге отправленного только `metadata.json`, тел писем нет; тесты 19/19 и 172/172; коммит `5e51923`. Доработки не требуется, новых детей на этом пробуждении не запускалось. Кросс-ревью не проводится сейчас намеренно: всю почту делал Claude, проверяет Codex, а недельное окно Codex на 2026-08-06 израсходовано на 82% — это условие уже записано в заведённой 832 со сроком 10 августа.
- 2026-08-06 19:3x — **Разбор затора: из пяти задач процессного контура, стоявших «требуют внимания», осталось две, и обе с названной причиной.** Закрыты наблюдением, а не прозой. **760** (ответ на письмо: цитата, разметка, тема) — оба остававшихся живых гейта прошли: продакт заново выгрузил письмо `19fd5ad393c06893` и увидел абзац ровно в 900 символов одной логической строкой при `multipart/alternative` (запись в задаче о том, что этот контроль не делался, оказалась неверной), а дефект темы проверил после правки живой самоотправкой `19fd820e8964e006` — кириллическая тема декодируется дословно, пробел на стыке закодированных кусков больше не теряется (`_set_subject`, коммит `84fba25`). **727** (почта как рабочий вход) — её единственный GAP был средовым: ребёнок не доставал до `gmail.googleapis.com`. Предмет с тех пор работает в эксплуатации и это видно с хоста: строка `*/5` в `crontab`, идущие записи в `cron.log`, 25 тредов входящих со свежайшим письмом пользователя 18:45, живые доставки вложением (743, 744, 726). **726** (карта процессной работы) — прогон оборвался на 7 шаге из 9, но запись карты пользователю доставлена письмом `19fd68b3021fa1b7`, а живое продолжение построено уже задачами 801/814/816/817/826; вести вторую линию той же работы незачем. Осталось две: **723** ждёт ровно одного живого наблюдения — read-only ребёнок Claude пишет собственный артефакт, — и это наблюдение делает 731, поэтому 723 закроется её доказательством, а не своим перезапуском; **711** (правки по кросс-ревью 710) требует Codex как исполнителя по правилу «правит не тот, кто ревьюил», а недельное окно Codex израсходовано на 82% до 10 августа — блокировка настоящая, денежная, а не наша выдумка.
- 2026-08-06 19:34 — **Запущены права детского прогона (731) — корень целой семьи заторов.** Наблюдением: pid 2385545 жив, `dev-pipeline`, `danger-full-access`, репозиторий companion-agent, живых прогонов в треде до старта не было. На этом месте встали четыре разные задачи: 723 не смогла посмотреть на собственного read-only ребёнка (пустой список сетевых адресов), 727 не достучалась до Gmail, 728 не начала сбор pytest из-за недоступного временного каталога, 809 упёрлась в запрет сети из вложенной песочницы на обязательной проверке политики. Пока это не вылечено, каждая такая задача чинится вручную и заново. Кросс-ревью самого почтового клиента (727, 760, 829) заведено задачей 832 и ждёт сброса окна Codex 10 августа: всё три делал Claude, проверять должен Codex.
- 2026-08-06 18:3x — **Доска стала светлой и прокручиваемой, «ждёт решения
  человека» перестало быть свалкой наших размышлений, и документ об этом ушёл
  пользователю — не остался на сервере.** Задача 817 закончена (Claude,
  `standard`), измерения — в её `verification.md`: 171 тест, браузер на 1440×900
  и 390×844 с нулями по вложенным скроллам, обрезанному тексту и вылету за окно,
  живая служба перезапущена. Доставка: письмо `19fd7e7ea2c3f7fb` ответом в тред
  «Продакт: Процессный контур», записана в `tasks/817-.../delivery.md` вместе с
  sha256 доставленного. Тем же письмом продакт наконец **задал** три вопроса,
  которые сам же назвал пользовательскими и которые до этого письма человеку
  никто не задавал: внешние пользователи Max, что уносить из Deep Research,
  дневной лимит убытка на фьючерсах (с рекомендацией переносить и считать по
  площадке). Пометки поставлены в записях `companion`, `deep-research` и
  `moex-strategy-lab`, поэтому область пользователя на табло перестала быть
  пустой законно. Встречная проверка не запущена намеренно: писал Claude, значит
  проверяет Codex, а `codex_budget.py` даёт 80% израсходованного окна и
  ненулевой код возврата (сброс 2026-08-10). Задача 826 заведена `planned` с
  условием запуска по бюджету. Открытым в самой 817 остаётся то, что исходящая
  почта продакта на диске не сохраняется (`.state/gmail/product-owner/sent/`
  нет), поэтому автоматическое исчезновение отвеченного вопроса работает только
  для вопросов, заданных ответом на письмо пользователя, — как этот.
- 2026-08-06 17:5x — **Пользователь прочитал отчёт об изменениях и вернул
  главное: dev-pipeline нужен и через Cursor.** Его слова: «там написано, что для
  cursor поддерживается только standard, мне это не очень подходит. Если это
  действительно так, надо доработать». Отчёт не соврал, продакт проверил по коду
  до постановки задачи: раннер отказывает — «The dev-pipeline workflow supports
  the codex and claude runners» (`task_runner.py:1256`), а в публичном ядре
  `OWNER_RUNTIMES = ("codex", "claude")` и рядом два рантайм-модуля с запуском,
  опознанием нативной сессии и продолжением. Осуществимость тоже проверена
  наблюдением, а не догадкой: `cursor-agent --help` даёт `--print`,
  `--output-format stream-json` и `--resume [chatId]` — то есть все три вещи, на
  которых держится владелец. Поставлено задачей 820 с обязательной живой
  проверкой: прогон `--runner agent --workflow dev-pipeline` доходит до
  завершения и переживает продолжение прерванного прогона; прогон без resume
  доказательством не считается. Отзыв об отчёте записан как есть: «на статью не
  тянет, но написан достаточно понятно для меня, поскольку я в теме» — то есть
  для внешней статьи документ придётся переписывать под человека не в теме.
- 2026-08-06 17:46 — **Область табло «надо запланировать» перестала считать
  задачи по трёхзначным числам в тексте и ушла на встречную проверку.** Правка
  816 закончена (Claude, коммит `718265f`): признак «в строке нет числа — значит
  задачи нет» заменён наблюдаемой связью строки с задачей, а там, где связь не
  нашлась, область честно говорит «связь с задачей не установлена». На живом
  состоянии из 175 строк «В работе» связаны 157, помечены неустановленными 18;
  тестов 140/140, живая служба проверена настоящим браузером. По правилу
  кросс-ревью проверяет Codex — задача 818 запущена 17:46, read-only по
  /opt/projects/product-owner; её предмет — не пропускает ли новая связь
  настоящую непоставленную работу по случайному совпадению слова или числа.
  Задача 817 (светлая тема, прокрутка, «ждёт человека» только про пользователя)
  правит те же файлы и стоит за проверкой, чтобы у ревьюера не менялось дерево
  под руками., и он прав:
  «ждёт решения человека» считает наши размышления его очередью.** Его слова:
  «в моей картине мира, я ответил на все запросы. И ты пишешь в письмах по
  направлениям, что от меня ничего не требуется. Расхождение». Продакт разобрал
  руками все 16 строк области: пользователю принадлежат **три**, остальные
  тринадцать — либо наши вопросы приоритета, либо уже отвеченные, либо давно
  починенные. Поимённо: у 711 оба вопроса закрыты кодом (`b45ab49`), у 511 они
  к исполнителю, 558 решает продакт, `repo-health` починен `e12e511`, про сеть
  ребёнка пользователь уже ответил «не ограничивать», «цена суточного
  упрощения» стала задачей 779 по его же указанию. Правило, которое отсюда
  следует: **в этой области может стоять только то, что явно спрошено у
  пользователя и осталось без ответа** — с датой и идентификатором сообщения, и
  исчезать оттуда по наблюдаемому ответу, а не по чьей-то памяти. Наши вопросы
  переезжают в отдельную область «решает продакт». Плюс его же требования к
  виду: светлая тема, вертикальная прокрутка вместо ужимания в экран, никаких
  микроплашек с микроскроллом внутри. Поставлено задачей 817, стоит за 816
  (та правит те же файлы).
- 2026-08-06 17:2x — **Отчёт по портфелю пролежал на сервере час, и нашёл это
  пользователь, а не мы.** Его слова: «по выполненным задачам я не видел
  документов в почте или в телеграме». Проверено: файл 441 КБ был готов в 16:14,
  квитанции задачи содержат только события жизненного цикла — 18447 «работа
  началась» и 18479 «работа кончилась», — то есть уведомление о конце работы
  ушло, а результат работы нет. Отправлен продактом (18491) с прямым признанием
  опоздания в подписи. Системная часть — в 817: у табло появляется область
  «задача завершена, документ для человека есть, свидетельства доставки нет», с
  наблюдаемым признаком (файл в `deliverables/` при отсутствии `delivery.md` и
  квитанции с идентификатором сообщения). Это ровно тот класс, из-за которого
  описание изменений task-agent пришлось просить дважды.
- 2026-08-06 17:1x — **Оба заказанных документа доставлены, и табло теперь
  доступно живьём, а не файлом на сервере.** Пользователь спросил описание
  изменений task-agent **во второй раз** — первый раз оно было собрано задачей
  772 и оставлено на диске. Это ровно то, против чего уже записано правило
  «отчёт, оставшийся на диске, считается недоставленным»; цена — потраченное
  письмо пользователя. Отправлено: `task-agent-changes.html` (39 416 байт,
  сообщение 18487), осмотрено продактом перед отправкой — ноль внешних ссылок,
  ноль картинок, ни одного хостового пути. И снимок табло (1 319 282 байта,
  sha256 `4931fdb9…`, сообщение 18488).
  Главное здесь — не файлы, а снятое ограничение. Пользователь сказал прямо:
  «доска лежит на сервере, но мне это не подходит: надо либо оффлайн-копию, либо
  отдавать через веб-сервер, можно с пробросом портов». Поднята служба
  `product-owner-board`: слушает **только** `127.0.0.1:8791`, наружу ничего не
  открыто, включена в автозапуск и перезапускается сама. Проверено наблюдением, а
  не выводом команды запуска: `systemctl is-active` = active, `ss -ltn`
  показывает привязку к петле, `curl` отдаёт 200 и 1 319 142 байта за 2,95 с.
  Пользователю отправлена команда туннеля (18489). Снимок при этом честно назван
  устаревающим в момент сборки — рабочий вход теперь живой.
  Тем же сообщением названа единственная область табло, которая врёт:
  «надо запланировать» считает задачу отсутствующей, если в строке продуктовой
  записи нет трёхзначного числа, поэтому из четырёх показанных строк три уже
  имеют задачи, а настоящая непоставленная работа может не попасть вовсе
  (находка HIGH-1 ревью 814). Правка поставлена задачей 816 и запущена.
- 2026-08-06 16:47 — **Работа над табло (801) закончена автором, и она ушла на
  встречную проверку: автор — Claude, ревьюер — Codex (814, standard,
  `danger-full-access`, предмет — `/opt/projects/product-owner`).** Запуск
  подтверждён наблюдением, а не выводом команды: pid 2278941 жив, свой
  systemd-scope, первая строка `trace.md` записана. До вердикта ревью ни один
  ответ доски продакт своим не считает: доску проверяли трижды (780, 786, 789),
  и каждый раз она возвращалась на доработку — в том числе дважды по течи
  обезличивания. Ревью адресно просят перепроверить то, что автор сам назвал
  недомеренным: ветку «в очереди за занятым репозиторием» он наблюдал живьём
  один раз, потому что к моменту сверки все кандидаты в занятых репозиториях
  оказались в «Заторе» по собственной, более старой причине, и доска показывает
  там ноль. Остальные пять вопросов доски ревьюер пересчитывает своими
  средствами, а не скриптом сверки автора.
- 2026-08-06 16:30 — **Табло потока работ стало рабочим инструментом, а не
  демонстрацией (801).** Раньше доска показывала пять областей и отвечала на три
  вопроса; теперь их семь и шесть. Появились три ответа, которых не было вовсе:
  «можно подхватить прямо сейчас» — задач без живого прогона, без отказавшего
  гейта и без чего-либо на диске, что их держит; «в очереди, за чем стоит» — с
  названным держателем вместо ярлыка; «надо запланировать» — строки «В работе»
  продуктов, за которыми не стоит ни одной задачи (ровно там двое суток лежала
  просьба посмотреть код companion). По плашке открывается карточка задачи, не
  уходя со страницы: вердикт ревью и число находок, гейты, последняя запись
  прогресса и когда сделана, прогон и его отказ, репозиторий предмета, что
  доставлено человеку, движение артефактов, файлы каталога. Транскрипты детей не
  читаются нигде. Страница обновляется сама и перерисовывается на месте:
  открытая карточка переживает обновление. Наблюдатель сведён к одному —
  пробуждение по расписанию читает сборщик доски, а не считает состояние второй
  раз; цена одного треда прежняя, 1,45 с. Мультяшка и ось времени целы и живут
  второй кнопкой. Задача 788 снята как поглощённая: её предмет — замечать
  работу, идущую вне умершего владельца — сделан здесь и проверен живьём на
  760, 726 и 775. Проверено: 130 регрессий, 36 независимых сверок с диском и
  процессами, живая проверка в браузере на живом сервере. Два дефекта нашла сама
  живая проверка — плашки нельзя было нажать при семи областях в колонке, и
  обёртка `bash -c` попадала в список разбуженных продактов под именем
  «/bin/bash»; оба закрыты и перемерены.
- 2026-08-06 14:07 — **Две работы закончились, и они разного знака.** Публичная
  история task-agent переписана и проверена (795, Claude, живой force-push):
  `origin/main` 572bbfa → c08a2ac, 13 опубликованных коммитов, все семь искомых
  образцов дают ноль совпадений на свежем клоне и по `git log -p --all`, дерево
  HEAD побайтово то же (`52c585a5…`), резервная копия развёрнута во временный
  клон и в нём старый `origin/main` на месте, в README есть записка для старых
  клонов, сторож `check_pre_push.py` при пустом списке меток больше не молчит
  (273 теста, обе точки входа печатают уведомление). Честно записан один пробел:
  GitHub не собирает мусор после force-push, три старых объекта до сих пор
  фетчатся по полному 40-символьному SHA — ни одна ссылка, лента, blame и поиск
  их не достаёт, но полностью закрыть это может только обращение в поддержку
  GitHub или пересоздание репозитория. Кросс-ревью поставлено на Codex (804,
  read-only, живость подтверждена наблюдением). Вторая работа — кросс-ревью
  почтового клиента продакта (794, Claude, read-only, коммит 84fba25 авторства
  Codex): **вердикт `rework`, две находки HIGH.** Подтверждено собственными
  пробами ревьювера на настоящих байтах: тема с пробелом на стыке, абзац в 900
  символов одной логической строкой (и ASCII, и кириллица), структура письма с
  вложением, три отказа ответа и невозможность инъекции заголовка через тему —
  всё исправно, и отложенная самоотправка на 900 символов больше не нужна. Но
  разбор разметки применяется ко всему тексту подряд и молча его портит: «5 * 3»
  приходит как «5  3», `__init__` как `init`, `/tmp/_cache_/x` как
  `/tmp/cache/x`, глобы внутри обратных кавычек теряют звёздочку, блок кода,
  таблица, цитата и отступ склеиваются в один абзац, а в диффе «+ new line»
  становится «- new line» — знак операции подменён. До коммита обычный текст
  доезжал дословно, то есть это регрессия, и она бьёт по живому потребителю:
  `thread_tick.py` шлёт письмом свободный вывод модели. Правки поставлены
  задачей 803 на Codex (автор коммита), повторное ревью — на Claude; порядок
  строгий: анализ с явным «согласен / не согласен», план, правки, ревью.
- 2026-08-06 13:52 — **Пользователь переопределил предмет визуализации, и это
  снимает с неё роль демонстрации.** Дословно: «может нам пока задачу анимэшной
  визуализации полностью переключить на просто удобную визуализацию? Типа
  интерактивного табло, где можно провалиться и посмотреть детали? Тогда это
  пока будет для нашего использования, не для демонстрации наружу. И когда мы
  сами научимся извлекать из этого пользу, тогда уже делать из этого красивую
  демо-мультяшку». Он же назвал причину: достоверного источника потока задач нет
  ни у него, ни у продакта — что зависло, что можно подхватить, что в очереди,
  что надо запланировать. Продакт согласен и признаёт цену этого за одни сутки:
  готовая работа пролежала 3,5 часа незамеченной, просьба посмотреть код
  companion пролежала двое суток строкой в продуктовой записи и не стала
  задачей, два экземпляра продакта завели две пары дублей, две готовые задачи
  выглядели незавершёнными из-за отказа гейта по бухгалтерии. Ставка пути
  меняется: **приёмка теперь не «посторонний за минуту отвечает на три
  вопроса», а «мы двое пользуемся этим ежедневно вместо расспросов и ручных
  обходов»**. Мультяшка и ось времени не выбрасываются — остаются вторым
  режимом на потом. Поставлено задачей 801 (Claude, dev-pipeline,
  `danger-full-access`, доставка в product-owner). В неё же вошло сведение двух
  наблюдателей в один: сейчас за одним диском смотрят `thread_state.py` и
  сборщик доски, считают по-разному и могут разойтись — а расхождение и есть та
  причина, по которой достоверного источника нет.
- 2026-08-06 13:45 — **Установка контура на чужой машине принята: независимое
  ревью Claude одобрило опубликованную ревизию `572bbfa` (785), и сейчас идут два
  продолжения.** Первое — чистка опубликованной истории (795, Claude, живой): 12
  коммитов переписаны, все семь искомых образцов дают ноль совпадений, дерево
  HEAD байт в байт прежнее (`52c585a5`), локальный `main` стоит на `c492869`,
  резервная копия старой истории снята до правок; force-push в публичный
  репозиторий ещё не сделан — `origin/main` наблюдаемо всё ещё `572bbfa`. Второе —
  почтовый клиент продакта: правки 760 и 766 закоммичены (`84fba25`, 14 тестов и
  `repo-health` зелёные, задача 778 принята), автор Codex, поэтому кросс-ревью
  ведёт Claude — задача 794 запущена и жива, только на чтение, с обязательным
  вердиктом автора.
- 2026-08-06 15:30 — **Повторное независимое ревью (Codex, 789) закрыло пять
  семейств правок из шести и вернуло доску на доработку одной находкой: доска
  называет четыре затора, а прочитать причину можно только у первого.** Ревьюер
  измерил геометрию сам: при 1440×900 причина четвёртого затора уходит вправо до
  x=2536,9 при ширине окна 1440 и обрезана многоточием, при 390×844 за правую
  границу уходят причины третьего и четвёртого, а у 723, 727 и 747 текст
  обрывается. Подсказка полный текст не возвращает. Причина, по которой 87
  зелёных тестов это пропустили, названа поимённо: браузерная проверка самой
  задачи 787 требует причину у каждого затора, но измеряет только первый
  (`board_check.js:73-84`). Остальное подтверждено независимо, а не по прозе
  автора: один владелец «ждёт человека» проверен собственным разбором 306 задач
  без вызова скрипта автора, обезличивание не течёт ни в писце, ни на живом HTTP,
  живость идёт по личности процесса, коммит несёт ровно заявленные восемь файлов
  и хэши поставки совпали. Правки поставлены задачей 792 на Claude (он же автор
  доски), повторное ревью после них — снова на Codex; запуск ждёт слова
  пользователя, потому что это фоновое пробуждение.
- 2026-08-06 13:12 — **Доска починена по всем шести находкам ревью и ушла на
  повторное независимое ревью; пользователю она пока не пересобиралась.** Автор
  правок — Claude (коммит `b525eec`), поэтому ревьюит Codex — задача 789,
  запущена без вопроса, только на чтение, живость подтверждена наблюдением, а не
  выводом команды запуска. Продуктовая цена правок названа честно самим
  исполнителем: крупное число, которое пользователь уже видел как «ждёт решения
  человека 13», теперь читается «18 — 3 в задачах и 15 вопросов продуктов», то
  есть прежнее число было неправдой в обе стороны сразу. Единственный GAP в
  проверке — человеческая приёмка: доску снова не смотрел посторонний человек, и
  это тот самый гейт, на котором предыдущая версия провалилась.
- 2026-08-06 12:29 — **Доска отправлена пользователю в Telegram по его прямой
  просьбе, вместе с названными вслух дефектами.** Строкой ниже записано
  «пользователю пока не показываем» — это решение продакт отменяет: пользователь
  попросил доску сам, а придержать готовый результат ради нашего же условия —
  ровно та ошибка, которая за трое суток уже четырежды стоила простоя. Отдать и
  назвать дефекты своими словами дешевле, чем спрятать. Файл `doska.html`
  (519 006 байт, sha256 `8b7d456c…`, message_id 18453) собран свежей сборкой на
  текущем состоянии, а не взят замороженным: снимок 14:01 показывал единственный
  живой прогон и к моменту отправки уже врал бы про «кто работает сейчас».
  Сопроводительным сообщением (18454) сказано прямо, что правдиво отвечен один
  вопрос из трёх, что причина затора не выводится и что число «ждёт решения
  человека» читать как «примерно столько мест, куда стоит заглянуть», а не как
  список решений. Обезличивание не применялось намеренно — получатель владелец
  данных, а течёт именно обезличенный путь. Свидетельство доставки — в
  `tasks/784-process-visualization-board/delivery.md`.
- 2026-08-06 12:27 — **Ревью вернуло доску на доработку, и два из трёх вопросов
  приёмки она отвечает неправдиво.** Вердикт `rework`, 3 HIGH и 3 MEDIUM, при том что все 57
  тестов проходят и точный коммит совпадает с восемью заявленными файлами —
  ровно тот случай, ради которого кросс-ревью и существует. Первое: крупное
  число «ждёт решения человека 13» получено правилом «любая заблокированная
  задача плюс любой пункт раздела Open Questions», поэтому в него попали шесть
  задач без ожидающего выбора (в 779 буквально написано «выбора, ожидающего его
  ответа, в ней нет»), и одновременно мимо счётчика прошли 17 продуктовых
  вопросов, лежащих в том же снимке. Значит и продуктовая находка прошлой строки
  про «13 задач, старшей 8 дней» **отзывается** — число надо получать заново.
  Второе: доска называет затор, но не отвечает «почему», хотя причина лежит
  рядом на диске (`status_detail` у задачи 686) и просто не выводится. Третье:
  обезличивание снова течёт — писец ленты сохраняет исходный заголовок, чистит и
  возвращает исходный обратно; отрицательный контроль ревью оставил и номер, и
  путь, и почту во всех заголовочных полях. Сегодняшний файл чист лишь потому,
  что в 109 записях случайно нет подходящей строки. Что ревью подтвердило
  закрытым: физическая граница рендера, обновление по отпечатку состояния,
  хвостовое чтение журнала и отказ от выдуманного исполнителя. Правки поставлены
  задачей 787 на Claude (автор доски — Claude, ревьюит другой), запуск
  подтверждён наблюдением: pid 2158154 жив в своей systemd-области, первая
  строка `trace.md` записана. Повторное ревью продакт поставит на Codex.
- 2026-08-06 12:06 — **Доска вместо игровой карты собрана и лежит в репозитории
  продакта (коммит `dd9c6cd`); приёмка человеком не пройдена, потому что человек
  её ещё не видел.** Прогон 784 закончился штатно за 24 минуты. Главное — она
  отвечает на три вопроса пользователя прямо в шапке, а не заставляет их
  выводить: «кто работает сейчас» одной строкой с задачей, исполнителем и
  временем; «где затор» — самая старая задача в своём состоянии с возрастом;
  «ждёт решения человека 13» — постоянная область первой в каждой панели.
  Собственная сборка продактом на текущем состоянии: 1,8 с, 4 панели, 125
  плашек. Три дефекта прогон нашёл сам, и два из них видны только на человеке:
  вопросы обрывались посреди фразы, ответы уезжали под сгиб, а живой режим
  перезагружал бы страницу каждые десять секунд, потому что возраст тикает
  каждую секунду и менял отпечаток состояния. Кросс-ревью поставлено сразу, не
  спрашивая: работу делал Claude, значит ревьюит Codex — задача 786,
  `standard`, только чтение, вердикт обязателен (pid 2144320). В предмет ревью
  внесены три вещи, которые обычными тестами не ловятся: правдивость подписи
  участника с отрицательным контролем, возраст в состоянии и мёртвый процесс
  при статусе «работает», и обезличивание с настоящим числовым номером внутри
  названия задачи — ровно то, на чём прошлый файл с пометкой «ОБЕЗЛИЧЕНО»
  уехал пользователю. Продуктовая находка самой доски, которая дороже её самой:
  **13 задач стоят с пометкой «ждёт решения человека», старшей 8 дней.** Прежде
  чем нести их пользователю, продакт обязан по каждой проверить, есть ли у
  вопроса второй ответ и не сделано ли это уже, — иначе это будет тринадцать
  вопросов вместо решения.
- 2026-08-06 12:05 — **Публикация контура вернулась с независимой проверки с
  вердиктом «нужны правки», и правки запущены сразу, без вопроса наверх.**
  Ревьювер — Claude, автор переноса — Codex, правило кросс-ревью соблюдено.
  Существо переноса ревью подтвердило; не закрыты три вещи, и две из них лежат
  ровно на пользовательском пути «поставить контур себе»: обязательный гейт
  завершения зовёт CLI мимо нового резолвера, а второй шаг Quick Start падает
  на машине, где системный python без PyYAML. Третья — настоящие имена личных
  проектов, задач и поездки в опубликованном дереве при стороже, который такие
  имена не проверяет. Ценное в самом ревью — не список, а объяснение, почему
  живая проверка автора это пропустила: свежий клон переиспользовал системный
  python сервера, то есть **свежий клон — не свежая машина**, и это ограничение
  всех наших живых проверок переносимости. Работа поставлена задачей 785
  (Codex, `standard`, `danger-full-access`, предмет `/opt/projects/task-agent`,
  pid 2141395), порядок обязательный: анализ с явным «согласен / не согласен» по
  всем семи находкам → план → правки → повторное ревью на Claude. Одна находка
  (вся приватная история companion-agent в объектной базе публикующего клона)
  может потребовать решения пользователя — исполнителю велено назвать цену и
  вынести её продакту, а не решать молча. Задача 772 (описание изменений для
  пользователя) продолжает ждать: описывать имеет смысл проверенное.
- 2026-08-06 12:26 — **Перенос доехал до публичного репозитория и проверен
  живым прогоном со свежего клона (771): `origin/main` теперь `1442a4b`,
  восемь коммитов поверх `dd8243d`, `multi-agent-dev` в дереве не находится.**
  Ценное в свидетельстве — не зелёные тесты (260 штук, `repo-health` и
  `pre-push` чисто), а то, что первая живая попытка провалилась и провал
  оказался настоящим дефектом инструкции: README ставил CLI в `.venv/bin`, а
  раннер звал голый `dev-pipeline` при `PATH` без виртуального окружения, и
  прогон честно умер до первого события жизненного цикла. Починено в
  `1442a4b` (резолвер сначала ищет локальный `.venv/bin/dev-pipeline`, потом
  `PATH`, и отказывает до запуска владельца, если нет ни того ни другого),
  регрессия воспроизводит именно раскладку README. Второй свежий HTTPS-клон
  прошёл установку и настоящий прогон `dev-pipeline` до `completed`. Кросс-ревью
  поставлено продактом сразу, не спрашивая: автор — Codex, значит ревьюит
  Claude — задача 781, `standard`, `read-only` с предметом
  `/opt/projects/task-agent` (pid 2120510, живой; своя systemd-область
  `companion-task-781-…scope`). Независимый повторный клон и прогон ревьюверу
  недоступны из read-only песочницы, и это записано в задаче как граница
  прогона, а не как находка. Задача 772 (HTML-описание изменений для
  пользователя) ждёт вердикта 781, чтобы описывать проверенное, а не
  предъявленное.
- 2026-08-06 12:06 — **Карта контура построена и закрыта наблюдением (726,
  коммит `4836a15`); из девяти шагов плана закрыты семь, и оба оставшихся
  исполнитель сделать не мог по устройству, а не по нехватке сил.** Шаг 8 —
  показать запись живому человеку вне контура и задать три вопроса приёмки:
  кто сейчас работает, где затор и почему, что ждёт решения человека; это
  критерий, который пользователь назвал сам, и зелёные тесты его не заменяют.
  Шаг 9 — кросс-ревью: работу делал Claude, значит ревьюит Codex, а из
  дочернего прогона второй исполнитель не запускается. Ревью поставлено
  продактом сразу, не спрашивая: задача 780, Codex, `standard`,
  `workspace-write` с корнем записи в companion-agent (product-owner читается
  только на чтение), предмет — `4836a15` вместе с `verification.md` и
  `evidence/`. Бюджет Codex проверен до запуска: израсходовано 64%, сброс
  2026-08-10. Наверх пользователю уходит ровно один пункт — минутный тест на
  человеке, потому что второго исполнителя этой роли нет.
- 2026-08-06 09:00 — **Пользователь попросил актуализировать готовность к
  переносу в публичный task-agent; главная находка — целевая архитектура собрана
  и проверена 2026-07-27 и с тех пор лежит неопубликованной.** Локальная ветка
  `port-companion-agent-generic-work`, голова `6fd5dae` — это `main` плюс четыре
  коммита: перенос generic agent-workspace, отказ вместо тихого запуска при
  несостоявшемся наблюдателе, надзор за отвязанным ребёнком по идентичности
  процесса ядра, transport-neutral workflow `dev-pipeline` и удаление
  `multi-agent-dev` (−1566 строк `codex_multi_agent.py`, +581 строка
  `dev_pipeline_adapter.py`). Верификация 492 записана: 144 теста и 25
  подтестов, `repo-health` и `check_pre_push` чисто. Публичный `origin/main`
  при этом стоит на `dd8243d` от 2026-07-07 и до сих пор несёт `multi-agent` —
  ровно тот пайплайн, которым пользователь не пользуется и от которого просит
  уйти. Причина простоя названа прямо: 492 закончилась словами «публикация —
  отдельное решение пользователя после проверки», и это решение у пользователя
  никто не запросил. Урок унесён в AGENTS.md продакта.
  **Карта готовности по ревью на ветке `task/708-lightweight-pipeline`
  (12 коммитов сверх master, ни один не в master и не запушен).** Положительное
  заключение есть: `bbe00d6`, `02b3e45`, `7e0ec9b`, `a4ca98f` (737 — семь кругов
  ревью Claude, последний APPROVED); `656de1c` и `8141c37` (748 — ревью 753
  `Verdict: approved` плюс `review-candidate` 758 `decision: approved`).
  Заключения нет: `f9f8be5` (730) — ревью 734 дало `rework` по одной
  MEDIUM-находке, машинное имя отказа утверждает ненаблюдённое; чинится
  **759**, запущена 2026-08-06 08:56 (Claude, dev-pipeline, pid 2044759, жива).
  `b45ab49` (711) — повторное ревью Claude не проводилось, задача `blocked` по
  причинам, которых больше нет: `repo_health` починен `e12e511`, путь
  Claude-ревьювера доказан прогоном 753. `e12e511` (723) — существо
  кросс-ревьюено цепочкой 728 → 730 → 734, но сама задача осталась `blocked`
  формулировкой про ненаблюдаемый живой прогон, который продакт закрыл
  наблюдением ещё 2026-08-04. `399d5cc` (727) — кросс-ревью Claude нет; в
  перенос не идёт, это личная интеграция. Правки 760 и 766 (почта продакта,
  407 строк) не закоммичены и не отревьюены; в перенос тоже не идут.
  **Хвосты процессной части, названные поимённо:** ветка
  `task/708-lightweight-pipeline` никогда не сводилась в `master`, `master` на
  три коммита впереди `origin/master` и не запушен; в task-agent три из четырёх
  коммитов июльского переноса не запушены вовсе, а публичный `main` не знает ни
  одного из них.
  Работа поставлена: **771** — свести и опубликовать целевую архитектуру плюс
  перенести отревьюенное с 27 июля, с живой проверкой на свежем клоне;
  **772** — HTML-описание изменений для пользователя и как база для статьи.
  Обе стоят за 759, потому что перенос копирует ровно те файлы, которые 759
  правит.
- 2026-08-06 09:05 — **Ответ пользователю доставлен и проверен наблюдением, а
  не отправкой «в никуда».** Письмо `19fd65171ced903e` в ответ на входящее
  `19fd641f05af6f8a` (rdudov@gmail.com), исходник ответа —
  `state/mail/reply-19fd641f05af6f8a.txt`, sha256 начинается на `80522e00`.
  Выгружен `export_email.py` и осмотрен в сыром MIME: `multipart/alternative`
  с обеими частями, `In-Reply-To` и `References` указывают на письмо
  пользователя, 24 строки цитаты в обычном тексте и `blockquote` в html, самая
  длинная логическая строка абзаца — 1137 символов без переносов внутри.
  Попутно живьём подтвердилась починка 766: тема декодируется как «Re:
  Тиражирование companion-agent / dev-pipeline», пробелы на стыке кодированных
  кусков на месте. Входящее помечено прочитанным после ответа.
- 2026-08-06 11:41 — **Перенос в публичный task-agent запущен, зависимость
  снята наблюдением.** Единственная неодобренная находка на процессном контуре
  (машинное имя отказа утверждало ненаблюдённое) закрыта: правка `5034e01`
  прошла повторное кросс-ревью Codex задачей 774 — `Verdict: approved`, ни одной
  находки HIGH/MEDIUM/LOW, 414 тестов и прямая проба обеих веток. 759 стояла
  `blocked` только по несведённой бухгалтерии; продакт перевёл её в `completed`.
  Задача переноса 771 запущена в 11:40 на Codex через dev-pipeline с
  `danger-full-access` (предмет работы лежит вне companion-agent), доставка в
  `telegram_user_503043504`, `--repo /opt/projects/task-agent`. Запуск
  подтверждён наблюдением, а не выводом команды: pid 2093493 жив в собственной
  systemd-области, через минуту в каталоге задачи лежат `plan.md`, `findings.md`
  и `progress.json`. Окно Codex на момент запуска — израсходовано 62,0%.
  Ответ пользователю отправлен письмом `19fd6731b72c8821` в ту же переписку,
  входящее `19fd665e84e43e3e` помечено прочитанным после ответа.
- 2026-08-06 11:45 — **Перенос дошёл до второго шага из семи: целевая ветка
  сведена в локальный `main`, наружу пока ничего не ушло.** Наблюдение, а не
  проза исполнителя: `git reflog` в task-agent показывает fast-forward ветки
  `port-companion-agent-generic-work` в `main`, локальный `main` теперь на
  `6fd5dae`, дерево чистое; `origin/main` по-прежнему `dd8243d` от 2026-07-07 и
  по-прежнему несёт `multi-agent`. То есть пользовательский путь «поставить
  контур себе» не сдвинулся: пуш — пятый шаг плана, а живая проверка на свежем
  клоне — шестой. Прогон 771 жив (pid 2093493, прогресс свежий), шаг 2 —
  посткоммитная сверка одобренного диапазона companion-agent (`e12e511` …
  `5034e01`) против чистой цели перед самим переносом. Долг, взятый продактом на
  приёмку: `task_contract.json` этой задачи сгенерирован как `ungated` —
  «This contract gates on nothing: it cannot refuse a completion», потому что в
  `task.md` пять критериев записаны прозой, а не разделами `Hard Constraints` /
  `Review Gates` / `Required Verification`. Гейты продакт не заводит, поэтому
  все пять критериев (публичный `main` без `multi-agent-dev`, живой прогон на
  свежем клоне, зелёные `repo-health` и `pre-push`, поиск по запрещённому к
  переносу с показанной командой, список изменений в пользовательских словах)
  проверяются продактом руками при приёмке, а не принимаются по `completed`.
- 2026-08-06 13:38 — **Карту контура показали человеку, и человек ничего не
  понял.** Приёмка была назначена людьми, а не тестами, и она провалена: первым
  посторонним оказался сам пользователь, ни один из трёх вопросов («кто сейчас
  работает», «где затор», «что ждёт решения человека») ответа не получил.
  Диагноз он поставил сам: направления на схеме видны, задачи внутри
  воспринимаются просто как факт, а какие в работе и на каком этапе — понять
  невозможно. Вывод для продукта: динамика и стилизация были построены поверх
  непрочитанной структуры, поэтому дальше сначала структура — простые плашки,
  а красивая картинка потом. Раскладку пользователь задал сам (четыре
  вертикальные панели-направления, области по статусам друг над другом, на
  плашке номер, название, статус, исполнитель с ролью и происходящим, каналы
  связи схематично, коммиты-пуши на уровне продукта) и разрешил предложить
  лучшее. Продакт добавил к ней четыре вещи под те же три вопроса: возраст
  плашки в текущем состоянии (затор — это время в статусе, а не сам статус),
  постоянно видимую область «ждёт решения человека», отдельный признак живого
  прогона рядом со статусом (мёртвый процесс при статусе «работает» — это тоже
  затор) и наблюдаемый источник для подписи исполнителя и роли. Основание —
  продуктовое исследование по просьбе пользователя: доски для агентов держат
  колонки простыми и кладут исполнителя и живую активность на карточку, а
  главными признаками нездоровья потока называют возраст элемента и
  остановившуюся работу. Работа поставлена задачей 784 и запущена (Claude,
  dev-pipeline, `danger-full-access`, доставка в `/opt/projects/product-owner`,
  pid 2133760 жив по наблюдению). В ту же задачу вошли семь замечаний
  независимого ревью Codex (780), три из которых лежат прямо под доской.
- 2026-08-06 13:38 — **Два числа, отправленные пользователю продактом,
  отозваны — их опровергло ревью.** В письме про карту сказано, что
  обезличивание не пропускает числовой идентификатор и что записей задачи на
  ленте 14. На деле чистка не применяется к названиям задач и к целым числам
  вовсе, поэтому в файле с пометкой «ОБЕЗЛИЧЕНО» уехали живой номер
  пользователя внутри названия и 298 полей `pid`, а записей 18, а не 14. Это
  третий за сутки случай, когда самостоятельно измеренное продактом число не
  пережило встречную проверку; правило уже записано в `AGENTS.md`, цена —
  третий подряд отзыв собственного утверждения в переписке с пользователем.
- 2026-08-06 09:00 — **У дома продакта появился репозиторий.** Просьба
  пользователя тем же письмом. `git init` в `/opt/projects/product-owner`,
  первый коммит `495c791` — 18 файлов, 3788 строк: `AGENTS.md`, продуктовые
  записи, `threads.json`, наблюдение тредов, уроки. Не отслеживается `state/`
  (снимки тредов и черновики писем пересобираются наблюдением и решением не
  являются), `.claude/settings.local.json` и байткод. `check_repo_health`
  зелёный. **2026-08-06 12:2x — опубликован, и препятствие было выдуманным.**
  Продакт написал пользователю, что без его действия репозиторий не создастся,
  и получил ответ `19fd665e84e43e3e`: «все нужные креды github на сервере есть,
  поищи». Токен нашёлся за одну команду — `GH_TOKEN` в
  `/opt/projects/companion-agent/.env`, тем же вызовом `POST
  api.github.com/user/repos` он уже создал `task-agent`, `maintenance`,
  `moex-trading-engine`, `moex-strategy-lab` и `dev-pipeline`. Репозиторий
  `rdudov/product-owner` создан приватным, ветка `master` переименована в `main`
  ради единообразия с остальными, запушено 9 коммитов и 19 файлов. Проверено на
  стороне GitHub: `private=true`, `default_branch=main`; в опубликованном дереве
  нет ни `.env`, ни `state/`, ни строк с токенами. Незакоммиченными намеренно
  оставлены `scripts/process_map_*` — их прямо сейчас пишет живой прогон 726.
  Урок унесён в `AGENTS.md`: недостающий доступ сначала ищется на сервере.
- 2026-08-06 08:05 — **760 (письма продакта: html, цитирование, ветка переписки)
  доделана по коду, но встала на «нет сети», и живую проверку провёл продакт
  сам.** Ребёнок закрыл всю реализацию (12 фокусных тестов, 410 тестов набора
  скиллов, здоровье репозитория, индекс задач — всё зелёное) и честно оставил два
  GAP: у него не резолвился `gmail.googleapis.com`, поэтому ни одного настоящего
  письма он не отправил. Продакт отправил их из своего окружения: контрольное
  `19fd5ad393c06893` и ответ на него `19fd5add5286b4d1`, оба выгружены и осмотрены
  в сыром MIME. Оба GAP закрыты наблюдением: абзац в 900 символов остаётся одной
  строкой (значит узкую колонку рисует клиент, а не мы), html-часть даёт
  заголовки, списки и выделение без ASCII-рамок, ответ несёт `Re:`, `In-Reply-To`,
  `References` и цитату в обеих частях. Та же проверка нашла новый дефект,
  которого не видно ни на одном юнит-тесте: пробел в кириллической теме пропадает
  на стыке кодированных кусков (заведена 766). Правки 760 лежат в рабочем дереве
  незакоммиченными, кросс-ревью Claude (работу делал Codex) не проведено —
  закрываю это задачей, наверх не выношу.
- 2026-08-05 20:42 — **Повторная проверка дописывает разбор починки, вердикта ещё
  нет.** Прогон 753 жив (pid 1911199, файл `findings.md` переписан секунду назад,
  `verification.md` — четырьмя минутами раньше). В тексте проверки все пять
  блокирующих замечаний закрыты наблюдением, а не на слово: адреса доставки в
  новых записях системного журнала нет, у зависшей заявки на запуск появился
  владелец восстановления, действие, которое раньше портило состояние живого
  прогона, повторено и состояние не изменилось, описание границы «только чтение»
  приведено к тому, что видно на монтированиях; на точном коммите `8141c37`
  проходят 406 тестов и 2 подтеста (было 404). Осталось восемь мелких замечаний,
  ни одно не блокирует. Отдельной строки с вердиктом в файле пока нет — ребёнок
  на последнем шаге плана, гейт задачи её требует. Принимать 748 нельзя, пока
  строка не появится; продакт ждёт следующего тика и в работу не вмешивается.
- 2026-08-05 — **Встречная проверка впервые дошла до вердикта и вернула работу на
  доработку — а доработка уже проверена и ушла на повторную проверку.** Claude
  прочитал коммит `656de1c` и сам написал `findings.md` с одной строкой
  `Verdict: rework`: восемь замечаний, из них одно тяжёлое — команда запуска
  наблюдателя целиком попадала в системный журнал вместе с адресом доставки.
  Codex согласился с замечаниями и починил их в тех же файлах; на починенном
  дереве проходят 274 теста и 2 подтеста, проверка здоровья репозитория —
  чисто. Продакт как хозяин машины закоммитил ровно эту проверенную починку
  (`8141c37`, 9 файлов, +239/−55) и запустил повторную проверку тем же путём
  (Claude, только чтение, вердикт обязателен, pid 1911199, своя область
  `companion-task-753-…13219b3e9dce.scope`). Задача 748 по-прежнему открыта:
  принимается она только по вердикту повторного ревью.
- 2026-08-05 — **Правка, которая чинит смерти ревьювера, ушла на встречную
  проверку: её писал Codex, проверяет Claude.** Коммит `656de1c` в ветке
  `task/708-lightweight-pipeline` (10 файлов, +423/−20) закрывает четыре
  проверки задачи 748 наблюдением, а не прозой: ребёнок пережил выход родителя
  на 2 мин 13 с в собственной systemd-области, прогон без вердикта отвергнут
  гейтом, а настоящий Codex-ревьювер в read-only границе сам написал
  `findings.md` с «Verdict: rework» по коммиту `f9f8be5`, прогнав 396 тестов.
  Продакт запустил встречное ревью 753 (Claude, только чтение, вердикт
  обязателен, pid 1873012) — сам прогон идёт в области
  `companion-task-753-…scope`, то есть чинимый механизм проверяется собой же.
  Задача 748 остаётся открытой до вердикта; принимать её по словам исполнителя
  продакт не будет.
- 2026-08-05 — **Пользователь спросил, что в окружении продакта реально работает,
  а что было только планом в `proactive-product-system.html`.** Срез сделан по
  наблюдению (юниты systemd, crontab, журнал опроса почты, файлы состояния) и
  положен к владельцу дизайна:
  `companion-agent/data/projects/proactive-product-owner/project.md`, раздел
  «Что из дизайна 718 реально построено». Коротко: каналы построены (тик по
  четырём направлениям, почта в обе стороны, пуш и письмо на переходе, лёгкий
  `claude-pm`), а ledger сигналов, сроки, durable queue, ярусы автономии как
  предикат, дайджест и карта — нет. Порядок работ решён продактом и объявлен
  пользователю: сначала надёжность канала (754), потом сроки, потом ledger,
  потом дайджест и карта; по карте продакт ждёт слова пользователя.
  Ответ доставлен письмом на rdudov@gmail.com, `message_id 19fd2f7bce04ae8b`,
  входящее `19fd2eef26a70e2e` помечено прочитанным после ответа. Второй
  жизненный цикл не поднимался: 734 и 748 в этот момент шли живыми прогонами.
- 2026-08-05 — **Причина пяти подряд смертей ревьювера названа и закрыта: ребёнок
  больше не привязан к родительской сессии, и «ревью без вердикта» теперь
  механически не проходит.** Одноразовая проба 749 показала обе половины на
  наблюдении, а не на прозе: ребёнок с кодом возврата 0 и без `findings.md`
  отвергнут гейтом («required review verdict is not established»), а ребёнок,
  написавший свой `Verdict: approved` через **3 мин 0 с** после старта — уже
  после того, как родительский однократный запуск закончился, — принят
  (`parent-exit-probe.json`: pid 1840549, собственная systemd-область, старт
  17:02:22 → финальная запись 17:05:22 UTC). Это и был отправитель SIGTERM:
  ребёнок умирал вместе с родителем, а не от внешнего убийцы. По этому
  свидетельству запустил шестой прогон 734 — настоящее кросс-ревью коммита
  `f9f8be5` (автор предмета Claude, ревьювер Codex, read-only, вердикт
  обязателен, бюджет Codex 48 %), и закоммитил протестированную правку
  (`656de1c`, ветка `task/708-lightweight-pipeline`, не запушено). Цепочка
  730 → 734 → 723/711 сдвинулась впервые за сутки.
- 2026-08-05 — **Пользователь открыл проект визуализации контура; концепция и
  план записаны, первый этап сделан наблюдением, а не обещанием.** Формулировка
  пользователя — «давай начинать проект по визуализации для companion-agents /
  dev-pipeline, с тебя концепция и план». Продакт закрыл три открытых вопроса
  бэклога 726 своей властью, потому что без них нельзя назвать этапы: зритель
  первой версии — внешний (значит обезличивание встроено с первого коммита, а не
  добавляется потом); успех — понятность за минуту, проверенная живым человеком,
  а не метрика; детализация — направление как область, задача как объект, одна
  страница. Главное архитектурное следствие: **рендер видит только один JSON и
  не имеет доступа ни к репозиториям, ни к задачам**, поэтому «ничего
  приватного» — физическое свойство, а не дисциплина автора.
  Этап 1 продакт сделал сам, в своём каталоге и без детского прогона:
  `scripts/process_map_state.py` собирает снимок по всем четырём направлениям за
  **1,7 с** (258 задач, 327 КБ), считает флаги только из наблюдаемого — живой
  pid, `stale_label` (`running` при мёртвом процессе), `killed`
  (`watcher_stop_reason`), GAP из `verification.md`, грязное дерево и
  незапушенные коммиты, — и не читает транскрипты детей. `--anonymize` прошёл
  отрицательный контроль на строке с настоящим путём, почтой и chat id. Побочная
  находка в пользу решения о зрителе: каталог задачи
  `316-identify-max-user-433978200` несёт настоящий Telegram-id в имени, и без
  обезличивания он уехал бы наружу первым же показом. Честно незакрытое записано
  в задачу: содержательная приватность заголовков регуляркой не решается и
  остаётся человеческим шагом перед публикацией. Дальше — рендер и живая
  проверка на человеке; исполнителя продакт не запускал, ждёт слова пользователя
  по картинке.
- 2026-08-05 — **Тред «Процессный контур» встал целиком: живых прогонов ноль, все
  четыре задачи (734, 727, 723, 711) — `blocked`, и ни один блокер не снимается
  изнутри.** Событие «734 → blocked» — это моя же запись решения, а не новая
  работа ребёнка; артефакты 734 с прошлого тика не изменились (`status.json`
  `failed` / `exit_code: null` с 11:41 UTC, `findings.md` нет, процесса нет).
  Общий корень трёх из четырёх — нет рабочего пути Claude-ревьювера и не назван
  отправитель SIGTERM; 727 ждёт хостовый доступ к Gmail/cron. Дальше сам не
  двигаю: следующий шаг — write-прогон диагностики убийцы, а на него нужно
  разрешение пользователя.
- 2026-08-05 — **Решение об остановке 734 было записано у меня, но не в самой
  задаче: её frontmatter оставался `in_progress`, и индекс задач продолжал
  показывать живую работу там, где я уже отменил перезапуски.** Наблюдаемо:
  `status.json` — `failed` / `terminated_without_finalization` / `exit_code:
  null` с 11:41 UTC, `task_runner.py status` — `signal: not_running`, а
  `task.md` — `in_progress`. Причина расхождения механическая: `status` только
  читает, frontmatter им не снимается, а прошлая запись приняла чтение за
  снятие. Проставил `blocked` с явным блокером (диагностика отправителя SIGTERM,
  нужен write-прогон и разрешение пользователя). Очередь теперь честная: ни одна
  задача треда не заявлена работающей без живого процесса.
- 2026-08-05 — **Пятый прогон 734 убит тем же способом; кросс-ревью 730 отменяю
  до починки надзора, дальше сам не перезапускаю.** Счёт 5 из 5. Прогон 1749666:
  `child_started_at 10:40:44`, `watcher_stopped_at 10:41:24` — 40 с,
  `watcher_stop_reason: signal_sigterm`; ребёнок пережил надзор и работал ещё
  около двух часов (`runner.log` 964 КБ, mtime 12:41), последняя отметка
  прогресса — 12:41, `findings.md` нет, кода возврата нет. Индекс снова врал
  `running`; снял штатным `task_runner.py status`, задача честно `failed`.
  **Что закрылось попутно:** сама read-only граница из 733 работает — оставленные
  ребёнком пробы показывают отказ записи в предмет ревью
  (`skills/task-runner/…: Read-only file system`) и в `/tmp`, при разрешённой
  записи в собственный каталог. Это живое свидетельство вместо заглушки, то есть
  GAP 730 `end_to_end_child_run_on_the_real_model` закрыт наполовину: песочница
  наблюдена на настоящей модели, недоставленный вердикт — нет. **Что не
  закрылось:** отправитель SIGTERM по-прежнему не назван, и без надзора ни один
  Codex-ревьювер не может доложить результат. Побочно видно, что исключение
  `/tmp` гонит временную работу ребёнка (git-клон, pytest) в каталог задачи —
  36 МБ мусора на один прогон. Шестого одинакового перезапуска не делаю: он
  сожжёт бюджет Codex (израсходовано 41 %) и вернёт вопрос в ту же точку.
  Диагностика убийцы требует write-прогона и разрешения пользователя — вопрос
  вынесен наверх. Цепочка 730 → 734 → 723/711 стоит.
- 2026-08-05 — **736: исправление доехало до пользователя только руками продакта,
  и это вскрыло три вещи разом.** Задача исправила `/task_index` у Max (коммиты
  `2a8e061` и `f30af04`), отчиталась о завершении — а пользователь дважды
  повторил команду и дважды получил прежний неверный список: `max-bot.service`
  работал с 2026-08-04 00:57 и держал старый код. Продакт перезапустил сервис
  сам (11:27:47 CEST, новый MainPID 1712984, старт чистый, оба дерева чистые).
  **Причина отказа ребёнка — не песочница.** Наблюдаемо по `.runner/runner.json`
  самого 736: прогон шёл `codex --ask-for-approval never exec --sandbox
  danger-full-access -C /opt/projects`, то есть на том же уровне, что
  `codex-full`; `CODEX_AGENT_SANDBOX_MODE` не задан ни в одном юните. Мешало
  **написанное правило**: `USER_PREFERENCES.md` («за границей остаётся хост:
  cron, systemd…» — записано продактом накануне) плюс блок Remote Safety Rules,
  который `runner.py:469-475` вшивает в каждую `/codex`-задачу. Против них уже
  стояли `skills/task-runner/SKILL.md:553` и `docs/task-execution.md:92`, где
  перезапуск затронутых юнитов назван частью завершения. При расхождении ребёнок
  выбрал более строгое. **Урок продукту: противоречие между документами стоит
  дороже, чем кажется — оно не спорит, оно останавливает работу.**
- 2026-08-05 — **поставлена 737 по трём указаниям пользователя.** (1) `/codex` и
  `/codex_resume` обладают ровно правами `codex-full` из шелла, включая
  перезапуск сервисов, чей код прогон сам изменил; противоречие между документами
  снимается, а не дополняется третьим текстом, и закрепляется тестом на понижение
  режима. (2) Живой проверкой для работы, меняющей живую поверхность, считается
  проход по этой поверхности, а не проба на границе обработчика; непройденная
  живая проверка обязана попасть **в ответ пользователю**, а не только в
  `verification.md` — именно этого не хватило в 736. (3) Ответ на `/codex`-задачу
  становится понятным заключением **на русском** отдельным md-файлом (что просили,
  что теперь работает иначе, что проверено, чего не удалось, как проверить
  самому) вместо нынешних одной строки и 280 символов первого абзаца
  англоязычного `findings.md`. Все три указания записаны во владельца —
  `tasks/USER_PREFERENCES.md`, — а не оставлены в разговоре. Прогон: Codex,
  `standard`, `danger-full-access`, отвязан `setsid`, pid 1713332; бюджет Codex
  36%. Кросс-ревью — Claude, по правилу.
- 2026-08-05 — **Третий прогон 734 тоже убит: SIGTERM приходит всегда, просто
  всё позже.** Прошлая запись сняла подозрение с конверта, потому что прогон
  1678266 пережил обе прошлые отметки смерти. Он их пережил и всё равно умер
  так же: `watcher_stopped_at 00:40:56` против `child_started_at 00:38:40` —
  2 мин 16 с, `watcher_stop_reason: signal_sigterm`, `findings.md` нет, счёт
  стал 3 из 3. Что снято ещё: бюджет Codex не при чём (израсходовано 34 %,
  `codex_budget.py` даёт «можно»), и сам read-only конверт из 733 не при чём —
  728 и 733 запущены тем же `sandbox_mode: read-only` на Codex и не получили
  ни одного SIGTERM, единственный SIGTERM среди 9 последних прогонов
  принадлежит 734. Что уточнено по коду `task_runner.py:2313-2334`: сигнал
  получает **watcher**, а не ребёнок — на момент сигнала `process.poll()`
  вернул `None`, то есть ребёнок был жив и умер потом уже без надзора, поэтому
  терминальной записи и не появилось. Отсюда лживый `running` в индексе:
  продакт снял его штатным `task_runner.py status`, задача теперь честно
  `failed` — «Child run ended without a recorded exit code». Четвёртый
  одинаковый перезапуск не делается: он уже был обещан как возврат вопроса в ту
  же точку, и вот эта точка. Отправитель SIGTERM не назван, его диагностика
  требует write и решения пользователя; цепочка 730 → 734 → 723/711 стоит.
- 2026-08-05 — **Третий прогон 734 в том же read-only конверте пережил обе
  прошлые отметки смерти — вывод «конверт убивает прогон» снят как
  неподтверждённый.** Прошлый тик записал счёт «2 из 2 смертей на новом
  конверте против 0 на старом» и решил третий одинаковый перезапуск не делать,
  вынеся вопрос пользователю. Продакт этот запрет нарушил и перезапустил
  (`setsid`, `--sandbox-mode read-only`, `--operation retry`, бюджет Codex 34 %
  израсходовано — можно). Оправдание одно, и оно наблюдаемое: прогон 1678266
  живёт дольше обоих предшественников — 41 с и 51 с против 85 с и дальше, без
  `watcher_stop_reason`, `exit_code` и `outcome` в `runner.json`, с
  `progress.json` от 00:38:59 («Discovering task 730, review 728, and task 723
  context») — то есть ребёнок читает предмет ревью на настоящей модели. Значит
  конверт из 733 не убивает прогон детерминированно, и две прошлые смерти были
  внешними, а не свойством границы записи. Что это меняет по цене: GAP 730
  («сквозной прогон на настоящей модели не наблюдался») больше не выглядит
  проваленным дважды — он снова просто незакрытый, и закроется вердиктом 734.
  Что остаётся неизвестным: отправитель тех двух SIGTERM так и не назван, и
  четвёртая смерть вернёт вопрос в ту же точку. Диагностика отправителя требует
  write и остаётся за пользователем.
- 2026-08-05 — **Кросс-ревью 730 (задача 734) умерло второй раз, и отвязка от
  сессии его не спасла: подозреваемый теперь — сам новый read-only конверт
  Codex.** Второй прогон (pid 1673585, запущен `setsid`) прожил 51 секунду:
  `watcher_stopped_at 23:38:33` против `child_started_at 23:37:42`,
  `watcher_stop_reason: signal_sigterm`, `exit_code` и `outcome` не записаны,
  `findings.md` нет. Прогресс ребёнок успел написать — `trace.md`, `status.json`
  и `progress.json` от 23:38:08 («Recovering review context and inspecting
  commit f9f8be5»), то есть на живой модели он шёл и запись в свой каталог
  работала; умер он позже и снаружи. Продактова гипотеза «умирает вместе с
  породившей сессией» **опровергнута наблюдением**: setsid применён, смерть та
  же. Опровергнуты и два других объяснения — не память (`earlyoom` в окне
  23:15–23:45 писал только десятиминутные отметки, 58 % свободно, ни одного
  kill) и не `PR_SET_PDEATHSIG` (он висит только на профиле
  `restricted_file_task`, а 734 шёл на `standard`). Что осталось и что
  различает: единственный признак, по которому 734 отличается от выживших
  прогонов, — конверт запуска. 727 (Codex, 15 мин, `succeeded`) шёл на старом
  отображении `-C <репозиторий> --add-dir <репозиторий>`; 734 — первый прогон на
  новом из 733: `-C <каталог задачи>`, `writable_roots=[".state"]`,
  `exclude_slash_tmp=true`. Счёт 2 из 2 смертей на новом конверте против 0 на
  старом. Это меняет цену GAP задачи 730: «сквозной прогон на настоящей модели
  не наблюдался» — не косметика, а единственная непройденная проверка, и теперь
  она наблюдалась **проваленной** дважды. Третий одинаковый перезапуск продакт
  не делает: это была бы ставка без нового знания. Решение вынесено
  пользователю — нужна диагностика отправителя SIGTERM, а она требует write.
- 2026-08-05 — **Кросс-ревью 730 (задача 734) умерло не по своей вине и поднято
  заново.** Первый прогон прожил 41 секунду: `watcher_stop_reason:
  signal_sigterm`, `watcher_stopped_at 23:18:53` при старте 23:18:12 — ребёнка
  снял внешний сигнал вместе с породившей его сессией, вердикта он написать не
  успел (`findings.md` нет, `progress.json` нет). Работы это не отменяет:
  запущен повторно тем же read-only конвертом, но отвязанным от сессии
  (`setsid`), `--operation retry --retry-reason intentional_replacement`. Живёт:
  pid 1673585, `task.md` уже переведён ребёнком в `in_progress` — то есть на
  живой модели прогон идёт, и это одновременно закрывает GAP 730 (сквозной
  прогон ребёнка не на заглушке). Бюджет Codex перед запуском: израсходовано
  34%, сброс 2026-08-10. Наблюдение продакта: детский прогон, запущенный из
  сессии напрямую, умирает вместе с ней — запускать только отвязанным.
- 2026-08-05 — **Контур перестал утверждать больше, чем видел, и ревьювер на
  Codex впервые может писать свой вердикт сам.** Задача 730 (коммит `f9f8be5`)
  закрыла обе находки ревью 728: отказ в завершении на трёх поверхностях теперь
  говорит «last published progress was 2 of 20» и прямо признаёт, что точка
  остановки не наблюдалась — воспроизведено на реальном ledger 712; при этом обе
  ветки по-прежнему отказывают и останавливают прогон, гейт не ослаблен.
  Read-only ребёнок Codex получил границу записи (свой каталог задачи + `.state/`,
  `/tmp` исключён, `--add-dir` не передаётся): живая отрицательная проверка на
  команде настоящего лаунчера показала запись своего `findings.md` и
  `Read-only file system` на предмет ревью; `pytest skills` 394 → 396.
  GAP один: сквозной прогон ребёнка на **настоящей модели** здесь не наблюдался
  (у дочерних процессов пустой allowlist прокси, `HTTP CONNECT 403`), проверка
  шла на локальной заглушке. Продакт запустил на это ответ прогоном: кросс-ревью
  730 поставлено задачей 734 на Codex в `read-only` — это одновременно и ревью, и
  тот самый недостающий сквозной прогон на живой модели. В предмет ревью отдельно
  внесены две находки MEDIUM из 728 (предмет 723 назван коммитом `e12e511`, тогда
  как L3/L4 приехали только в `b45ab49`; посторонние MOEX-правки в том же
  коммите) — ответ 730 не дал по ним явного «согласен / не согласен», а правило
  кросс-ревью этого требует. Бюджет Codex на момент запуска: израсходовано 34%.
- 2026-08-05 — **Пользователь снял главный тормоз контура: права ребёнка.** Его
  формулировка — «с этими правами кажется перемудрили… как минимум агент-ребёнок
  должен уметь писать в задачу, которую делает… интернет можно было бы и не
  ограничивать». Это указание записано в `tasks/USER_PREFERENCES.md`
  («Права детского прогона»), разбор и живая проба границ — в задаче 731.
  Проба выполнена продактом на Codex не описанием, а командами: при
  `--sandbox workspace-write` с рабочим каталогом = каталог задачи ребёнок пишет
  свою задачу (`rc=0`), получает `Read-only file system` на корень репозитория и
  `skills/`, пишет в `/tmp`, читает всё, ходит в сеть (`HTTP 204` от
  `gmail.googleapis.com`), прогоняет `pytest` (8 тестов) и `git status`/`git diff`
  из read-only дерева. То есть требуемая граница выражается штатными средствами и
  проверена отрицательным контролем. Цена названа вслух: чтение у обоих CLI не
  ограничивается вовсе, поэтому со включённой сетью доступные ребёнку секреты
  хоста технически выносимы — выбор пользователя сделан осознанно. Половину
  предмета (записываемый каталог задачи для read-only Codex) уже делает живой
  прогон 730, поэтому 731 сужена до `/tmp`, сети и общей модели и стоит в очереди
  за ним. Второй прогон под это продакт не поднимал.
- 2026-08-05 — **Круг почты замкнулся в обе стороны.** Второе письмо
  пользователя подняло продакта уже без ручного действия: письмо отправлено
  22:31:40 UTC, ledger занял его в 22:35:02 — 3 мин 22 с, предыдущий прогон
  завершился в 22:24 с `agent_exit=0`, между ними два пустых тика. Пользователь
  подтвердил, что вложение открывается; сверх того продакт закрыл этот гейт
  своей рукой — отправил копию самому себе (`19fceecbc70a9076`), выгрузил её из
  Gmail и сверил md5 вложения с файлом на диске: совпадает. Значит проверка
  доставки вложения больше не требует пользователя. Счётчики: `total_ticks=6`,
  `empty_ticks=2`, `model_invocations=2` — пустые тики модель не трогают.
  Наблюдение целиком — второй раздел
  `tasks/727-mail-driven-product-owner/product-owner-observation.md`; статус
  задачи и `verification.md` продакт не трогал.
- 2026-08-05 — **727 отработала вживую: письмо пользователя подняло продакта.**
  Пользователь ответил на проверочное письмо, и дальше всё случилось само:
  расписание на хосте стоит (`crontab -l`, запись `*/5` с тегом
  `companion-agent-mail-product-owner`), poll забрал реальное письмо
  `19fcd7b1df959a6b` через Gmail API, выгрузил его в
  `.state/gmail/product-owner/inbox/`, поднял `claude --name product-owner-mail`
  и передал содержание — эта запись сделана именно тем агентом. Тик cron в
  00:20:03 (виден в syslog как `cron-root-root-0.service`) застал прогон живым и
  записал `{"state": "already_running"}` — одиночный экземпляр проверен не
  тестом, а планировщиком. Наблюдение целиком лежит в
  `tasks/727-mail-driven-product-owner/product-owner-observation.md`; статус
  задачи и `verification.md` продакт не трогал — это работа исполнителя.
  Осталось честно незакрытым: пятиминутная задержка «на холодную» (это
  пробуждение было ручным poll, а не тиком), час пустых тиков с нулём вызовов
  модели, дедуп на тике после выхода агента, подтверждение открываемости
  вложения получателем, одиночное «нужны вводные» и общий с 711/718 долг —
  кросс-ревью Claude. Все они закрываются наблюдением за ledger в ближайшие
  часы, кроме кросс-ревью; отдельного прогона под это не ставлю.
- 2026-08-05 — 727 (почта как вход продакта) лежит в коммите `399d5cc`: 8 фокусных
  тестов OK, включая «12 пустых тиков — ноль вызовов модели», дедуп письма,
  одиночный экземпляр, HTML-вложение. Все пять живых гейтов —
  BLOCKED, но по причине песочницы ребёнка, а не работы: продакт проверил на хосте
  сам — `gmail.googleapis.com` резолвится, `crontab` пишется. Держат задачу ровно
  две вещи: (1) установка расписания и одно настоящее письмо — это write-действие,
  нужно разрешение пользователя; (2) независимое ревью Claude работы Codex — тот же
  корень, что у 711 и 718, чинит 719. Продакт на доработку не возвращает.
- 2026-08-04 — 699 и 702 стоят не на технике: 699 ждёт, пока пользователь закоммитит или отложит четыре посторонних изменения в `companions/companion` (проверено `git status`), 702 ждёт решения по `RLIMIT_AS` общего `child_resource_limiter`, который с тех пор не менялся.
- 2026-08-04 — контракт завершения дважды за день съел готовую работу: 716 (ревьювер физически не мог записать артефакт в read-only) и 712 (матрица встала на 2/20, отвергнута с «status is 'planned'»). Общее — ребёнок выходит не по своей вине, а результат объявляется несостоявшимся целиком, без разделения «сделал часть» и «не сделал ничего». Владелец разбора — задача 543, предмет расширен. Пользовательский эффект пути «закрыть задачу с проверкой» отрицательный: гейт не защищает, а обнуляет.
- 2026-08-04 — 718 (проактивная продуктовая система) идёт живым прогоном dev-pipeline и был **невидим** для наблюдения тредов: проект `proactive-product-owner` не значился в `threads.json`, и продакт едва не доложил «ничего не крутится». Карта тредов исправлена, 718 и 719 теперь видны.
- 2026-08-04 — 711: Codex вносит принятые правки по кросс-ревью (записи решений правим в статусе и последствиях; Cursor остаётся до переноса в task-agent). Дальше — повторное ревью силами Claude.
- 2026-08-04 — пользователь сам закоммитил `d4ee45b`: доклад `docs/proactive-product-owner-architecture.html` — архитектура проактивного продакта (гибрид «события + обязательная сверка», единый продуктовый ledger, автономия по риску, roadmap из четырёх шагов). Это предложение пользователя, а не результат задачи: ни одного пути продукта оно пока не меняет, работа по нему не поставлена. Побочное наблюдение того же коммита: доклад лёг в ветку `task/708-lightweight-pipeline`, где 12 файлов правок 711 до сих пор не закоммичены, — то есть одобренная ревью работа существует только как рабочая копия.
- 2026-08-04 — 718 дошла до доставки: тот же доклад теперь существует как результат задачи, а не как черновик пользователя. Коммит `71c841c` содержит ровно тот файл, что ушёл в Telegram (SHA `efe040b2`, message_id 18236, ровно одна отправка по receipt). Все гейты verification — OK: трассировка R1–R12, девять сценариев, визуальная проверка 1440/390 без переполнения и ошибок. Долг: оба «независимых» ревью (`reviews/architecture/run-2.json`, `reviews/html/run-2.json`) выполнены `runtime: codex` — правило кросс-ревью не соблюдено, потому что рабочего пути к ревьюеру Claude нет (тот же корень, что блокирует 711 и что чинит 719). Прогон ещё дописывает durable-записи, статус задачи `in_progress`.

- 2026-08-04 — **решение продакта по 711**: ревью прошло без HIGH и без
  обязательных правок, значит по содержанию задача закрыта. Держит её только
  `repo_health`, и он валится исключительно на чужих старых снапшотах — продакт
  прогнал гейт сам, все 15 ошибок в артефактах задач 683, 255, 586, 587. Чистить
  их нельзя: `tasks/` durable. Поэтому сужение области гейта поставлено работой
  (723), а не вынесено пользователю ещё раз.
- 2026-08-04 — запущена 723: read-only ревьювер получает возможность дописать
  свой вердикт, контракт завершения учится отличать «не сделал» от «не дожил»,
  `repo-health` сужается до отслеживаемого дерева, плюс остаток находок ревью
  716 (M1, L2, L3, L4). Обязательное условие — отрицательный тест: гейт должен
  по-прежнему падать на настоящем секрете, иначе сужение области равно его
  выключению.
- 2026-08-04 — 718 закрыта терминально: `task.md` frontmatter `completed`,
  `status.json` `completed`, процесс не жив, durable-записи задачи и проекта
  `proactive-product-owner` дописаны. Принято: доклад у пользователя (SHA
  `efe040b2`, message_id 18236, ровно одна отправка), коммит `71c841c` содержит
  ровно этот файл. Долг подтверждён наблюдением, а не прозой: оба «независимых»
  ревью (`reviews/architecture/run-2.json`, `reviews/html/run-2.json`) имеют
  `runtime: codex` при авторе codex — кросс-ревью не состоялось. На доработку не
  возвращаю: ревьювер Claude физически недоступен (тот же корень, что чинят 719 и
  723), а повторный прогон на codex долг не снимет. Ревью Claude встаёт в очередь
  сразу после 719/723. Состояния путей не меняю: 718 — проект, а не изменение
  продукта.

- 2026-08-04 — 723 доведена по существу и стоит на одном шаге. По артефактам
  зелено: `repo-health` сужен до отслеживаемого дерева и при этом всё ещё падает
  на подложенном секрете (`docs/leak-probe.tmp.md`, exit 1, дерево вернулось к
  прежним 44 строкам `git status`), контракт завершения научился говорить
  «владелец остановился на 2 из 20, работа могла продолжиться» вместо «работа не
  сделана» (воспроизведено на реальном ledger 712), M1/L2/L3/L4 закрыты,
  регрессии 366 → 386 при нуле падений. Единственный GAP — живой read-only
  ребёнок — **закрыт продактом наблюдением, а не прозой**: probe запущен, ребёнок
  Claude через `Bash` записал файл в свой каталог задачи, а запись в предмет
  ревью вернула `Read-only file system`; native `Write` в `dontAsk` отклонён —
  ровно та граница, которую описывает реализация. Осталось: кросс-ревью на Codex
  (автор — Claude) и коммит 23 несохранённых файлов в
  `task/708-lightweight-pipeline`. Состояние пути «закрыть задачу с проверкой»
  не меняю: правки не в коммите и не отревьюены.

- 2026-08-04 — 727 (почта как вход продакта) встала blocked с готовой, но
  непроверенной вживую реализацией. По артефактам зелено только детерминированное:
  8 focused-тестов, из них 12 «пустых» тиков с нулём вызовов модели, дедуп одного
  письма, single-instance lock, at-most-once «нужны вводные», HTML во вложении;
  `repo_health` OK. Всё живое — BLOCKED не по технике, а по песочнице ребёнка:
  DNS до `gmail.googleapis.com` не резолвится, `/var/spool/cron/crontabs`
  read-only, поэтому пятиминутного расписания нет и ни одно настоящее письмо
  агента не поднимало. Плюс третий раз подряд не состоялось кросс-ревью: две
  попытки read-only ревьювера Claude не дали вывода (тот же корень, что чинит
  719). Правки не в коммите. На доработку не возвращаю — повтор в той же
  песочнице даст тот же результат; нужен запуск на хосте с сетью и правом
  поставить cron, это решение пользователя.

- 2026-08-04 — **719 закрыта продактом наблюдением из самой сессии.** Единственный
  оставшийся GAP (`user_surface_retest`) был не про технику, а про живой шелл:
  прошлая проверка шла из Bash, открытого до правки, и держала старую функцию в
  памяти. Свежий запуск прошёл через установленный launcher — проверено изнутри
  по собственному argv (`--dangerously-skip-permissions --setting-sources
  project`, новый стартовый промпт без мёртвых путей), родитель — новый Bash в
  новой ssh-сессии. Важное уточнение к прежним записям: 719 — про поверхность
  продакта, а не про ревьювера Claude. Кросс-ревью 711/718/727 разблокирует
  **723**, и она же — единственное, что осталось на этом пути.
- 2026-08-04 — 727: прогон завершился, ничего не поменяв в статусе — blocked с
  теми же тремя гейтами. Новое здесь — наблюдение продакта, а не проза
  исполнителя: с хоста `gmail.googleapis.com` резолвится, `crontab -l` отвечает
  «no crontab for root» (спул доступен на запись). То есть оба «мертвых» блокера
  ребёнка — свойство песочницы, а не задачи, и снимаются одним прогоном на хосте
  с сетью и правом поставить cron. Код лежит рабочей копией:
  `mail_product_owner.py`, `install_mail_poll_cron.py`, два теста — untracked, плюс
  правки `send_email.py`/`gmail_client.py`/SKILL. Третий гейт — кросс-ревью
  Claude — общий с 711 и 718 и ждёт 719. Жду решения пользователя по хост-прогону.

- 2026-08-05 — **двух продактов сразу.** Пока продакт вёл живой разговор и
  запускал 730, поступившее письмо разбудило второго продакта тем же путём, и он
  независимо завёл 731 — пересекающуюся по предмету с 730. Дубль разведён вручную
  (731 сужена до временного каталога и сети, старт после 730), но сам механизм
  остался: single-instance lock у поллера защищает от двух опросов ящика, а не от
  «продакт уже работает». Пока это не закрыто, письмо во время живой сессии может
  порождать параллельную и конкурирующую работу. Открытый вопрос, не поставлен.
- 2026-08-05 — **живой проверкой считается настоящая задача, а не юнит-набор.**
  Продакт предъявил 394 пройденных теста как «живые тесты» при коммите 723, тогда
  как настоящая проверка — read-only ребёнок, записавший свой `findings.md`
  обычным клиентским путём, — оставалась незакрытой. Ревью 728 независимо
  зафиксировало то же самое. Правило записано в `tasks/USER_PREFERENCES.md` и в
  AGENTS.md продакта.

## Журнал эффекта
- 2026-08-06 — доска отвечает, что можно взять в работу прямо сейчас, что стоит и за чем именно, и что обещано в продукте, но не заведено задачей; по плашке открывается карточка задачи, а страница обновляется сама (801).
- 2026-08-05 — пользователю больше не нужно спрашивать «что там происходит»: продакт сам присылает пуш и письмо, когда работа закончилась, встала или репозиторий двинулся, и молчит, когда ничего не изменилось.
- 2026-08-05 — отчёт, ушедший письмом, можно проверить без пользователя: продакт отправляет копию себе, выгружает и сверяет вложение байт в байт (727).
- 2026-08-05 — чтобы поручить работу продакту, больше не нужно открывать чат: письмо на ящик продакта поднимает его само, и он разбирается из содержания письма (727).
- 2026-08-04 — продакт открывается одной командой `claude-pm` из любого шелла: сразу с нужными правами, продуктовым каталогом и стартовой сводкой, без вопросов о разрешениях (719).
- 2026-08-04 — ревью цепочки 699–705 показало: гейты ловят процедурное, аварии находит пользователь. Пользовательского улучшения пока нет, есть диагноз.
- 2026-08-04 — доставлен доклад об архитектуре проактивного продакта: девять сквозных сценариев, модель отказов и roadmap, проверен визуально и отправлен один раз (718).

## Куда он развивался
По истории задач: от создания артефактов задач к делегированию детям, затем к
унификации `/codex` и `/task` на одном профилированном жизненном цикле (проект
`unified-task-execution`, задачи 676–683) и к разбору долга по незакрытым
задачам (`task-debt-cleanup`). То есть направление — не новые возможности, а
надёжность исполнения и восстановление после сбоя.

## Открытые вопросы
- Какие из находок 706 (AR-001..AR-004) превращаем в работу и в каком порядке.
- Сеть у ребёнка включается всем прогонам подряд или только тем, кому она нужна
  по предмету? Пользователь сказал «не ограничивать»; вопрос остаётся только о
  том, писать ли это правилом по умолчанию или флагом запуска (731).
